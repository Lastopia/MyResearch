# 前言

>目标是分析不同位置编码如何影响位置/内容信息的解耦，并进一步影响训练表现与 SAE 表征结构。

**配置**
``` python
models = ["rope", "pope", "alibi"]
seeds = [41,42,43]
train_steps = 30000
train_seq_len = 1024
sae_type = TopK
k = 64 
sae_catch = residual post block
sae_layer = [2,6,10]
dict_size = 4096
```
# 第一阶段：训练效果与训练稳定性

![Phase 2 Training Loss|487](output/task1/figures/summary/phase2/phase2_train_loss_all_models_seeds.png) ![Phase 2 Validation Loss|492](output/task1/figures/summary/phase2/phase2_valid_loss_all_models_seeds.png)

- 训练上，三个模型都能稳定下降，没有明显的差异，说明后续的问题并不是训练失败导致的
- 后续的结果可以证明`PoPE`训练的优化是否能转化成同等水平的验证泛化和SAE表征质量

- 验证上，各个模型不同seed的 Valid Loss 均值为 `ALibi`: 4.16 , `RoPE`: 4.19 , `PoPE`: 4.33
- `PoPE`的损失一直居高不下，泛化能力很差
- `RoPE`的损失较小，泛化能力尚可
- `ALibi` 的损失全阶段最小，泛化能力优秀

- 综上所述， 位置编码在同等的训练效果下，显著影响了模型的泛化和学习能力
- `PoPE`对位置和内容的解耦有可能阻碍了模型学习深层次的表征从而降低模型的泛化能力

| Metric                     |  ALibi |   RoPE |   PoPE |
| -------------------------- | -----: | -----: | -----: |
| Valid Loss                 |   4.16 |   4.19 |   4.33 |
| Perplexity                 |  64.01 |  66.20 |  75.90 |
| Loss Spike Count           |    297 |    283 |    271 |
| Gradient Norm Mean         | 1.1923 | 1.3184 | 1.3182 |
| Gradient Norm Variance     | 0.0462 | 0.0654 | 0.0637 |
| Train Loss Variance        | 0.5862 | 0.6120 | 0.5414 |
| Train efficiency (token/s) |   None |   None |   None |
| Convergence speed          |   None |   None |   None |

- PoPE Perplexity 显著升高
- Loss Spike Count 是训练中损失波动上升的次数，暗示优化稳定性，但看来`PoPE`的高稳定性更有可能是没有进行深层次的学习，缺乏有效的归纳偏置
- Gradient Norm Mean/Variance 暗示更新信号强弱，弱的话有可能是更加平稳，也有可能是学不动。RoPE 和 PoPE 类似，而 ALibi 则相较于两者更加稳定
- Train Loss Variance 数值越高，说明训练过程中波动越大
# 第二阶段：Attention 结构分析

| Metric                       | `alibi` | `pope` |  `rope` |
| ---------------------------- | ------: | -----: | ------: |
| Attention Distance           |   36.37 |  53.97 |  120.65 |
| Attention Entropy            |  2.6298 | 3.4314 |  4.1559 |
| Entropy Pattern Consistency  |  0.9486 | 0.0147 | -0.0052 |
| Distance Pattern Consistency |  0.9900 | 0.0281 |  0.0058 |
| Local@16 Pattern Consistency |  0.9964 | 0.0164 | -0.0026 |
| Spectral Concentration Top8  |  0.9425 | 0.6763 |  0.6086 |
| Toeplitz Deviation           |  2.4524 | 1.6134 |  1.7304 |
| Local Mass@4                 |  0.4619 | 0.4311 |  0.2285 |
| Local Mass@16                |  0.6847 | 0.6065 |  0.3836 |
| Local Mass@64                |  0.8559 | 0.7916 |  0.5931 |
| Far Attention Mass           |  0.0329 | 0.0563 |  0.1613 |
| Attention Sink Mass          |  0.0039 | 0.0035 |  0.0058 |

## Attention Distance

![Attention Distance|997](output/task1/figures/summary/phase3/phase3_seed42_distance_by_head_layers_x_models.png)

- Attention distance 衡量 query 关注 key 的平均距离。数值越高，说明模型更倾向长程注意力；数值越低，说明注意力更局部。

- 从图的形态上来看，ALibi更像是形成一个固定的注意力模版，不同注意力头之间的比例是固定的，会变化学习的只有注意力距离整体缩放的比例，而RoPE和PoPE不同头之间的注意力距离是在不断地变化的，反应模型在不断地学习如何调整注意力的距离。

- 相比之下，PoPE显著缺乏长程的注意力
## Attention Entropy

![Attention Entropy](output/task1/figures/summary/phase3/phase3_seed42_entropy_by_head_layers_x_models.png)

- Attention entropy 衡量注意力分布的分散程度。entropy 越高，注意力越分散；entropy 越低，注意力越集中。 entropy 低有可能表示有效聚焦，但是也有可能暗示了过强的结构偏置

- 类似前者，PoPE的entropy越低，现在看来并非是有效聚焦，而是结构偏置过强
## Attention Heatmap

![Attention Heatmap Head 0](output/task1/figures/summary/phase3/phase3_seed42_attention_heatmap_head0_layers_x_models.png)

- Attention heatmap 展示具体 head 在不同 layer 中的 query-key 注意力模式。对角线附近越亮，说明模型更关注附近 token；出现远距离条纹或块状结构，说明存在特殊长程或结构化注意力。

- 其实和上面的功能一样，且这张图是只有Head0的，ALibi 的Head0 看上去非常局部，但是Head7则会变得非常分散

> 一些意外的收货：
> - ALibi 的其中一个短板就是无法自适应调整注意力距离，我在想，是不是在超长上下文的处理中，RoPE能自适应学习到多个相似距离的head，学习一些重要的上下文距离的不同模式，而ALibi却是已经固定好的模版，且每个注意力距离只有一个head。
> - 控制ALibi的head的不同注意力距离的是每一个head的m参数，那是不是只要我们对m进行一些设计，并对一些重要的距离比如近距离多设置几个相似的或者说相同的m的heads，来让模型对同一个距离学习不同的注意力模式，会不会能够提升ALibi在超长上下文中的表现？
> - 貌似还真有人已经提出来了，就是对这个的改进方案，2025年的Cable模型，在ALibi的基础上添加了自适应的m
## Toeplitz Deviation

![Toeplitz Deviation](output/task1/figures/summary/phase3/phase3_seed42_toeplitz_deviation_by_head_layers_x_models.png)

- Toeplitz deviation 用于衡量 attention logits 是否接近只依赖相对距离的 Toeplitz 结构。数值越低，通常表示越接近相对位置平移结构；数值越高，表示偏离该结构更明显。它直接描述了位置编码带来的相对位置结构

- 平均 Toeplitz deviation：
	- `pope`：1.61
	- `rope`：1.73
	- `alibi`：2.45

- PoPE 在该指标上最低，说明它更接近规则的相对位置结构。但从图形形态看，PoPE 也不是所有 head 都一致低 deviation：中后层仍有个别 head 出现明显突起，说明其相对位置规则性并非均匀分布在所有 head 上，而是存在 head-level 差异。RoPE 在 L1 中有非常突出的异常高 deviation head，后续层则更平缓；ALiBi 的 deviation 随层加深整体变高，并且中间 head 更容易出现峰值。

- 这个结果需要和 spectral/entropy 图分开解释：ALiBi 有强线性位置 bias，但在当前 Toeplitz deviation 定义下并不最低；PoPE 更接近该指标所刻画的相对位置平移结构，但这种规则性没有带来更低的 valid loss，也没有带来更好的 SAE explained variance。这是一个值得强调的结果：更强的位置结构规则性不必然等于更好的训练效果或更好的稀疏表征。

## Attention Sink Mass

![Attention Sink Mass](output/task1/figures/summary/phase3/phase3_seed42_attention_sink_mass_by_head_layers_x_models.png)

- Attention sink mass 衡量attention 是真的在看有用上下文，还是有一部分 head 总是习惯性地把注意力丢到开头位置？该指标可帮助区分“有效局部注意力”和“退化的固定位置注意力”。

- 当前跨 seed、layer 的平均 attention sink mass：
	- `rope`：0.0058
	- `alibi`：0.0039
	- `pope`：0.0035

- attention sink mass 的绝对值都很低，说明三个模型都没有明显退化成固定关注开头 token 的模式。至于差异性还暂时不好评价，有时候开头位置或许确实固定存在更多需要值得注意的事情，这种规律可能被学到了也说不定
# 第三阶段：SAE 重构与特征健康

## SAE Validation MSE

![SAE Validation MSE](output/task1/figures/summary/phase4a/phase4a_validation_mse_mean.png)

- SAE validation MSE 衡量 SAE 对验证激活的重构误差。数值越低，说明 SAE 更容易重构该模型的 residual 表示；数值越高，说明表示更难被当前 SAE 配置压缩重构。

- 浅层更接近 token、标点、局部形式和短程模式；中层开始混合更多上下文、语法和位置/内容组合；深层更接近预测目标，通常更抽象、更混合，也更难被稀疏字典重构。

- 局限是它受 SAE 容量、top-k、训练步数影响，不能单独说明表示是否更可解释。

- 位置编码不仅改变 attention score 的结构，也会改变 block 输出 residual stream 的 SAE 可重构性，其中 RoPE 最适合当前 TopK SAE 重构，ALiBi 浅层较易重构但中深层复杂度上升明显，而 PoPE 从早层开始就表现出更强的位置/内容混合带来的重构困难。
## SAE Explained Variance

### 说明

Explained variance 表示 SAE 重构解释原始激活方差的比例。数值越高，说明 SAE 对表示结构捕捉得越好；数值越低，说明当前稀疏字典难以重构该表示。

该指标是 SAE 表征可重构性的核心指标。

局限是高 explained variance 不等于 feature 更语义化，也不等于位置/内容更解耦。

### 结果展示

![SAE Explained Variance](output/task1/figures/summary/phase4a/phase4a_explained_variance_mean.png)

跨 layer 平均 explained variance：

- `rope`：0.741
- `alibi`：0.723
- `pope`：0.709

RoPE 的 residual 表示最容易被当前 TopK SAE 重构。ALiBi 虽然训练效果最好，但 SAE explained variance 不是最高。这说明语言建模优势和 SAE 可重构性不是同一个维度。

图形形态上，三种模型的 explained variance 都随 layer 加深而下降，说明深层 residual 表示更难被当前 SAE 设置重构。ALiBi 的下降最陡：layer 2 与 RoPE 几乎重合，但到 layer 6 已经明显低于 RoPE，并在 layer 10 接近 PoPE。这与前面 attention 图形成互补：ALiBi 在 attention score 层面有稳定位置模板，但这种结构并没有转化为深层 residual 表示中更容易被 SAE 捕捉的稀疏结构。

## SAE Train / Valid MSE Curves

### 说明

SAE train/valid MSE 曲线展示 SAE 训练过程是否稳定，以及是否存在过拟合。train MSE 下降而 valid MSE 不降，可能说明 SAE 过拟合训练激活。

局限是曲线主要用于诊断 SAE 训练质量，而不是最终机制解释。

### 结果展示

![SAE Train MSE](output/task1/figures/summary/phase4a/phase4a_train_mse_curves.png)

![SAE Valid MSE](output/task1/figures/summary/phase4a/phase4a_valid_mse_curves.png)

曲线显示 SAE 训练整体正常，没有明显失效。形态上，train/valid MSE 都呈现早期快速下降、后期趋于平台的模式，说明 SAE 在前期迅速捕捉主要可重构结构，后期收益变小。部分 layer 后期 valid MSE 下降趋缓或略有回升，说明当前 2000 step 基本能训练出可用 SAE，但仍可能存在容量或稀疏度设置的影响。

因此这些曲线更像“SAE 训练已进入平台期”的诊断，而不是“SAE 完全收敛到最优”的证据。后续若要加强说服力，可以比较更长 SAE 训练或不同 top-k/dictionary size 是否改变各模型排序。

## Dead Feature Rate

### 说明

Dead feature rate 表示几乎不激活的 SAE 特征比例。数值越高，说明字典中无效特征越多；数值越低，说明字典利用率更高。

局限是 dead feature 很低不代表 feature 语义清晰，只说明特征被使用。

### 结果展示

![Dead Feature Rate](output/task1/figures/summary/phase4a/phase4a_dead_feature_rate_mean.png)

当前 dead feature rate 接近 0，图形上不同模型和不同层都贴近底部，没有某个模型或层出现明显高死特征率。说明 SAE 没有出现大量无效特征，SAE 训练质量基本可信。

这个图的主要价值是排除性证据：后续 SAE explained variance 或 feature ratio 的差异，不太可能是因为某个模型的 SAE 大量特征死亡导致的。

## Feature Frequency Entropy

### 说明

Feature frequency entropy 衡量 SAE feature 使用频率是否均匀。数值越高，说明 feature 使用更分散；数值越低，说明少数 feature 被过度使用。

该指标可用于观察不同位置编码是否导致 feature 使用集中化。

局限是高 entropy 不一定代表更可解释，只说明使用分布更均匀。

### 结果展示

![Feature Frequency Entropy](output/task1/figures/summary/phase4a/phase4a_feature_frequency_entropy_normalized_mean.png)

ALiBi 在 layer 2 的 feature frequency entropy 较低，同时 top feature activation frequency 较高，说明早层可能存在更集中的特征使用模式。这与 ALiBi attention 更集中、更局部的现象可能相关。

从图形形态看，feature frequency entropy 的模型差异主要集中在浅层；到了更深层，不同模型之间的差异变小。这说明 ALiBi 的“集中使用少数特征”的现象更像是早层效应，而不是贯穿所有层的全局现象。这个层依赖形态也与 explained variance 中 ALiBi 早层较强、深层优势消失的趋势一致。

## Top Feature Activation Frequency

### 说明

Top feature activation frequency 表示最常激活的 SAE feature 的激活频率。数值越高，说明少数 feature 更频繁地承担表示；数值越低，说明激活分布更均匀。

局限是该指标目前没有专门 summary 图，需要配合 feature entropy 解读。

### 结果展示

![Top Feature Activation Frequency](output/task1/figures/summary/phase4a/phase4a_top_feature_activation_frequency_mean.png)

当前跨 layer 平均值：

- `rope`：0.2429
- `alibi`：0.2428
- `pope`：0.1641

图形形态上，ALiBi 在浅层仍然表现出较高的 top feature activation frequency，说明早层表示更集中地依赖少数高频 SAE 特征；但跨层平均后，RoPE 与 ALiBi 几乎相同。PoPE 的 top feature activation frequency 明显更低，说明其 SAE 激活分布更分散。这个指标需要和 explained variance 一起看：PoPE 分散不等于更好，因为它的 SAE explained variance 仍然最低。

## Feature Reuse Rate

### 说明

Feature reuse rate 表示有多少 SAE feature 在足够多 token 上被复用。数值越高，说明特征复用更广；数值越低，说明更多特征只在少数样本上激活。

局限是复用率高低不直接代表语义质量。

### 结果展示

![Feature Reuse Rate](output/task1/figures/summary/phase4a/phase4a_feature_reuse_rate_mean.png)

当前跨 layer 平均值：

- `alibi`：0.9867
- `pope`：0.9866
- `rope`：0.9859

三个模型的 feature reuse rate 都接近 0.986，图形上也没有出现某个模型或某层显著塌缩。这说明当前 SAE 字典整体被充分使用，后续 explained variance、feature entropy、feature ratio 的差异不太可能来自大量 feature 没有被复用。

## SAE Dictionary Size Sweep

### 说明

建议补充 dictionary size = 2048 / 4096 / 8192。若某模型在更大字典下 explained variance 大幅提升，说明当前容量可能限制了它的 SAE 重构。

该指标用于排除“某模型 SAE 效果差只是因为容量不足”的解释。

### 结果展示

本轮已经运行 dictionary size = 2048 / 4096 / 8192，且固定 top-k = 64。主要结果如下：

| 模型 | 主配置 EV | dict size EV range | best dict size gain over primary | 解释 |
|---|---:|---:|---:|---|
| `rope` | 0.7409 | 0.0042 | 0.0003 | 字典容量变化几乎不改变 RoPE 的 SAE 重构排序 |
| `alibi` | 0.7225 | 0.0058 | 0.0006 | 对字典容量最敏感，但提升仍然很小 |
| `pope` | 0.7086 | 0.0049 | 0.0003 | 扩大字典没有显著修复 PoPE 的 SAE 重构劣势 |

从结果来看，dictionary size 的影响明显小于模型间差异。主配置下 SAE explained variance 排名仍为 `rope > alibi > pope`，而扩大到 8192 或缩小到 2048 都没有改变这个结论。这个结果很关键：它排除了“PoPE SAE 效果较差只是因为 4096 字典容量不合适”的主要反驳。

图形形态上，dictionary size sweep 更像是局部小幅扰动，而不是整体趋势反转。也就是说，SAE 容量不是当前三种位置编码差异的主导因素。

## SAE Top-k Sweep

### 说明

建议补充 top-k = 32 / 64 / 128。top-k 越小，稀疏性越强，重构更难；top-k 越大，重构更容易，但特征可能更不稀疏。

该指标用于判断不同位置编码下表示对稀疏约束的敏感性。

### 结果展示

本轮已经运行 top-k = 32 / 64 / 128，且固定 dictionary size = 4096。主要结果如下：

| 模型 | 主配置 EV | top-k EV range | best top-k gain over primary | 解释 |
|---|---:|---:|---:|---|
| `rope` | 0.7409 | 0.0958 | 0.0487 | top-k 越大，重构越好，但排序仍保持第一 |
| `alibi` | 0.7225 | 0.1092 | 0.0553 | 对 top-k 最敏感，说明其 residual 表示受稀疏约束影响更大 |
| `pope` | 0.7086 | 0.1080 | 0.0559 | 增大 top-k 能明显改善重构，但仍没有超过 RoPE |

top-k sweep 的影响明显大于 dictionary size sweep。形态上，top-k=128 普遍带来更高 explained variance，top-k=32 明显降低重构质量，这符合“更稀疏则更难重构”的预期。

但更重要的是：即使 top-k 改变，主结论仍没有被推翻。RoPE 仍是 SAE 重构最强，PoPE 仍最弱，ALiBi 居中。这说明当前 SAE 重构排序不是某个单一稀疏度设置导致的偶然结果。

## Feature Stability Across SAE Seeds

### 说明

建议比较不同 SAE seed 下 SAE feature 的相似性或功能分布稳定性。若某类位置编码下 feature 分布跨 SAE seed 更稳定，说明其表示结构更可复现。

局限是 feature matching 本身需要额外方法，例如 decoder cosine similarity、activation correlation 或 top-token overlap。

### 结果展示

![Matched Feature Fraction](output/task1/figures/summary/phase4b/phase4b_matched_feature_fraction_mean.png)

![Decoder Cosine Stability](output/task1/figures/summary/phase4b/phase4b_decoder_cosine_mean_mean.png)

![Activation Correlation Stability](output/task1/figures/summary/phase4b/phase4b_activation_correlation_mean_mean.png)

本轮使用主 SAE 配置 dictionary size = 4096、top-k = 64，在 SAE seed 42 / 43 / 44 之间做 mutual nearest neighbor feature matching。跨层平均结果如下：

| 模型 | matched feature fraction | decoder cosine mean | activation correlation mean | decoder cos >= 0.7 | activation corr >= 0.7 |
|---|---:|---:|---:|---:|---:|
| `alibi` | 0.4754 | 0.5588 | 0.4326 | 0.2382 | 0.2538 |
| `pope` | 0.4956 | 0.5705 | 0.4175 | 0.2646 | 0.2351 |
| `rope` | 0.4979 | 0.5686 | 0.4593 | 0.2441 | 0.2636 |

结果显示，大约一半 SAE feature 能通过 mutual nearest matching 找到稳定对应项。PoPE 和 RoPE 的 decoder 方向相似性略高，RoPE 的 activation correlation 最高，ALiBi 略低。

形态上，decoder cosine 与 activation correlation 并不完全一致：PoPE 的 decoder 方向稳定性较高，但激活相关性偏低；RoPE 的激活相关性更好。这说明“feature 方向相似”和“feature 在样本上实际激活相似”不是同一个问题。对论文来说，这个结果支持一个更细的结论：RoPE 不仅 SAE explained variance 更高，它的 SAE feature 在跨 seed 激活行为上也更稳定；PoPE 虽然能形成相似 decoder 方向，但这些方向对应的激活行为更不稳定。

# 第四阶段：位置/内容解耦分析

## Raw Position R2

### 说明

Raw position R2 衡量从原始 residual 表示中线性回归连续位置的能力。数值越高，说明连续位置信息越容易被读出；数值低或为负，说明该 probe 比均值预测还差。

局限是当前 R2 经常为负，因此不宜作为主结论指标。它可能受目标尺度、probe 训练、层内方差等因素影响。

### 结果展示

![Raw Position R2](output/task1/figures/summary/phase5/phase5_raw_position_r2_mean.png)

平均 raw position R2：

- `pope`：-0.069
- `rope`：-0.131
- `alibi`：-0.203

整体都不强，因此不能据此断言哪个模型“位置表示最好”。从图形形态看，三个模型的 raw position R2 都处在负值区域，说明连续位置线性回归整体不可靠；模型间差异更多是“谁没那么差”，而不是谁真正强。该指标需要补 baseline 和稳定性检查，不适合作为主结论。

## Raw Position Bin Accuracy

### 说明

Position bin accuracy 将位置离散成若干 bin 后做分类。数值越高，说明模型表示中更容易读出粗粒度位置信息。

相比 R2，该指标更稳健，适合作为位置可读性的主指标之一。

局限是它只表示粗粒度位置，不表示连续位置精度。

### 结果展示

![Raw Position Bin Accuracy](output/task1/figures/summary/phase5/phase5_raw_position_bin_accuracy_mean.png)

平均 raw position bin accuracy：

- `alibi`：0.121
- `rope`：0.115
- `pope`：0.104

粗粒度位置可读性整体不高，但 ALiBi 略高。从图形形态看，三种模型都随 layer 加深而下降，说明粗粒度位置信息在浅层更容易线性读出，深层逐渐减弱。ALiBi 在 layer 2 明显高于其他模型，但到 layer 6/10 差异缩小，说明 ALiBi 的位置偏置更明显地影响浅层 residual 表示，而不是在深层持续扩大位置可读性。

新增 majority baseline 后，raw position bin 的 baseline margin 平均值为：

- `alibi`：0.0588
- `rope`：0.0520
- `pope`：0.0418

这说明粗粒度位置 probe 虽然绝对 accuracy 不高，但确实超过简单多数类 baseline；因此它不是完全无效信号。ALiBi 的 margin 略高，说明其位置偏置在 raw residual 中留下了更可读的粗粒度位置信息，但差异仍然较小，不能单独作为强结论。

## Raw Token Category Accuracy

### 说明

Token category accuracy 衡量从 residual 表示中预测 token 内容类别的能力。数值越高，说明内容信息更容易被读出。

该指标用于与 position probe 对照，判断内容信息和位置信息在表示中的可读性差异。

局限是 token category 是粗类别，不等同于完整 token identity。

### 结果展示

![Raw Token Category Accuracy](output/task1/figures/summary/phase5/phase5_raw_token_category_accuracy_mean.png)

平均 raw token category accuracy：

- `rope`：0.975
- `alibi`：0.971
- `pope`：0.971

内容类别信息非常容易读出，远强于位置信息。从图形形态看，三种模型的 token category accuracy 都随 layer 加深而下降，但整体仍保持在很高水平。ALiBi 在 layer 2 最高，但下降最快；RoPE 在中深层保持最高，说明 RoPE 的深层 residual 表示对内容类别信息的线性保留更稳定。

新增 majority baseline 后，raw token category 的 baseline margin 平均值为：

- `rope`：0.1545
- `alibi`：0.1505
- `pope`：0.1503

这支持一个核心观察：当前 residual 表示中内容信息比绝对/连续位置信息更稳定、更可线性读取，而且明显超过多数类 baseline。同时，位置编码对内容信息的影响也具有层依赖性：ALiBi 早层内容可读性强，但深层不保持优势；RoPE 在中深层更稳定。

## SAE Position Bin Accuracy

### 说明

SAE position bin accuracy 衡量从 SAE feature 激活中预测位置 bin 的能力。数值越高，说明 SAE 稀疏特征保留了更多粗粒度位置信息。

局限是如果 SAE 特征主要重构内容或局部模式，位置 bin accuracy 可能较低。

### 结果展示

![SAE Position Bin Accuracy](output/task1/figures/summary/phase5/phase5_sae_position_bin_accuracy_mean.png)

平均 SAE position bin accuracy：

- `alibi`：0.095
- `rope`：0.093
- `pope`：0.089

SAE 特征中的位置可读性仍较弱，说明当前 TopK SAE 并没有把位置因素明显拆成强 position features。从图形形态看，SAE position bin accuracy 也随 layer 加深下降，且三种模型曲线距离较近。这说明 SAE 特征中的粗粒度位置信号并没有形成显著模型分离，至少在当前 probe 和 feature 分类设置下，位置解耦信号较弱。

新增 majority baseline 后，SAE position bin 的 baseline margin 平均值为：

- `alibi`：0.0329
- `rope`：0.0306
- `pope`：0.0262

因此 SAE 特征中的位置读出并非完全等同于多数类 baseline，但 margin 明显小于 raw residual。这说明 SAE 保留了一些粗粒度位置信息，但当前位置因素没有被强烈拆分成可线性读出的 feature 方向。

## SAE Token Category Accuracy

### 说明

SAE token category accuracy 衡量从 SAE feature 中预测内容类别的能力。数值越高，说明 SAE 稀疏特征保留了更多内容信息。

该指标可用于判断 SAE 是否主要保留内容结构。

### 结果展示

![SAE Token Category Accuracy](output/task1/figures/summary/phase5/phase5_sae_token_category_accuracy_mean.png)

平均 SAE token category accuracy：

- `rope`：0.962
- `alibi`：0.961
- `pope`：0.950

SAE 特征中内容信息仍然高度可读。相比位置指标，内容信息在 SAE 表示中保留得更稳定。从图形形态看，SAE token accuracy 同样随 layer 加深下降，但下降幅度小于位置指标，并且整体数值远高于 position bin accuracy。这说明 SAE 更稳定地保留内容类别信息，而不是位置结构。

新增 majority baseline 后，SAE token category 的 baseline margin 平均值为：

- `rope`：0.1417
- `alibi`：0.1409
- `pope`：0.1298

RoPE 与 ALiBi 接近，PoPE 略低。这与 SAE explained variance 中 RoPE 最好、PoPE 最弱的趋势一致，说明 PoPE 的 SAE 特征对内容类别信息的保留也相对弱一些。

## Mixed Feature Ratio

### 说明

Mixed feature ratio 表示同时与内容和位置相关的 SAE 特征比例。数值越高，说明内容和位置更容易混合在同一 feature 中；数值越低，说明混合特征更少。

该指标直接服务于“位置/内容解耦”的主题。

局限是 feature 类型划分依赖阈值，阈值选择会影响比例。

### 结果展示

![Mixed Feature Ratio](output/task1/figures/summary/phase5/phase5_mixed_feature_ratio_mean.png)

平均 mixed ratio：

- `alibi`：0.0224
- `rope`：0.0205
- `pope`：0.0186

混合特征比例整体很低，说明大部分 SAE feature 没有同时强烈携带位置和内容信号。ALiBi 略高，但差异较小。

从图形形态看，三种模型的 mixed ratio 都随 layer 加深下降，说明越深层越少有 feature 被当前规则标记为同时携带内容和位置。ALiBi 在 layer 2 的 mixed ratio 最高，之后快速下降但仍略高于其他模型；PoPE 全程最低。这提示 ALiBi 的强位置偏置可能在浅层带来更多内容/位置混合特征，但这个效应的绝对规模很小，不宜过度解释。

## Content-only Feature Ratio

### 说明

Content-only ratio 表示主要与内容相关、但不明显与位置相关的 SAE feature 比例。数值越高，说明内容因素更容易被单独拆出。

局限是该指标依赖内容分数定义，目前内容类别较粗。

### 结果展示

![Content-only Feature Ratio](output/task1/figures/summary/phase5/phase5_content_only_ratio_mean.png)

平均 content-only ratio：

- `pope`：0.0812
- `rope`：0.0794
- `alibi`：0.0776

三者差异较小。图形形态上，content-only ratio 与 position/mixed 指标类似，也呈现随层变化的趋势，说明 feature 类型比例不是固定属性，而是随层深改变。PoPE 的 content-only ratio 略高，但这没有带来更好的语言建模表现，因此不能把 content-only 比例直接等同于模型效果。

## Position-only Feature Ratio

### 说明

Position-only ratio 表示主要与位置相关、但不明显与内容相关的 SAE feature 比例。数值越高，说明位置因素更容易被 SAE 单独拆出。

该指标是判断位置/内容解耦的重要指标之一。

局限是当前 position probe 本身较弱，因此 position-only ratio 的解释需要谨慎。

### 结果展示

![Position-only Feature Ratio](output/task1/figures/summary/phase5/phase5_position_only_ratio_mean.png)

平均 position-only ratio：

- `pope`：0.0812
- `rope`：0.0794
- `alibi`：0.0776

position-only 和 content-only 的比例几乎相等，这是由当前分类规则决定的可能性较高。图形上 position-only ratio 与 content-only ratio 高度相似，说明当前 feature 类型划分可能受到阈值或互补规则影响，而不一定反映真实的语义/位置功能对称性。正式写作时需要说明该指标依赖阈值定义。

## Content-position Score Correlation

### 说明

Content-position score correlation 衡量 feature 的内容分数和位置分数是否相关。数值越高，说明内容和位置信号更容易一起出现；数值越低或接近 0，说明二者更独立。

该指标可以作为解耦程度的连续度量。

局限是相关性低不一定代表完全解耦，也可能是两个分数都较弱。

### 结果展示

暂未生成专门图。当前平均值：

- `alibi`：0.105
- `rope`：0.097
- `pope`：0.083

三者都为弱正相关，说明内容和位置分数并非强耦合。ALiBi 略高，可能与其更强的位置偏置和局部模式有关。

## Low-selectivity Feature Ratio

### 说明

Low-selectivity feature ratio 表示既不强烈对应内容、也不强烈对应位置的 SAE 特征比例。数值越高，说明当前指标无法解释的特征越多；数值越低，说明更多 feature 可被内容/位置维度解释。

局限是低选择性并不代表无意义，可能对应语法、频率、局部模式或更复杂语义。

### 结果展示

暂未生成专门 summary 图，但 `phase5_disentanglement_runs.csv` 中已经包含该指标。当前跨 layer 平均 low-selectivity ratio：

- `alibi`：0.8207
- `rope`：0.8184
- `pope`：0.8162

大约 82% 的 SAE feature 仍被当前规则归为 low-selectivity，说明仅用“内容类别/频率”和“位置相关性/位置 bin”还只能解释 SAE feature 的一小部分。这个结果不应被理解为这些 feature 无意义，而更可能说明当前定义的 content/position 指标仍然偏粗，需要结合 top activating contexts、token identity、语法模式或局部上下文模式继续解释。

## 推荐补强指标：Probe Baseline

### 说明

建议加入 random label baseline、majority baseline、shuffled position baseline。若真实 probe 仅略高于 baseline，则不能说明模型真的编码了对应信息。

这是当前最需要补强的部分，尤其因为 position R2 出现负值。

### 结果展示

![Raw Position Bin Baseline Margin](output/task1/figures/summary/phase5/phase5_raw_position_bin_baseline_margin_mean.png)

![SAE Position Bin Baseline Margin](output/task1/figures/summary/phase5/phase5_sae_position_bin_baseline_margin_mean.png)

本轮已经加入 probe baseline：连续位置回归使用均值 baseline，分类 probe 使用训练集多数类 baseline。关键结论是：位置类指标虽然绝对数值不高，但多数分类指标确实高于 majority baseline。

平均 baseline margin：

| 指标 | `alibi` | `pope` | `rope` |
|---|---:|---:|---:|
| raw position bin | 0.0588 | 0.0418 | 0.0520 |
| SAE position bin | 0.0329 | 0.0262 | 0.0306 |
| raw segment position | 0.1208 | 0.1073 | 0.1238 |
| SAE segment position | 0.0612 | 0.0480 | 0.0520 |

这说明 position probe 不是完全无效，但 SAE 表示中的位置 margin 明显低于 raw residual，说明 SAE 没有把位置因素强烈保留下来或拆分出来。ALiBi 在 bin position 上略高，RoPE 在 raw segment position 上略高；这些差异都不大，适合做辅助证据，不适合作为单独主结论。

## 推荐补强指标：Segment Position Accuracy

### 说明

将位置分为 early/middle/late 三段，训练分类 probe。该指标比连续 R2 更稳健，也比 16-bin 分类更容易解释。

数值越高，说明模型更容易编码粗粒度位置段。

### 结果展示

![Raw Segment Position Accuracy](output/task1/figures/summary/phase5/phase5_raw_segment_position_accuracy_mean.png)

![Raw Segment Position Baseline Margin](output/task1/figures/summary/phase5/phase5_raw_segment_position_baseline_margin_mean.png)

![SAE Segment Position Accuracy](output/task1/figures/summary/phase5/phase5_sae_segment_position_accuracy_mean.png)

当前 segment position accuracy 平均值：

- raw：`rope` 0.4577，`alibi` 0.4548，`pope` 0.4413
- SAE：`alibi` 0.3952，`rope` 0.3860，`pope` 0.3820

baseline margin 平均值：

- raw：`rope` 0.1238，`alibi` 0.1208，`pope` 0.1073
- SAE：`alibi` 0.0612，`rope` 0.0520，`pope` 0.0480

图形形态上，segment position 的 baseline margin 随 layer 加深明显下降，说明粗粒度位置段信息主要集中在浅层。Raw residual 中 RoPE 的中层位置段读出更强；SAE 特征中 ALiBi 略高，说明 ALiBi 的位置偏置可能让 SAE 保留了稍多粗粒度位置段信息，但整体 margin 仍然有限。

## 推荐补强指标：Token Identity / Frequency Probe

### 说明

建议加入 token identity 或 token frequency bin probe，用于区分 SAE feature 是否主要编码 token 内容、频率或词形类别。

这有助于解释 low-selectivity feature 可能并非无意义，而是当前内容类别太粗。

### 结果展示

![Raw Token Frequency Bin Accuracy](output/task1/figures/summary/phase5/phase5_raw_token_frequency_bin_accuracy_mean.png)

![SAE Token Frequency Bin Accuracy](output/task1/figures/summary/phase5/phase5_sae_token_frequency_bin_accuracy_mean.png)

![SAE Top Token Identity Baseline Margin](output/task1/figures/summary/phase5/phase5_sae_top_token_identity_baseline_margin_mean.png)

当前 token frequency bin accuracy 平均值：

- raw：`alibi` 0.8633，`rope` 0.8632，`pope` 0.8592
- SAE：`rope` 0.9237，`alibi` 0.9192，`pope` 0.9111

当前 top-token identity accuracy 平均值：

- raw：`rope` 0.9575，`pope` 0.9542，`alibi` 0.9529
- SAE：`rope` 0.9190，`alibi` 0.9024，`pope` 0.8771

这些内容/频率相关 probe 的 baseline margin 明显高于位置 probe。例如 SAE top-token identity baseline margin：`rope` 0.2523，`alibi` 0.2356，`pope` 0.2104。图形上，RoPE 在中深层的 token identity 保留最强，PoPE 最弱，ALiBi 早层较强但下降较快。这进一步支持：SAE 特征更稳定地保留 token 内容/频率信息，而不是位置结构；PoPE 的 SAE 表示在内容身份保留上也偏弱。

## Feature Overlap Across SAE Seeds

### 说明

建议比较不同 SAE seed 下 content-only、position-only、mixed feature 的重合程度。若某类 feature 跨 seed 稳定，说明它更可能是位置编码导致的可靠结构。

### 结果展示

![Feature Label Overlap](output/task1/figures/summary/phase5/phase5_overlap_same_label_fraction.png)

![Content-only Preservation](output/task1/figures/summary/phase5/phase5_overlap_content_only_preservation_fraction.png)

![Position-only Preservation](output/task1/figures/summary/phase5/phase5_overlap_position_only_preservation_fraction.png)

![Mixed Preservation](output/task1/figures/summary/phase5/phase5_overlap_mixed_preservation_fraction.png)

本轮在 decoder cosine mutual nearest matching 后，统计匹配 feature 的 label 是否保持一致。跨层平均结果如下：

| 模型 | same label fraction | content-only preservation | position-only preservation | mixed preservation | low-selectivity preservation |
|---|---:|---:|---:|---:|---:|
| `alibi` | 0.8217 | 0.5855 | 0.3960 | 0.3553 | 0.9021 |
| `pope` | 0.8206 | 0.5949 | 0.3770 | 0.3660 | 0.9029 |
| `rope` | 0.8255 | 0.5818 | 0.3811 | 0.4088 | 0.9056 |

逐指标来看：

**same label fraction** 衡量所有自动匹配 feature 中，有多少在另一个 SAE seed 中仍保持相同 label。数值越高，说明整体 feature 类型分布越稳定；数值越低，说明 feature 对齐后类型容易改变。当前三者都在 0.82 左右，`rope` 最高 0.8255，`alibi` 0.8217，`pope` 0.8206。图形上三条曲线/柱子的距离很近，说明整体 label overlap 并没有形成强烈模型分化。这个指标支持“SAE feature 类型分布有一定跨 seed 稳定性”，但它不能说明是哪一类 feature 稳定，因此必须拆开看下面几个 preservation 指标。

**content-only preservation** 衡量一个 seed 中被标为 content-only 的 feature，在匹配到另一个 seed 后仍然是 content-only 的比例。数值越高，说明内容型 feature 更可复现；数值越低，说明内容型 feature 容易在另一个 SAE seed 中变成 mixed、low-selectivity 或其他类型。当前 `pope` 最高 0.5949，`alibi` 0.5855，`rope` 0.5818，三者都接近 0.58-0.59。形态上 content-only preservation 明显低于 overall same label fraction，但高于 position-only 和 mixed，说明内容型 feature 有中等稳定性：不是完全随机，但也不是非常稳固的 feature identity。对论文来说，这说明“内容信息在 SAE 中更容易被读出”并不等于“每个内容 feature 都能跨 seed 一一复现”。

**position-only preservation** 衡量 position-only feature 跨 SAE seed 后是否仍保持 position-only。数值越高，说明纯位置 feature 更可复现；数值越低，说明纯位置 feature identity 不稳定。当前 `alibi` 最高 0.3960，`rope` 0.3811，`pope` 0.3770，整体只有约 0.38-0.40。这个结果比 content-only 明显更低，说明纯位置 feature 的跨 seed 稳定性较弱。结合前面的 position probe margin 较小，可以得到更稳妥的结论：SAE 中确实存在一些位置相关 feature，但它们不是很强、也不是高度可复现的离散 feature。

**mixed preservation** 衡量同时带有内容和位置信号的 mixed feature 是否跨 seed 保持 mixed。数值越高，说明内容/位置混合型 feature 更稳定；数值越低，说明混合结构容易被不同 SAE seed 拆成不同形态。当前 `rope` 最高 0.4088，`pope` 0.3660，`alibi` 0.3553。图形上 mixed preservation 整体也偏低，但 RoPE 略高。这和 RoPE 在 SAE explained variance、top-token identity、activation correlation 上的优势方向一致：RoPE 的 residual 表示更容易形成跨 seed 稍微稳定一些的 SAE 激活结构。不过 mixed preservation 最高也只有约 0.41，因此不能把 mixed feature 写成强稳定对象。

**low-selectivity preservation** 衡量 low-selectivity feature 是否跨 seed 仍保持 low-selectivity。数值越高，说明低选择性背景 feature 很稳定；数值越低，说明大量低选择性 feature 会转成高选择性类别。当前三者都约 0.90，`rope` 0.9056，`pope` 0.9029，`alibi` 0.9021。这个指标远高于 content-only、position-only 和 mixed，是 overall same label fraction 能达到 0.82 的主要原因。形态上看，low-selectivity preservation 是明显的高平台，说明 SAE 中最稳定的并不是我们最关心的高选择性解耦 feature，而是大量低选择性、背景性的 feature。

综合这些指标，Feature Overlap 的结论不是简单“稳定”或“不稳定”，而是分层的：整体 label overlap 看起来较高，但主要由 low-selectivity feature 支撑；content-only 有中等稳定性；position-only 和 mixed 稳定性较弱。对论文写法来说，这一点很关键：我们可以说不同位置编码导致的 SAE feature 分布和 probe 趋势具有可比较性，但不能把单个 position-only 或 mixed feature 当作高度稳定、跨 seed 唯一对应的对象。

# 第五阶段：解释性案例分析

## LLM Feature Interpretation（未运行）

### 说明

LLM feature interpretation 通过抽取高激活上下文，让语言模型解释 SAE feature 可能对应的语义或模式。该指标可把统计性的 content/position 分数转化为可读案例。

高质量解释有助于判断 feature 是否真的对应位置、内容、语法或局部结构。

局限是解释依赖外部模型，可能有幻觉，需要配合定量指标。

### 结果展示

暂未。当前 `run_interpret=false`，解释阶段没有运行。

## Feature Case Study

### 说明

建议为每个模型和每层选取若干代表性 feature：高 content score、高 position score、高 mixed score、low-selectivity 但高频 feature。展示其 top activating contexts。

该分析可直接服务于论文叙述：不同位置编码是否改变 SAE feature 的语义类型。

### 结果展示

本轮已经生成 `phase5_feature_case_studies.json` 和 `phase5_feature_case_studies.csv`，共 9720 条 top activating context 记录。每个模型在 content-only、position-only、mixed、low-selectivity 四类上都生成了 810 条案例素材。

| 模型 | content-only | position-only | mixed | low-selectivity |
|---|---:|---:|---:|---:|
| `alibi` | 810 | 810 | 810 | 810 |
| `pope` | 810 | 810 | 810 | 810 |
| `rope` | 810 | 810 | 810 | 810 |

部分示例：

| 模型 | label | layer | feature | center token | 示例上下文 |
|---|---|---:|---:|---|---|
| `rope` | content-only | 2 | 3184 | `).` | `Masterpiece Collection toys released at the time). Each character offers varying levels of Power (` |
| `rope` | position-only | 2 | 3672 | `making` | `and the Asus ZenFone 2 are making big splashes, especially among the Android` |
| `rope` | mixed | 2 | 348 | 换行 | `strom," he said. The national department store chain dropped the` |
| `alibi` | position-only | 2 | 3229 | `strom` | `strom," he said. The national` |
| `pope` | mixed | 2 | 2146 | 换行 | `strom," he said. The national department store chain dropped the` |

目前这些 case study 更适合作为“候选案例池”，还不能直接当作最终语义解释。形态上可以看到，高激活上下文经常落在标点、换行、局部短语边界、专名片段或常见叙述模板上。这与前面的 probe 结果是一致的：SAE feature 中稳定可读的内容/频率/局部格式信息比纯位置因素更强。

但是，真正要在论文中展示个别 feature 的语义解释，还需要下一步对这些 top contexts 做人工筛选或 LLM interpretation。否则只展示 top context 容易显得碎片化。

# 第六阶段：建议补强实验

## SAE 容量/稀疏度敏感性

### 说明

建议在当前主实验基础上，少量补充 dictionary size 或 top-k sweep。优先级高于长度外推，因为它直接关系到 SAE 结论是否稳健。

### 结果展示

本轮已经真正运行 dictionary size 和 top-k sweep，并生成 `phase4a_sae_sweep_conclusions.json`。

跨 layer 聚合结果：

| 模型 | 主配置 EV mean | 主配置 EV rank | dictionary size EV range | dictionary size best gain | top-k EV range | top-k best gain |
|---|---:|---:|---:|---:|---:|---:|
| `rope` | 0.7409 | 1 | 0.0042 | 0.0003 | 0.0958 | 0.0487 |
| `alibi` | 0.7225 | 2 | 0.0058 | 0.0006 | 0.1092 | 0.0553 |
| `pope` | 0.7086 | 3 | 0.0049 | 0.0003 | 0.1080 | 0.0559 |

分 layer 结果：

| 模型 | layer | 主配置 EV | best dict size | dict EV range | dict best gain | best top-k | top-k EV range | top-k best gain |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `rope` | 2 | 0.7880 | 4096 | 0.0035 | 0.0000 | 128 | 0.0812 | 0.0409 |
| `rope` | 6 | 0.7340 | 4096 | 0.0029 | 0.0000 | 128 | 0.0925 | 0.0475 |
| `rope` | 10 | 0.7009 | 2048 | 0.0063 | 0.0008 | 128 | 0.1138 | 0.0576 |
| `alibi` | 2 | 0.7874 | 4096 | 0.0031 | 0.0000 | 128 | 0.0896 | 0.0445 |
| `alibi` | 6 | 0.6952 | 2048 | 0.0049 | 0.0002 | 128 | 0.1118 | 0.0579 |
| `alibi` | 10 | 0.6849 | 2048 | 0.0094 | 0.0016 | 128 | 0.1262 | 0.0636 |
| `pope` | 2 | 0.7268 | 4096 | 0.0036 | 0.0000 | 128 | 0.1026 | 0.0534 |
| `pope` | 6 | 0.7109 | 4096 | 0.0030 | 0.0000 | 128 | 0.1001 | 0.0527 |
| `pope` | 10 | 0.6882 | 2048 | 0.0080 | 0.0008 | 128 | 0.1213 | 0.0615 |

自动摘要给出的三个核心结论是：

- Primary SAE explained-variance ranking：`rope > alibi > pope`
- Dictionary-size sensitivity ranking：`alibi > pope > rope`
- Top-k sensitivity ranking：`alibi > pope > rope`

从数据上看，dictionary size 的影响非常小：跨层 EV range 只有 0.004-0.006 左右，best gain 基本接近 0。即使在最敏感的 `alibi` layer 10，dictionary size best gain 也只有 0.0016。这说明把字典从 2048 改到 8192，并没有实质改变 SAE 重构质量，更没有改变 `rope > alibi > pope` 的主排序。

top-k 的影响明显更大：跨层 EV range 达到 0.096-0.109，best gain 约 0.049-0.056。所有模型、所有 layer 的 best top-k 都是 128，说明更宽松的稀疏约束可以显著提高重构质量。形态上，top-k sweep 呈现一致的单调改善趋势，而 dictionary size sweep 更像小幅波动。

但更重要的是，改变容量或稀疏度后，主排序没有反转：RoPE 仍最适合当前 SAE 重构，PoPE 仍最弱。也就是说，当前 SAE 差异不是单一 dictionary size 或 top-k 选择造成的偶然结果。

因此，这部分现在可以作为 SAE 结论的稳健性证据写入论文：当前 SAE 差异不是单一 dictionary size 或 top-k 选择造成的偶然结果。

## 解耦指标可靠性检查

### 说明

建议优先补 probe baseline、segment position accuracy、train/valid probe gap。当前 position R2 不稳定，这部分会直接影响论文说服力。

### 结果展示

本轮已经补上 probe baseline、segment position accuracy、token frequency bin probe 和 top-token identity probe。结果表明：

| 指标 | `alibi` | `pope` | `rope` | 主要含义 |
|---|---:|---:|---:|---|
| raw position bin baseline margin | 0.0588 | 0.0418 | 0.0520 | raw residual 中粗粒度位置读出略高于多数类 baseline |
| SAE position bin baseline margin | 0.0324 | 0.0267 | 0.0314 | SAE 中位置读出仍存在，但比 raw residual 弱 |
| raw segment position baseline margin | 0.1208 | 0.1073 | 0.1238 | 三段式位置比 16-bin 更稳定，RoPE/ALiBi 略高 |
| SAE segment position baseline margin | 0.0576 | 0.0526 | 0.0569 | SAE 保留部分粗粒度位置段信息，但 margin 明显下降 |
| SAE token category baseline margin | 0.1409 | 0.1298 | 0.1417 | SAE 中内容类别明显比位置更可读 |
| SAE token frequency baseline margin | 0.4216 | 0.4110 | 0.4265 | token 频率信息非常强，远高于位置 margin |
| SAE top-token identity baseline margin | 0.2358 | 0.2134 | 0.2547 | token 身份信息保留明显，RoPE 最强、PoPE 最弱 |

- 位置类 probe 超过 majority baseline，但 margin 明显小于内容/频率类 probe。
- SAE 中的位置 baseline margin 低于 raw residual，说明 SAE 没有强烈保留或拆分位置因素。
- 内容、频率、top-token identity 在 raw residual 和 SAE feature 中都更稳定可读。

因此，当前解耦结论比之前更稳：位置编码确实显著改变 attention 结构，但 SAE 中稳定保留的主要还是内容/频率信息；位置因素在 SAE feature 中存在，但不是强主导信号。

#

### Phase 2: Attention Structure

| Metric                       | `alibi` | `pope` |  `rope` |
| ---------------------------- | ------: | -----: | ------: |
| Attention Distance           |   36.37 |  53.97 |  120.65 |
| Attention Entropy            |  2.6298 | 3.4314 |  4.1559 |
| Entropy Pattern Consistency  |  0.9486 | 0.0147 | -0.0052 |
| Distance Pattern Consistency |  0.9900 | 0.0281 |  0.0058 |
| Local@16 Pattern Consistency |  0.9964 | 0.0164 | -0.0026 |
| Spectral Concentration Top8  |  0.9425 | 0.6763 |  0.6086 |
| Toeplitz Deviation           |  2.4524 | 1.6134 |  1.7304 |
| Local Mass@4                 |  0.4619 | 0.4311 |  0.2285 |
| Local Mass@16                |  0.6847 | 0.6065 |  0.3836 |
| Local Mass@64                |  0.8559 | 0.7916 |  0.5931 |
| Far Attention Mass           |  0.0329 | 0.0563 |  0.1613 |
| Attention Sink Mass          |  0.0039 | 0.0035 |  0.0058 |

### Phase 3: SAE Reconstruction, Sweep, Stability

| Metric | `alibi` | `pope` | `rope` |
|---|---:|---:|---:|
| SAE Validation MSE | 0.2773 | 0.2907 | 0.2638 |
| SAE Explained Variance | 0.7220 | 0.7083 | 0.7404 |
| SAE EV Rank | 2.0000 | 3.0000 | 1.0000 |
| Dead Feature Rate | 0.0003 | 0.0002 | 0.0003 |
| Feature Reuse Rate | 0.9715 | 0.9698 | 0.9714 |
| Top Feature Activation Frequency | 0.2308 | 0.1743 | 0.2419 |
| Feature Frequency Entropy | 0.9554 | 0.9623 | 0.9583 |
| Dict-size EV Range | 0.0058 | 0.0049 | 0.0042 |
| Dict-size Best Gain | 0.0006 | 0.0003 | 0.0003 |
| Top-k EV Range | 0.1092 | 0.1080 | 0.0958 |
| Top-k Best Gain | 0.0553 | 0.0559 | 0.0487 |
| Matched Feature Fraction | 0.4754 | 0.4956 | 0.4979 |
| Decoder Cosine Stability | 0.5588 | 0.5705 | 0.5686 |
| Activation Correlation Stability | 0.4326 | 0.4175 | 0.4593 |
| Decoder Cosine >= 0.7 | 0.2382 | 0.2646 | 0.2441 |
| Activation Corr >= 0.7 | 0.2538 | 0.2351 | 0.2636 |

### Phase 4: Disentanglement and Overlap

| Metric | `alibi` | `pope` | `rope` |
|---|---:|---:|---:|
| Raw Position R2 | -0.2031 | -0.0690 | -0.1314 |
| Raw Position-bin Margin | 0.0588 | 0.0418 | 0.0520 |
| Raw Segment-position Margin | 0.1208 | 0.1073 | 0.1238 |
| Raw Token-category Margin | 0.1505 | 0.1503 | 0.1545 |
| Raw Token-frequency Margin | 0.3655 | 0.3614 | 0.3654 |
| Raw Top-token Margin | 0.2862 | 0.2874 | 0.2907 |
| SAE Position-bin Margin | 0.0324 | 0.0267 | 0.0314 |
| SAE Segment-position Margin | 0.0576 | 0.0526 | 0.0569 |
| SAE Token-category Margin | 0.1382 | 0.1321 | 0.1432 |
| SAE Token-frequency Margin | 0.4216 | 0.4110 | 0.4265 |
| SAE Top-token Margin | 0.2358 | 0.2134 | 0.2547 |
| Mixed Feature Ratio | 0.0212 | 0.0187 | 0.0205 |
| Content-only Ratio | 0.0787 | 0.0811 | 0.0794 |
| Position-only Ratio | 0.0787 | 0.0811 | 0.0794 |
| Content-position Score Corr | 0.1065 | 0.0773 | 0.1035 |
| Mean Content Score | 0.0054 | 0.0055 | 0.0058 |
| Mean Position Score | 0.0229 | 0.0246 | 0.0222 |
| Overlap Same-label Fraction | 0.8217 | 0.8206 | 0.8255 |
| Overlap Content-only Preservation | 0.5855 | 0.5949 | 0.5818 |
| Overlap Position-only Preservation | 0.3960 | 0.3770 | 0.3811 |
| Overlap Mixed Preservation | 0.3553 | 0.3660 | 0.4088 |
| Overlap Low-selectivity Preservation | 0.9021 | 0.9029 | 0.9056 |

### Phase 5: Case Study

| Metric | `alibi` | `pope` | `rope` |
|---|---:|---:|---:|
| Feature Case-study Contexts | 3240 | 3240 | 3240 |

# 总结

综合目前 Task1 的结果，可以得到一个更稳的主线：不同位置编码确实显著改变了 attention 层面的位置信息组织方式，但这种改变不会简单等价为“训练效果更好”或“SAE 表征更好”。本轮新增的 checkpoint reuse audit 显示 `all_reused_without_retraining=true`，所有 base model checkpoint 在本轮运行中都没有被修改。因此新增的 SAE sweep、feature stability、feature overlap 和 case study 可以被解释为已有 checkpoint 上的后处理与 SAE 训练差异，而不是重新训练语言模型导致的变化。

第一，训练与泛化结果仍显示，ALiBi 的 final valid loss 最低，RoPE 次之，PoPE 最高；但训练 loss 曲线没有同样清晰分层。这说明 PoPE 的问题不太像是训练失败，而更像是训练集拟合可以进行、但泛化和表示组织没有同等受益。ALiBi 的优势也不应被写成简单“模型更强”，而应写成“该位置偏置在当前训练预算下带来了更有利的泛化归纳偏置”。

第二，attention 结构仍是最强的机制证据。ALiBi 的 entropy、distance、local mass、far mass 呈现高度稳定的 head-wise 模板：有些 head 长期局部，有些 head 专门承担远程注意力，而且这个相对比例跨层重复。量化上，ALiBi 的 entropy pattern consistency 约 0.949，distance pattern consistency 约 0.990，local_mass_16 pattern consistency 约 0.996；RoPE 和 PoPE 基本接近 0。这说明 ALiBi 不是只让 attention 更局部，而是把 head 的位置尺度分工稳定地写进了 attention score 结构中。

第三，SAE 结果没有跟随语言建模性能排序，而且这个结论经过 sweep 后更稳。主配置下 SAE explained variance 排名为 `rope > alibi > pope`；dictionary size sweep 显示容量变化带来的 EV range 很小，RoPE 约 0.0042，ALiBi 约 0.0058，PoPE 约 0.0049；top-k sweep 的影响更大，三者 EV range 约 0.096-0.109，但排序仍没有反转。这说明 RoPE 的 SAE 重构优势不是单一 dictionary size 或 top-k 配置造成的偶然结果。

第四，跨 SAE seed 的稳定性分析补强了 SAE 结论，但也提醒我们不要过度解释单个 feature identity。约一半 feature 可以通过 decoder cosine mutual nearest matching 找到对应项；RoPE 的 activation correlation mean 最高，约 0.459，ALiBi 约 0.433，PoPE 约 0.418。Feature label overlap 约 0.82，但主要由 low-selectivity feature 的高稳定性贡献；content-only preservation 约 0.58-0.59，position-only 约 0.38-0.40，mixed 约 0.36-0.41。因此更稳妥的表述是：SAE 的整体功能分布和趋势可比较，但单个高选择性 feature 的身份不应被当作完全稳定的对象。

第五，位置/内容解耦结果仍表明，内容和频率信息比位置信息更稳定地进入 residual 与 SAE feature。SAE position bin baseline margin 约 0.027-0.032，SAE segment position margin 约 0.053-0.058；相比之下，SAE token frequency margin 约 0.411-0.427，top-token identity margin 约 0.213-0.255。这支持一个关键结论：位置编码差异主要强烈体现在 attention 结构上，而在 SAE feature 层面，最稳定保留的是内容、频率和 token identity 信息。

第六，PoPE 的结果尤其值得谨慎分析。PoPE 的 Toeplitz deviation 最低，说明它更接近某种规则的相对位置结构；但它的 valid loss、SAE explained variance、SAE top-token identity margin 都不占优。SAE sweep 也没有把 PoPE 的排序翻转。这说明“更规则的位置结构”不必然带来更好的泛化或更好的 SAE 特征组织。这个发现对论文很有价值，因为它能避免把位置编码分析写成单纯性能排名，而是强调位置结构、内容保留、稀疏可解释性之间存在复杂关系。

总体而言，目前最有价值的论文叙述应当是：ALiBi 通过强而稳定的 attention head 尺度模板改变信息流，并带来更好的语言建模泛化；RoPE 的 residual 表示更适合当前 TopK SAE 重构，并且跨 SAE seed 的激活稳定性更强；PoPE 虽然更接近某些相对位置结构指标，但没有转化为更好的泛化、SAE 重构或 token identity 保留。由此可以支撑本文的核心问题：位置编码影响的不只是模型性能，而是位置结构如何进入 attention、如何与内容信息分离或混合，以及这些结构最终是否被 SAE 捕捉为可解释特征。
