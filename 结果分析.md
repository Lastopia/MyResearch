# 结果分析

本文档面向 `output/task1` 的当前实验结果。实验核心不是比较 `rope`、`pope`、`alibi` 谁的语言建模性能更好，而是分析不同位置编码如何影响位置/内容信息的解耦，并进一步影响训练表现与 SAE 表征结构。

当前 Task1 设置：

- 模型：`rope`、`pope`、`alibi`
- 随机种子：41、42、43
- 训练步数：30000
- 训练序列长度：1024
- SAE 层：2、6、10
- SAE 设置：TopK SAE，dictionary size 4096，top-k 64
- 解释阶段：暂未运行

# 第一阶段：训练效果与训练稳定性

## 训练损失曲线

### 说明

训练损失曲线用于观察模型在优化过程中的收敛趋势。损失下降越快、曲线越平滑，通常说明训练过程更稳定；损失长期较高或波动剧烈，可能说明模型结构、位置编码或优化动态存在额外困难。

局限是训练损失只反映训练集拟合，不能单独说明泛化能力，也不能直接说明位置/内容是否解耦。本文中它主要作为“训练动态结果变量”，用来观察不同位置编码造成的优化差异。

### 结果展示

![Phase 2 Training Loss](output/task1/figures/summary/phase2/phase2_train_loss_all_models_seeds.png)

从图中看，三个模型都能稳定下降，没有出现发散。曲线形态上，所有模型在早期快速下降阶段几乎重合，随后进入缓慢下降的长尾阶段；不同 seed 之间的波动大于模型间的训练曲线形态差异。`alibi`、`rope` 和 `pope` 在训练 loss 上没有形成像验证 loss 那样清晰的长期分层。

这说明 PoPE 的问题不一定是训练失败，也不是训练曲线形态上出现明显异常。更合理的解释是：PoPE 在训练集上仍能被正常优化，但这种优化没有转化成同等水平的验证泛化或 SAE 表征质量。因此训练曲线本身只能说明三种位置编码都可训练，不能单独说明哪种位置编码带来了更好的有效表示。

## 验证损失曲线

### 说明

验证损失用于衡量模型在未见数据上的泛化表现。验证损失越低，说明模型对语言建模任务的泛化越好。若训练损失下降但验证损失不降，可能说明过拟合或模型结构不利于泛化。

局限是验证损失仍然是任务性能指标，不直接说明 SAE 特征是否更可解释，也不直接说明位置和内容是否解耦。

### 结果展示

![Phase 2 Validation Loss](output/task1/figures/summary/phase2/phase2_valid_loss_all_models_seeds.png)

从图中和汇总表看，`alibi` 的验证损失最低，`rope` 次之，`pope` 最高。三个 seed 下趋势稳定。当前均值为：

- `alibi`：final valid loss 4.1590
- `rope`：final valid loss 4.1927
- `pope`：final valid loss 4.3294

从曲线形态看，验证 loss 在 10000 step 左右已经出现清晰分层：PoPE 始终位于上方，ALiBi 始终位于下方，RoPE 处于中间。也就是说，模型间差异不是训练结束时才突然出现，而是在中期训练过程中已经形成，并且之后保持到 30000 step。三条模型族内部的 seed 曲线也较紧，说明这个分层不是由单个 seed 异常造成的。

这说明 ALiBi 在当前训练预算下带来更好的语言建模泛化，但本文不把它解释为“模型胜负”，而是作为位置编码影响训练效果的结果变量。结合训练 loss 图看，训练曲线不明显分层而验证曲线稳定分层，提示位置编码更可能影响泛化和表示组织，而不是简单影响训练集拟合能力。

## Final Valid Loss

### 说明

Final valid loss 是训练结束时的验证损失。它用于比较不同位置编码在相同训练步数、相同数据规模下的最终泛化结果。数值越低表示语言建模表现越好。

局限是它不能揭示内部机制，也无法直接区分“位置编码更有效”和“优化路径更有利”。

### 结果展示

![Final Valid Loss](output/task1/figures/summary/phase2/phase2_final_valid_loss_mean.png)

结果显示 `alibi < rope < pope`。图形上三个柱子不是随机交错，而是形成稳定阶梯：ALiBi 最低、RoPE 居中、PoPE 最高。paired stats 中：

- `alibi - rope` 的 final loss 平均差为 -0.0337
- `pope - rope` 的 final loss 平均差为 +0.1366

这说明 ALiBi 的优势和 PoPE 的劣势在三个 seed 中都比较稳定。

## Final Perplexity

### 说明

Perplexity 是语言建模中常用的可解释指标，由 loss 指数变换得到。数值越低，说明模型对下一个 token 的预测越确定、越准确。

局限是 perplexity 和 valid loss 信息高度重叠，更多用于直观表达性能差距。

### 结果展示

![Final Perplexity](output/task1/figures/summary/phase2/phase2_final_perplexity_mean.png)

当前均值：

- `alibi`：64.01
- `rope`：66.20
- `pope`：75.90

图形形态与 final valid loss 完全一致，说明 perplexity 只是把 loss 差异映射到更直观的尺度上。PoPE 相比 RoPE 的 perplexity 明显升高，说明其位置编码方式在当前设置下没有转化为更好的语言建模泛化。该图不提供新的机制信息，但适合在论文中作为性能差异的直观展示。

## Best Valid Loss

### 说明

Best valid loss 是训练过程中达到的最低验证损失。若 best loss 和 final loss 接近，说明训练后期没有明显退化；若二者差距大，可能说明过拟合或后期不稳定。

局限是当前实验中 best loss 基本等于 final loss，因此它的额外信息有限。

### 结果展示

![Best Valid Loss](output/task1/figures/summary/phase2/phase2_best_valid_loss_mean.png)

本实验中 best valid loss 和 final valid loss 基本一致，图形排序也与 final valid loss 基本相同，说明模型在 30000 step 时仍处于较稳定的训练末端，没有明显后期退化。主要趋势仍为 `alibi` 最低，`pope` 最高。形态上没有出现某个模型曾经达到很好 best loss、但后期明显退化的情况，因此当前差异更像是稳定泛化能力差异，而不是训练末端偶然波动。

## Loss Spike Count

### 说明

Loss spike count 统计训练中损失突然上升的次数，可用于描述优化稳定性。数值越高，说明训练过程中瞬时波动越频繁；数值越低，说明训练曲线更平滑。

局限是 spike 阈值会影响统计结果，而且 spike 次数不一定和最终性能完全一致。

### 结果展示

![Loss Spike Count](output/task1/figures/summary/phase2/phase2_loss_spike_count_mean.png)

当前均值：

- `pope`：271.0
- `rope`：283.0
- `alibi`：296.7

有趣的是，`alibi` 的最终 valid loss 最低，但 loss spike count 反而最高。柱状图形态与 valid loss 排序相反：ALiBi 的短期训练波动更多，PoPE 的 spike count 更少，但 PoPE 的泛化最差。这说明 ALiBi 的性能优势不来自更少的短期波动，而可能来自位置偏置带来的有效归纳偏置。

## Gradient Norm Mean / Variance

### 说明

梯度范数均值和方差用于观察训练过程中的优化尺度和稳定性。梯度范数较低可能表示更新更温和；方差较低表示梯度波动更小。

局限是梯度指标不直接说明表示解耦，也容易受到学习率、batch、checkpoint 复用方式影响。

### 结果展示

暂未生成专门图。当前表格中 `alibi` 的 grad norm mean 明显低于 `rope` 和 `pope`：

- `alibi`：1.1923
- `rope`：1.3184
- `pope`：1.3182

这说明 ALiBi 的训练更新尺度更小，但仍获得更低验证损失。该现象可作为“位置编码改变优化动态”的辅助证据。

## Train Loss Variance

### 说明

Train loss variance 衡量训练损失波动程度。数值越低，训练过程越平滑；数值越高，说明训练过程波动更大。

局限是它和 loss spike count 类似，只描述优化动态，不直接解释机制。

### 结果展示

暂未生成专门图。当前均值：

- `pope`：0.5414
- `alibi`：0.5862
- `rope`：0.6120

PoPE 的训练损失方差较低，但最终性能最差。这说明更平滑的训练曲线不必然对应更好的泛化或更好的 SAE 表征。结合训练 loss 曲线看，较低方差还可能来自曲线变化幅度较小或训练轨迹较“钝”，而不是来自更优的优化质量。因此该指标只能作为训练动态诊断，不能作为模型效果或解耦质量的正向证据。

## 训练效率指标

### 说明

`seconds_per_step` 和 `tokens_per_second` 用于比较训练效率。数值越低或吞吐越高，说明训练更快。

局限是当前结果存在 checkpoint 复用和部分计时为 0 的情况，因此不适合用于正式比较。

### 结果展示

暂未。当前效率指标不建议纳入主要分析。

## 推荐补强指标：收敛速度（未运行）

### 说明

建议新增“达到某个验证损失阈值所需 step”或“前 N step 的 loss 下降斜率”。这比单纯 final loss 更能说明位置编码是否改变优化速度。

高收敛速度说明模型更快进入有效建模区域；低收敛速度说明训练更慢或归纳偏置较弱。

### 结果展示

本轮没有重跑 Phase2 训练阶段，`phase2_summary.json` 仍是旧训练结果，因此当前文档暂不更新收敛速度数值。该指标已经在代码中实现，下一次重跑 train 后会自动进入 phase2 summary 和图表。

# 第二阶段：Attention 结构分析

## Attention Distance

### 说明

Attention distance 衡量 query 关注 key 的平均距离。数值越高，说明模型更倾向长程注意力；数值越低，说明注意力更局部。

该指标可用于连接位置编码和信息流结构：如果某种位置编码强烈鼓励局部注意力，那么它可能改变位置/内容信息在网络中的混合方式。

局限是距离本身不说明注意力是否语义有效，也不能判断内容匹配能力。

### 结果展示

![Attention Distance](output/task1/figures/summary/phase3/phase3_seed42_distance_by_head_layers_x_models.png)

跨 seed、layer 的平均值：

- `rope`：120.65
- `pope`：53.97
- `alibi`：36.37

结果表明 RoPE 的 attention 最长程、最分散；ALiBi 平均上最局部；PoPE 位于二者之间。这与 ALiBi 的线性距离 bias 预期一致。

从图形形态看，ALiBi 不是简单地让所有 head 都变局部，而是形成非常稳定的 head-wise 尺度模板：head 0-5 多数较局部，head 6-7 出现明显长程峰值，head 8 之后又回到较局部状态。这个形状在 L1、L6、L10 中反复出现。RoPE 和 PoPE 则没有这种跨层重复模板，不同层中高 distance head 的位置会明显变化。这个形态说明 ALiBi 的 slope 设计把不同 head 预先分配到不同注意力距离尺度上，而 RoPE/PoPE 的 head 距离模式更依赖训练后形成的层内分化。

## Attention Entropy

### 说明

Attention entropy 衡量注意力分布的分散程度。entropy 越高，注意力越分散；entropy 越低，注意力越集中。

该指标用于判断位置编码是否导致模型形成更尖锐或更局部的注意力模式。

局限是低 entropy 不一定更好，可能表示有效聚焦，也可能表示过强的结构偏置。

### 结果展示

![Attention Entropy](output/task1/figures/summary/phase3/phase3_seed42_entropy_by_head_layers_x_models.png)

平均 entropy：

- `rope`：4.16
- `pope`：3.43
- `alibi`：2.63

ALiBi 的 entropy 明显最低，说明其注意力分布更集中。这说明 ALiBi 的训练优势可能伴随强局部归纳偏置，而不是更自由的全局信息混合。

更重要的是图形形态：ALiBi 在不同 layer 中的 head-wise entropy 柱形几乎是同一个模板的缩放版本。具体表现为 head 0-7 的 entropy 大体递增，head 8 突然降低，随后 head 8-11 再次递增；这个相对排序在 L1、L6、L10 中都保持得很清楚。相比之下，RoPE 和 PoPE 的柱形形状在不同层之间会重排，高 entropy head 的位置并不稳定。这个发现说明 ALiBi 不只是降低 entropy，而是把 head 的注意力分散程度组织成跨层重复的结构模板。

## Attention Entropy Head Pattern Consistency

### 说明

该指标衡量不同 layer 之间，head-wise attention entropy 向量是否保持相似。具体做法是：对每一层取一个长度为 head 数量的 entropy 向量，然后计算不同 layer 两两之间的相关性。

相关性越高，说明不同层中各个 head 的相对注意力分散程度越一致，也就是 head 的尺度分工更像一个跨层重复的模板。相关性越低，说明不同层的 head 功能分化更强，entropy pattern 更随层变化。

这个指标特别适合检验你刚才观察到的现象：ALiBi 每层 entropy 绝对值会变，但各 head 的相对比例几乎一致。局限是它只衡量 head entropy 的相对排序/形状一致性，不说明 attention 关注的具体 token 内容是否一致，也不能单独判断这种一致性是好还是坏。

### 结果展示

当前根据 `phase3_head_metrics.csv` 中所有 seed 和所有 layer 的 head entropy 计算，结果非常明显：

| 模型 | 全层两两相关均值 | 相邻层相关均值 | 解释 |
|---|---:|---:|---|
| `alibi` | 0.949 | 0.955 | 不同层的 head entropy 相对模式高度一致 |
| `pope` | 0.015 | 0.046 | 不同层之间几乎不保持固定的 head entropy 模板 |
| `rope` | -0.005 | -0.012 | 不同层之间几乎没有稳定的 head entropy 相对模式 |

这说明 ALiBi 的 attention entropy 不只是整体更低，而且不同层之间的 head-wise entropy pattern 高度一致。换句话说，ALiBi 的 slope bias 很可能在各层重复施加了一套稳定的 head 尺度分工：有些 head 始终更局部，有些 head 始终相对更分散。相比之下，RoPE 和 PoPE 的不同层之间没有保持这种固定比例关系，说明它们的 attention head 模式更依赖层内学习和层间分化。

这个现象可以作为本文机制分析中的一个强证据：ALiBi 将位置结构强烈、稳定地约束在 attention score 层面，而 RoPE/PoPE 的位置结构更可能与层内表示学习共同演化。因此，ALiBi 的 valid loss 更好并不等价于 SAE 表征更可解释；它可能是通过稳定的 attention 尺度模板改善了语言建模，而不是让 residual 表征中的位置/内容解耦变得最好。

## Attention Heatmap

### 说明

Attention heatmap 展示具体 head 在不同 layer 中的 query-key 注意力模式。对角线附近越亮，说明模型更关注附近 token；出现远距离条纹或块状结构，说明存在特殊长程或结构化注意力。

局限是 heatmap 是视觉证据，通常需要配合 distance、entropy 等定量指标解释。

### 结果展示

![Attention Heatmap Head 0](output/task1/figures/summary/phase3/phase3_seed42_attention_heatmap_head0_layers_x_models.png)

从 seed42 的 head0 图看，ALiBi 更明显地集中在局部区域，RoPE 的注意力更分散。形态上，ALiBi heatmap 更接近沿对角线集中的窄带结构，说明 query 主要关注附近 token；RoPE 的可见区域更扩散，说明注意力质量更容易分布到较远位置；PoPE 大体位于二者之间。

该图与 attention distance 和 entropy 指标相互支持：ALiBi 的低 entropy、低平均距离并不是单纯的数值现象，而是在 heatmap 上表现为清晰的局部带状结构。需要注意的是，当前只展示代表 head，因此它适合作为机制可视化例子，不应单独替代全 head 的统计结果。

## Spectral Concentration

### 说明

Spectral concentration 衡量 attention logits 或相关矩阵的奇异值能量集中程度。数值越高，说明结构更低秩、更集中；数值越低，说明结构更分散、更复杂。

该指标可用于判断某种位置编码是否诱导更强的规则结构。

局限是高谱集中不一定代表更好的模型，只说明结构更集中。

### 结果展示

![Singular Values Head 0](output/task1/figures/summary/phase3/phase3_seed42_singular_values_head0_layers_x_models.png)

平均 top8 spectral concentration：

- `alibi`：0.942
- `pope`：0.676
- `rope`：0.609

ALiBi 的谱集中度显著更高，说明它产生了更低秩、更规则的 attention 结构。RoPE 的结构更分散。

需要特别说明的是，图中的 singular values 是基于 raw attention logits 计算的，而不是基于 softmax 之后的 attention probability。ALiBi 会直接向 attention logits 中加入按距离线性变化的 bias，因此其 raw-logit 奇异值的绝对尺度会被显著放大。图中 ALiBi 的纵坐标远高于 RoPE/PoPE，主要反映的是 ALiBi 显式线性位置 bias 在 logits 中引入了强尺度、低秩的结构性成分，而不能直接解释为 ALiBi 的 attention “强很多”或模型性能更好。

因此，该图更适合作为 ALiBi 显式位置先验强度的可视化。跨模型比较时，应优先参考归一化后的 spectral concentration，例如 top8 energy ratio，而不是直接比较 raw singular value 的绝对大小。

## Toeplitz Deviation

### 说明

Toeplitz deviation 用于衡量 attention logits 是否接近只依赖相对距离的 Toeplitz 结构。数值越低，通常表示越接近相对位置平移结构；数值越高，表示偏离该结构更明显。

该指标非常贴合本文主题，因为它直接描述位置编码带来的相对位置结构。

局限是它主要刻画位置结构规则性，不能说明语义内容是否被正确建模。

### 结果展示

![Toeplitz Deviation](output/task1/figures/summary/phase3/phase3_seed42_toeplitz_deviation_by_head_layers_x_models.png)

平均 Toeplitz deviation：

- `pope`：1.61
- `rope`：1.73
- `alibi`：2.45

PoPE 在该指标上最低，说明它更接近规则的相对位置结构。但从图形形态看，PoPE 也不是所有 head 都一致低 deviation：中后层仍有个别 head 出现明显突起，说明其相对位置规则性并非均匀分布在所有 head 上，而是存在 head-level 差异。RoPE 在 L1 中有非常突出的异常高 deviation head，后续层则更平缓；ALiBi 的 deviation 随层加深整体变高，并且中间 head 更容易出现峰值。

这个结果需要和 spectral/entropy 图分开解释：ALiBi 有强线性位置 bias，但在当前 Toeplitz deviation 定义下并不最低；PoPE 更接近该指标所刻画的相对位置平移结构，但这种规则性没有带来更低的 valid loss，也没有带来更好的 SAE explained variance。这是一个值得强调的结果：更强的位置结构规则性不必然等于更好的训练效果或更好的稀疏表征。

## Head Taxonomy Counts

### 说明

Head taxonomy 将 attention head 粗略分类为 local_syntactic、long_range_dependency、position_sensitive、content_matching、unclassified 等类别。类别数量可用于比较不同位置编码下 attention head 的功能分布。

局限是当前分类依赖启发式阈值，不能当作严格语义解释。

### 结果展示

暂未生成专门图。当前平均 counts：

- `alibi`：local 35.7，long-range 36.7，position-sensitive 37.0，unclassified 50.3
- `pope`：local 30.7，long-range 34.7，position-sensitive 37.0，unclassified 63.7
- `rope`：local 30.0，long-range 35.7，position-sensitive 37.0，unclassified 69.0

ALiBi 的 unclassified head 更少，local head 更多，说明它的 head 行为更容易被当前规则捕捉为结构化模式。

## 推荐补强指标：Local Attention Mass

### 说明

建议新增窗口内注意力质量，例如 local mass@4、@16、@64。数值越高，说明模型越依赖局部上下文；数值越低，说明注意力更多分布到远处。

该指标比 attention distance 更直观，特别适合解释 ALiBi。

### 结果展示

![Local Attention Mass@16](output/task1/figures/summary/phase3/phase3_seed42_local_mass_16_by_head_layers_x_models.png)

当前跨 seed、layer 的平均 local mass：

- `alibi`：local@4 = 0.462，local@16 = 0.685，local@64 = 0.856
- `pope`：local@4 = 0.431，local@16 = 0.607，local@64 = 0.792
- `rope`：local@4 = 0.229，local@16 = 0.384，local@64 = 0.593

图形形态上，ALiBi 的 local mass 呈现非常稳定的跨层模板：head 0-2 和 head 8-10 的局部注意力质量很高，head 5-7 是明显低谷；这个形状在 L1、L6、L10 中基本重复。RoPE 和 PoPE 的 head 局部性分布则随层变化更明显。该结果进一步支持：ALiBi 不是简单地让所有 head 都局部，而是把不同 head 固定到不同注意力距离尺度上。

## 推荐补强指标：Far Attention Mass

### 说明

Far attention mass 衡量远距离 token 获得的注意力比例。数值越高，说明模型更依赖长程信息；数值越低，说明模型更局部。

它可与 local mass 配对，用于解释位置编码如何改变长程/短程信息流。

### 结果展示

![Far Attention Mass](output/task1/figures/summary/phase3/phase3_seed42_far_mass_by_head_layers_x_models.png)

当前跨 seed、layer 的平均 far attention mass：

- `rope`：0.161
- `pope`：0.056
- `alibi`：0.033

从图形形态看，ALiBi 的远程注意力几乎集中在 head 6-7，其他 head 接近 0，而且这个形状在不同层中重复出现。RoPE 的远程注意力更分散，多个 head 都会承担长程注意力；PoPE 处于二者之间。这个结果和 attention distance、local mass 互相印证：ALiBi 的位置偏置形成了稳定的 head-wise 距离分工模板。

## 推荐补强指标：Attention Sink Mass

### 说明

Attention sink mass 衡量模型是否过度关注 BOS 或早期 token。数值高表示存在明显 attention sink；数值低表示注意力没有集中到固定开头位置。

该指标可帮助区分“有效局部注意力”和“退化的固定位置注意力”。

### 结果展示

![Attention Sink Mass](output/task1/figures/summary/phase3/phase3_seed42_attention_sink_mass_by_head_layers_x_models.png)

当前跨 seed、layer 的平均 attention sink mass：

- `rope`：0.0058
- `alibi`：0.0039
- `pope`：0.0035

attention sink mass 的绝对值都很低，说明三个模型都没有明显退化成固定关注开头 token 的模式。图形上，ALiBi 仍然呈现较稳定的 head-wise 模板，中间 head 的 sink mass 相对更高；其跨层 pattern consistency 约为 0.831，高于 RoPE/PoPE 的接近 0。这个指标不是主结论，但可以作为 ALiBi head 模板一致性的补充证据。

# 第三阶段：SAE 重构与特征健康

## SAE Validation MSE

### 说明

SAE validation MSE 衡量 SAE 对验证激活的重构误差。数值越低，说明 SAE 更容易重构该模型的 residual 表示；数值越高，说明表示更难被当前 SAE 配置压缩重构。

局限是它受 SAE 容量、top-k、训练步数影响，不能单独说明表示是否更可解释。

### 结果展示

![SAE Validation MSE](output/task1/figures/summary/phase4a/phase4a_validation_mse_mean.png)

结果显示 layer 2 上 `rope` 和 `alibi` 都较好；layer 6/10 上 `rope` 的 MSE 更低。PoPE 整体重构误差偏高。

从层间形态看，SAE 重构误差不是平行移动关系，而是随层深发生分化：ALiBi 早层表现较好，但到中后层优势明显减弱；RoPE 的曲线更平稳，深层仍保持较好重构；PoPE 则整体偏高。这提示不同位置编码对 residual 表示复杂度的影响具有层依赖性，不能只用跨层平均值概括。

## SAE Explained Variance

### 说明

Explained variance 表示 SAE 重构解释原始激活方差的比例。数值越高，说明 SAE 对表示结构捕捉得越好；数值越低，说明当前稀疏字典难以重构该表示。

该指标是 SAE 表征可重构性的核心指标。

局限是高 explained variance 不等于 feature 更语义化，也不等于位置/内容更解耦。

### 结果展示

![SAE Explained Variance](output/task1/figures/summary/phase4a/phase4a_explained_variance_mean.png)

跨 layer 平均 explained variance：

- `rope`：0.741
- `alibi`：0.723
- `pope`：0.709

RoPE 的 residual 表示最容易被当前 TopK SAE 重构。ALiBi 虽然训练效果最好，但 SAE explained variance 不是最高。这说明语言建模优势和 SAE 可重构性不是同一个维度。

图形形态上，三种模型的 explained variance 都随 layer 加深而下降，说明深层 residual 表示更难被当前 SAE 设置重构。ALiBi 的下降最陡：layer 2 与 RoPE 几乎重合，但到 layer 6 已经明显低于 RoPE，并在 layer 10 接近 PoPE。这与前面 attention 图形成互补：ALiBi 在 attention score 层面有稳定位置模板，但这种结构并没有转化为深层 residual 表示中更容易被 SAE 捕捉的稀疏结构。

## SAE Train / Valid MSE Curves

### 说明

SAE train/valid MSE 曲线展示 SAE 训练过程是否稳定，以及是否存在过拟合。train MSE 下降而 valid MSE 不降，可能说明 SAE 过拟合训练激活。

局限是曲线主要用于诊断 SAE 训练质量，而不是最终机制解释。

### 结果展示

![SAE Train MSE](output/task1/figures/summary/phase4a/phase4a_train_mse_curves.png)

![SAE Valid MSE](output/task1/figures/summary/phase4a/phase4a_valid_mse_curves.png)

曲线显示 SAE 训练整体正常，没有明显失效。形态上，train/valid MSE 都呈现早期快速下降、后期趋于平台的模式，说明 SAE 在前期迅速捕捉主要可重构结构，后期收益变小。部分 layer 后期 valid MSE 下降趋缓或略有回升，说明当前 2000 step 基本能训练出可用 SAE，但仍可能存在容量或稀疏度设置的影响。

因此这些曲线更像“SAE 训练已进入平台期”的诊断，而不是“SAE 完全收敛到最优”的证据。后续若要加强说服力，可以比较更长 SAE 训练或不同 top-k/dictionary size 是否改变各模型排序。

## Dead Feature Rate

### 说明

Dead feature rate 表示几乎不激活的 SAE 特征比例。数值越高，说明字典中无效特征越多；数值越低，说明字典利用率更高。

局限是 dead feature 很低不代表 feature 语义清晰，只说明特征被使用。

### 结果展示

![Dead Feature Rate](output/task1/figures/summary/phase4a/phase4a_dead_feature_rate_mean.png)

当前 dead feature rate 接近 0，图形上不同模型和不同层都贴近底部，没有某个模型或层出现明显高死特征率。说明 SAE 没有出现大量无效特征，SAE 训练质量基本可信。

这个图的主要价值是排除性证据：后续 SAE explained variance 或 feature ratio 的差异，不太可能是因为某个模型的 SAE 大量特征死亡导致的。

## Feature Frequency Entropy

### 说明

Feature frequency entropy 衡量 SAE feature 使用频率是否均匀。数值越高，说明 feature 使用更分散；数值越低，说明少数 feature 被过度使用。

该指标可用于观察不同位置编码是否导致 feature 使用集中化。

局限是高 entropy 不一定代表更可解释，只说明使用分布更均匀。

### 结果展示

![Feature Frequency Entropy](output/task1/figures/summary/phase4a/phase4a_feature_frequency_entropy_normalized_mean.png)

ALiBi 在 layer 2 的 feature frequency entropy 较低，同时 top feature activation frequency 较高，说明早层可能存在更集中的特征使用模式。这与 ALiBi attention 更集中、更局部的现象可能相关。

从图形形态看，feature frequency entropy 的模型差异主要集中在浅层；到了更深层，不同模型之间的差异变小。这说明 ALiBi 的“集中使用少数特征”的现象更像是早层效应，而不是贯穿所有层的全局现象。这个层依赖形态也与 explained variance 中 ALiBi 早层较强、深层优势消失的趋势一致。

## Top Feature Activation Frequency

### 说明

Top feature activation frequency 表示最常激活的 SAE feature 的激活频率。数值越高，说明少数 feature 更频繁地承担表示；数值越低，说明激活分布更均匀。

局限是该指标目前没有专门 summary 图，需要配合 feature entropy 解读。

### 结果展示

![Top Feature Activation Frequency](output/task1/figures/summary/phase4a/phase4a_top_feature_activation_frequency_mean.png)

当前跨 layer 平均值：

- `rope`：0.2429
- `alibi`：0.2428
- `pope`：0.1641

图形形态上，ALiBi 在浅层仍然表现出较高的 top feature activation frequency，说明早层表示更集中地依赖少数高频 SAE 特征；但跨层平均后，RoPE 与 ALiBi 几乎相同。PoPE 的 top feature activation frequency 明显更低，说明其 SAE 激活分布更分散。这个指标需要和 explained variance 一起看：PoPE 分散不等于更好，因为它的 SAE explained variance 仍然最低。

## Feature Reuse Rate

### 说明

Feature reuse rate 表示有多少 SAE feature 在足够多 token 上被复用。数值越高，说明特征复用更广；数值越低，说明更多特征只在少数样本上激活。

局限是复用率高低不直接代表语义质量。

### 结果展示

![Feature Reuse Rate](output/task1/figures/summary/phase4a/phase4a_feature_reuse_rate_mean.png)

当前跨 layer 平均值：

- `alibi`：0.9867
- `pope`：0.9866
- `rope`：0.9859

三个模型的 feature reuse rate 都接近 0.986，图形上也没有出现某个模型或某层显著塌缩。这说明当前 SAE 字典整体被充分使用，后续 explained variance、feature entropy、feature ratio 的差异不太可能来自大量 feature 没有被复用。

## 推荐补强指标：SAE Dictionary Size Sweep（未运行）

### 说明

建议补充 dictionary size = 2048 / 4096 / 8192。若某模型在更大字典下 explained variance 大幅提升，说明当前容量可能限制了它的 SAE 重构。

该指标用于排除“某模型 SAE 效果差只是因为容量不足”的解释。

### 结果展示

当前已经生成 `phase4a_sae_sweep_summary.csv`，但本轮配置只有 dictionary size = 4096，因此暂时不能比较不同字典容量。该指标的代码和表格出口已经就位，后续只要在配置中加入多个 `dictionary_sizes`，就会自动汇总到 sweep summary。

## 推荐补强指标：SAE Top-k Sweep（未运行）

### 说明

建议补充 top-k = 32 / 64 / 128。top-k 越小，稀疏性越强，重构更难；top-k 越大，重构更容易，但特征可能更不稀疏。

该指标用于判断不同位置编码下表示对稀疏约束的敏感性。

### 结果展示

当前已经生成 `phase4a_sae_sweep_summary.csv`，但本轮配置只有 top-k = 64，因此暂时不能比较不同稀疏度。该指标的代码和表格出口已经就位，后续只要在配置中加入多个 `topk_values`，就会自动汇总到 sweep summary。

## 推荐补强指标：Feature Stability Across Seeds（未实现）

### 说明

建议比较不同 seed 下 SAE feature 的相似性或功能分布稳定性。若某类位置编码下 feature 分布跨 seed 更稳定，说明其表示结构更可复现。

局限是 feature matching 本身需要额外方法，例如 decoder cosine similarity、activation correlation 或 top-token overlap。

### 结果展示

暂未。本轮还没有做跨 seed 的 SAE feature stability 分析。

# 第四阶段：位置/内容解耦分析

## Raw Position R2

### 说明

Raw position R2 衡量从原始 residual 表示中线性回归连续位置的能力。数值越高，说明连续位置信息越容易被读出；数值低或为负，说明该 probe 比均值预测还差。

局限是当前 R2 经常为负，因此不宜作为主结论指标。它可能受目标尺度、probe 训练、层内方差等因素影响。

### 结果展示

![Raw Position R2](output/task1/figures/summary/phase5/phase5_raw_position_r2_mean.png)

平均 raw position R2：

- `pope`：-0.069
- `rope`：-0.131
- `alibi`：-0.203

整体都不强，因此不能据此断言哪个模型“位置表示最好”。从图形形态看，三个模型的 raw position R2 都处在负值区域，说明连续位置线性回归整体不可靠；模型间差异更多是“谁没那么差”，而不是谁真正强。该指标需要补 baseline 和稳定性检查，不适合作为主结论。

## Raw Position Bin Accuracy

### 说明

Position bin accuracy 将位置离散成若干 bin 后做分类。数值越高，说明模型表示中更容易读出粗粒度位置信息。

相比 R2，该指标更稳健，适合作为位置可读性的主指标之一。

局限是它只表示粗粒度位置，不表示连续位置精度。

### 结果展示

![Raw Position Bin Accuracy](output/task1/figures/summary/phase5/phase5_raw_position_bin_accuracy_mean.png)

平均 raw position bin accuracy：

- `alibi`：0.121
- `rope`：0.115
- `pope`：0.104

粗粒度位置可读性整体不高，但 ALiBi 略高。从图形形态看，三种模型都随 layer 加深而下降，说明粗粒度位置信息在浅层更容易线性读出，深层逐渐减弱。ALiBi 在 layer 2 明显高于其他模型，但到 layer 6/10 差异缩小，说明 ALiBi 的位置偏置更明显地影响浅层 residual 表示，而不是在深层持续扩大位置可读性。

新增 majority baseline 后，raw position bin 的 baseline margin 平均值为：

- `alibi`：0.0588
- `rope`：0.0520
- `pope`：0.0418

这说明粗粒度位置 probe 虽然绝对 accuracy 不高，但确实超过简单多数类 baseline；因此它不是完全无效信号。ALiBi 的 margin 略高，说明其位置偏置在 raw residual 中留下了更可读的粗粒度位置信息，但差异仍然较小，不能单独作为强结论。

## Raw Token Category Accuracy

### 说明

Token category accuracy 衡量从 residual 表示中预测 token 内容类别的能力。数值越高，说明内容信息更容易被读出。

该指标用于与 position probe 对照，判断内容信息和位置信息在表示中的可读性差异。

局限是 token category 是粗类别，不等同于完整 token identity。

### 结果展示

![Raw Token Category Accuracy](output/task1/figures/summary/phase5/phase5_raw_token_category_accuracy_mean.png)

平均 raw token category accuracy：

- `rope`：0.975
- `alibi`：0.971
- `pope`：0.971

内容类别信息非常容易读出，远强于位置信息。从图形形态看，三种模型的 token category accuracy 都随 layer 加深而下降，但整体仍保持在很高水平。ALiBi 在 layer 2 最高，但下降最快；RoPE 在中深层保持最高，说明 RoPE 的深层 residual 表示对内容类别信息的线性保留更稳定。

新增 majority baseline 后，raw token category 的 baseline margin 平均值为：

- `rope`：0.1545
- `alibi`：0.1505
- `pope`：0.1503

这支持一个核心观察：当前 residual 表示中内容信息比绝对/连续位置信息更稳定、更可线性读取，而且明显超过多数类 baseline。同时，位置编码对内容信息的影响也具有层依赖性：ALiBi 早层内容可读性强，但深层不保持优势；RoPE 在中深层更稳定。

## SAE Position Bin Accuracy

### 说明

SAE position bin accuracy 衡量从 SAE feature 激活中预测位置 bin 的能力。数值越高，说明 SAE 稀疏特征保留了更多粗粒度位置信息。

局限是如果 SAE 特征主要重构内容或局部模式，位置 bin accuracy 可能较低。

### 结果展示

![SAE Position Bin Accuracy](output/task1/figures/summary/phase5/phase5_sae_position_bin_accuracy_mean.png)

平均 SAE position bin accuracy：

- `alibi`：0.095
- `rope`：0.093
- `pope`：0.089

SAE 特征中的位置可读性仍较弱，说明当前 TopK SAE 并没有把位置因素明显拆成强 position features。从图形形态看，SAE position bin accuracy 也随 layer 加深下降，且三种模型曲线距离较近。这说明 SAE 特征中的粗粒度位置信号并没有形成显著模型分离，至少在当前 probe 和 feature 分类设置下，位置解耦信号较弱。

新增 majority baseline 后，SAE position bin 的 baseline margin 平均值为：

- `alibi`：0.0329
- `rope`：0.0306
- `pope`：0.0262

因此 SAE 特征中的位置读出并非完全等同于多数类 baseline，但 margin 明显小于 raw residual。这说明 SAE 保留了一些粗粒度位置信息，但当前位置因素没有被强烈拆分成可线性读出的 feature 方向。

## SAE Token Category Accuracy

### 说明

SAE token category accuracy 衡量从 SAE feature 中预测内容类别的能力。数值越高，说明 SAE 稀疏特征保留了更多内容信息。

该指标可用于判断 SAE 是否主要保留内容结构。

### 结果展示

![SAE Token Category Accuracy](output/task1/figures/summary/phase5/phase5_sae_token_category_accuracy_mean.png)

平均 SAE token category accuracy：

- `rope`：0.962
- `alibi`：0.961
- `pope`：0.950

SAE 特征中内容信息仍然高度可读。相比位置指标，内容信息在 SAE 表示中保留得更稳定。从图形形态看，SAE token accuracy 同样随 layer 加深下降，但下降幅度小于位置指标，并且整体数值远高于 position bin accuracy。这说明 SAE 更稳定地保留内容类别信息，而不是位置结构。

新增 majority baseline 后，SAE token category 的 baseline margin 平均值为：

- `rope`：0.1417
- `alibi`：0.1409
- `pope`：0.1298

RoPE 与 ALiBi 接近，PoPE 略低。这与 SAE explained variance 中 RoPE 最好、PoPE 最弱的趋势一致，说明 PoPE 的 SAE 特征对内容类别信息的保留也相对弱一些。

## Mixed Feature Ratio

### 说明

Mixed feature ratio 表示同时与内容和位置相关的 SAE 特征比例。数值越高，说明内容和位置更容易混合在同一 feature 中；数值越低，说明混合特征更少。

该指标直接服务于“位置/内容解耦”的主题。

局限是 feature 类型划分依赖阈值，阈值选择会影响比例。

### 结果展示

![Mixed Feature Ratio](output/task1/figures/summary/phase5/phase5_mixed_feature_ratio_mean.png)

平均 mixed ratio：

- `alibi`：0.0224
- `rope`：0.0205
- `pope`：0.0186

混合特征比例整体很低，说明大部分 SAE feature 没有同时强烈携带位置和内容信号。ALiBi 略高，但差异较小。

从图形形态看，三种模型的 mixed ratio 都随 layer 加深下降，说明越深层越少有 feature 被当前规则标记为同时携带内容和位置。ALiBi 在 layer 2 的 mixed ratio 最高，之后快速下降但仍略高于其他模型；PoPE 全程最低。这提示 ALiBi 的强位置偏置可能在浅层带来更多内容/位置混合特征，但这个效应的绝对规模很小，不宜过度解释。

## Content-only Feature Ratio

### 说明

Content-only ratio 表示主要与内容相关、但不明显与位置相关的 SAE feature 比例。数值越高，说明内容因素更容易被单独拆出。

局限是该指标依赖内容分数定义，目前内容类别较粗。

### 结果展示

![Content-only Feature Ratio](output/task1/figures/summary/phase5/phase5_content_only_ratio_mean.png)

平均 content-only ratio：

- `pope`：0.0812
- `rope`：0.0794
- `alibi`：0.0776

三者差异较小。图形形态上，content-only ratio 与 position/mixed 指标类似，也呈现随层变化的趋势，说明 feature 类型比例不是固定属性，而是随层深改变。PoPE 的 content-only ratio 略高，但这没有带来更好的语言建模表现，因此不能把 content-only 比例直接等同于模型效果。

## Position-only Feature Ratio

### 说明

Position-only ratio 表示主要与位置相关、但不明显与内容相关的 SAE feature 比例。数值越高，说明位置因素更容易被 SAE 单独拆出。

该指标是判断位置/内容解耦的重要指标之一。

局限是当前 position probe 本身较弱，因此 position-only ratio 的解释需要谨慎。

### 结果展示

![Position-only Feature Ratio](output/task1/figures/summary/phase5/phase5_position_only_ratio_mean.png)

平均 position-only ratio：

- `pope`：0.0812
- `rope`：0.0794
- `alibi`：0.0776

position-only 和 content-only 的比例几乎相等，这是由当前分类规则决定的可能性较高。图形上 position-only ratio 与 content-only ratio 高度相似，说明当前 feature 类型划分可能受到阈值或互补规则影响，而不一定反映真实的语义/位置功能对称性。正式写作时需要说明该指标依赖阈值定义。

## Content-position Score Correlation

### 说明

Content-position score correlation 衡量 feature 的内容分数和位置分数是否相关。数值越高，说明内容和位置信号更容易一起出现；数值越低或接近 0，说明二者更独立。

该指标可以作为解耦程度的连续度量。

局限是相关性低不一定代表完全解耦，也可能是两个分数都较弱。

### 结果展示

暂未生成专门图。当前平均值：

- `alibi`：0.105
- `rope`：0.097
- `pope`：0.083

三者都为弱正相关，说明内容和位置分数并非强耦合。ALiBi 略高，可能与其更强的位置偏置和局部模式有关。

## Low-selectivity Feature Ratio

### 说明

Low-selectivity feature ratio 表示既不强烈对应内容、也不强烈对应位置的 SAE 特征比例。数值越高，说明当前指标无法解释的特征越多；数值越低，说明更多 feature 可被内容/位置维度解释。

局限是低选择性并不代表无意义，可能对应语法、频率、局部模式或更复杂语义。

### 结果展示

暂未生成专门 summary 图，但 `phase5_disentanglement_runs.csv` 中已经包含该指标。当前跨 layer 平均 low-selectivity ratio：

- `alibi`：0.8207
- `rope`：0.8184
- `pope`：0.8162

大约 82% 的 SAE feature 仍被当前规则归为 low-selectivity，说明仅用“内容类别/频率”和“位置相关性/位置 bin”还只能解释 SAE feature 的一小部分。这个结果不应被理解为这些 feature 无意义，而更可能说明当前定义的 content/position 指标仍然偏粗，需要结合 top activating contexts、token identity、语法模式或局部上下文模式继续解释。

## 推荐补强指标：Probe Baseline

### 说明

建议加入 random label baseline、majority baseline、shuffled position baseline。若真实 probe 仅略高于 baseline，则不能说明模型真的编码了对应信息。

这是当前最需要补强的部分，尤其因为 position R2 出现负值。

### 结果展示

![Raw Position Bin Baseline Margin](output/task1/figures/summary/phase5/phase5_raw_position_bin_baseline_margin_mean.png)

![SAE Position Bin Baseline Margin](output/task1/figures/summary/phase5/phase5_sae_position_bin_baseline_margin_mean.png)

本轮已经加入 probe baseline：连续位置回归使用均值 baseline，分类 probe 使用训练集多数类 baseline。关键结论是：位置类指标虽然绝对数值不高，但多数分类指标确实高于 majority baseline。

平均 baseline margin：

| 指标 | `alibi` | `pope` | `rope` |
|---|---:|---:|---:|
| raw position bin | 0.0588 | 0.0418 | 0.0520 |
| SAE position bin | 0.0329 | 0.0262 | 0.0306 |
| raw segment position | 0.1208 | 0.1073 | 0.1238 |
| SAE segment position | 0.0612 | 0.0480 | 0.0520 |

这说明 position probe 不是完全无效，但 SAE 表示中的位置 margin 明显低于 raw residual，说明 SAE 没有把位置因素强烈保留下来或拆分出来。ALiBi 在 bin position 上略高，RoPE 在 raw segment position 上略高；这些差异都不大，适合做辅助证据，不适合作为单独主结论。

## 推荐补强指标：Segment Position Accuracy

### 说明

将位置分为 early/middle/late 三段，训练分类 probe。该指标比连续 R2 更稳健，也比 16-bin 分类更容易解释。

数值越高，说明模型更容易编码粗粒度位置段。

### 结果展示

![Raw Segment Position Accuracy](output/task1/figures/summary/phase5/phase5_raw_segment_position_accuracy_mean.png)

![Raw Segment Position Baseline Margin](output/task1/figures/summary/phase5/phase5_raw_segment_position_baseline_margin_mean.png)

![SAE Segment Position Accuracy](output/task1/figures/summary/phase5/phase5_sae_segment_position_accuracy_mean.png)

当前 segment position accuracy 平均值：

- raw：`rope` 0.4577，`alibi` 0.4548，`pope` 0.4413
- SAE：`alibi` 0.3952，`rope` 0.3860，`pope` 0.3820

baseline margin 平均值：

- raw：`rope` 0.1238，`alibi` 0.1208，`pope` 0.1073
- SAE：`alibi` 0.0612，`rope` 0.0520，`pope` 0.0480

图形形态上，segment position 的 baseline margin 随 layer 加深明显下降，说明粗粒度位置段信息主要集中在浅层。Raw residual 中 RoPE 的中层位置段读出更强；SAE 特征中 ALiBi 略高，说明 ALiBi 的位置偏置可能让 SAE 保留了稍多粗粒度位置段信息，但整体 margin 仍然有限。

## 推荐补强指标：Token Identity / Frequency Probe

### 说明

建议加入 token identity 或 token frequency bin probe，用于区分 SAE feature 是否主要编码 token 内容、频率或词形类别。

这有助于解释 low-selectivity feature 可能并非无意义，而是当前内容类别太粗。

### 结果展示

![Raw Token Frequency Bin Accuracy](output/task1/figures/summary/phase5/phase5_raw_token_frequency_bin_accuracy_mean.png)

![SAE Token Frequency Bin Accuracy](output/task1/figures/summary/phase5/phase5_sae_token_frequency_bin_accuracy_mean.png)

![SAE Top Token Identity Baseline Margin](output/task1/figures/summary/phase5/phase5_sae_top_token_identity_baseline_margin_mean.png)

当前 token frequency bin accuracy 平均值：

- raw：`alibi` 0.8633，`rope` 0.8632，`pope` 0.8592
- SAE：`rope` 0.9237，`alibi` 0.9192，`pope` 0.9111

当前 top-token identity accuracy 平均值：

- raw：`rope` 0.9575，`pope` 0.9542，`alibi` 0.9529
- SAE：`rope` 0.9190，`alibi` 0.9024，`pope` 0.8771

这些内容/频率相关 probe 的 baseline margin 明显高于位置 probe。例如 SAE top-token identity baseline margin：`rope` 0.2523，`alibi` 0.2356，`pope` 0.2104。图形上，RoPE 在中深层的 token identity 保留最强，PoPE 最弱，ALiBi 早层较强但下降较快。这进一步支持：SAE 特征更稳定地保留 token 内容/频率信息，而不是位置结构；PoPE 的 SAE 表示在内容身份保留上也偏弱。

## 推荐补强指标：Feature Overlap Across Seeds（未实现）

### 说明

建议比较不同 seed 下 content-only、position-only、mixed feature 的重合程度。若某类 feature 跨 seed 稳定，说明它更可能是位置编码导致的可靠结构。

### 结果展示

暂未。本轮还没有做跨 seed 的 feature identity/overlap 对齐分析。

# 第五阶段：解释性案例分析

## LLM Feature Interpretation（未运行）

### 说明

LLM feature interpretation 通过抽取高激活上下文，让语言模型解释 SAE feature 可能对应的语义或模式。该指标可把统计性的 content/position 分数转化为可读案例。

高质量解释有助于判断 feature 是否真的对应位置、内容、语法或局部结构。

局限是解释依赖外部模型，可能有幻觉，需要配合定量指标。

### 结果展示

暂未。当前 `run_interpret=false`，解释阶段没有运行。

## Feature Case Study（未实现）

### 说明

建议为每个模型和每层选取若干代表性 feature：高 content score、高 position score、高 mixed score、low-selectivity 但高频 feature。展示其 top activating contexts。

该分析可直接服务于论文叙述：不同位置编码是否改变 SAE feature 的语义类型。

### 结果展示

暂未。本轮仍未生成 top activating contexts 或人工/LLM feature case study。当前已有的 token identity、frequency 和 low-selectivity 结果可以作为后续挑选案例 feature 的依据。

# 第六阶段：建议补强实验

## SAE 容量/稀疏度敏感性（未运行）

### 说明

建议在当前主实验基础上，少量补充 dictionary size 或 top-k sweep。优先级高于长度外推，因为它直接关系到 SAE 结论是否稳健。

### 结果展示

本轮已经生成 `phase4a_sae_sweep_summary.csv`，说明容量/稀疏度敏感性分析的代码出口已经具备。但当前配置仍只有 dictionary size = 4096、top-k = 64，因此还没有真正比较不同容量或稀疏度。

当前主配置结果仍显示：RoPE 的 SAE explained variance 最高，ALiBi 次之，PoPE 最低。后续如果要补强 SAE 结论，建议优先小规模增加 dictionary size 或 top-k，而不是增加长度外推。

## 解耦指标可靠性检查

### 说明

建议优先补 probe baseline、segment position accuracy、train/valid probe gap。当前 position R2 不稳定，这部分会直接影响论文说服力。

### 结果展示

本轮已经补上 probe baseline、segment position accuracy、token frequency bin probe 和 top-token identity probe。结果表明：

| 指标 | `alibi` | `pope` | `rope` | 主要含义 |
|---|---:|---:|---:|---|
| raw position bin baseline margin | 0.0588 | 0.0418 | 0.0520 | raw residual 中粗粒度位置读出略高于多数类 baseline |
| SAE position bin baseline margin | 0.0329 | 0.0262 | 0.0306 | SAE 中位置读出仍存在，但比 raw residual 弱 |
| raw segment position baseline margin | 0.1208 | 0.1073 | 0.1238 | 三段式位置比 16-bin 更稳定，RoPE/ALiBi 略高 |
| SAE segment position baseline margin | 0.0612 | 0.0480 | 0.0520 | SAE 保留部分粗粒度位置段信息，但 margin 明显下降 |
| SAE token category baseline margin | 0.1409 | 0.1298 | 0.1417 | SAE 中内容类别明显比位置更可读 |
| SAE token frequency baseline margin | 0.4214 | 0.4132 | 0.4259 | token 频率信息非常强，远高于位置 margin |
| SAE top-token identity baseline margin | 0.2356 | 0.2104 | 0.2523 | token 身份信息保留明显，RoPE 最强、PoPE 最弱 |

- 位置类 probe 超过 majority baseline，但 margin 明显小于内容/频率类 probe。
- SAE 中的位置 baseline margin 低于 raw residual，说明 SAE 没有强烈保留或拆分位置因素。
- 内容、频率、top-token identity 在 raw residual 和 SAE feature 中都更稳定可读。

因此，当前解耦结论比之前更稳：位置编码确实显著改变 attention 结构，但 SAE 中稳定保留的主要还是内容/频率信息；位置因素在 SAE feature 中存在，但不是强主导信号。

# 总结

综合目前 Task1 的结果，可以得到一个比较清晰的主线：不同位置编码确实显著改变了 attention 层面的位置信息组织方式，但这种改变并不会简单地等价为“训练效果更好”或“SAE 表征更好”。ALiBi、RoPE、PoPE 的差异更多体现为不同层面之间的错位关系。

第一，训练与泛化结果显示，ALiBi 的 final valid loss 最低，RoPE 次之，PoPE 最高；但训练 loss 曲线并没有同样清晰分层。这说明 PoPE 的问题不太像是训练失败，而更像是训练集拟合可以进行、但泛化和表示组织没有同等受益。ALiBi 的优势也不应被写成简单“模型更强”，而应写成“该位置偏置在当前训练预算下带来了更有利的泛化归纳偏置”。

第二，attention 结构给出了最强的机制证据。ALiBi 的 entropy、distance、local mass、far mass 都呈现高度稳定的 head-wise 模板：有些 head 长期局部，有些 head 专门承担远程注意力，而且这个相对比例跨层重复。量化上，ALiBi 的 entropy pattern consistency 约 0.949，distance pattern consistency 约 0.990，local_mass_16 pattern consistency 约 0.996；RoPE 和 PoPE 基本接近 0。这说明 ALiBi 不是只让 attention 更局部，而是把 head 的位置尺度分工稳定地写进了 attention score 结构中。

第三，SAE 结果没有跟随语言建模性能排序。RoPE 的 SAE explained variance 最高，ALiBi 次之，PoPE 最低；ALiBi 虽然 valid loss 最好，但深层 SAE 可重构性不如 RoPE。这说明 attention 层面的强位置先验并不自动转化为 residual 表征中更容易被 TopK SAE 重构的稀疏结构。换句话说，训练泛化优势、attention 结构规则性、SAE 可重构性是三个相关但不同的维度。

第四，位置/内容解耦结果表明，内容和频率信息比位置信息更稳定地进入 residual 与 SAE feature。位置 probe 的 baseline margin 确实为正，说明位置信息不是完全不可读；但无论 raw 还是 SAE，位置 margin 都明显小于 token category、token frequency 和 top-token identity。SAE 中 position bin margin 只有约 0.026-0.033，而 SAE token frequency margin 约 0.413-0.426，top-token identity margin 约 0.210-0.252。这支持一个重要结论：当前位置编码差异主要强烈体现在 attention 结构上，而在 SAE feature 层面，最稳定保留的是内容/频率相关信息。

第五，PoPE 的结果尤其值得谨慎分析。PoPE 的 Toeplitz deviation 最低，说明它更接近某种规则的相对位置结构；但它的 valid loss、SAE explained variance、SAE token identity margin 都不占优。这说明“更规则的位置结构”不必然带来更好的泛化或更好的 SAE 特征组织。这个发现对论文很有价值，因为它能避免把位置编码分析写成单纯的性能排名，而是强调位置结构、内容保留、稀疏可解释性之间存在复杂关系。

总体而言，目前最有价值的论文叙述应当是：ALiBi 通过强而稳定的 attention head 尺度模板改变信息流，并带来更好的语言建模泛化；RoPE 的 residual 表示更适合当前 SAE 重构，并在中深层更稳定保留 token 内容身份；PoPE 虽然更接近某些相对位置结构指标，但没有转化为更好的泛化或 SAE 表征。由此可以支撑本文的核心问题：位置编码影响的不只是模型性能，而是位置结构如何进入 attention、如何与内容信息分离或混合，以及这些结构最终是否被 SAE 捕捉为可解释特征。
