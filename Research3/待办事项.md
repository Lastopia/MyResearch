# 【项目目标与研究范围】
- [x] 明确研究问题：验证 ALiBi 的固定跨头距离尺度和单调距离惩罚是否限制长程信息利用。
- [x] 明确核心方法：RA-CABLE 在保留 CABLE 动态局部距离先验的基础上，使用真实 QK 内容分数减弱相关远端 token 的距离惩罚。
- [x] 将首轮实验限定为 RoPE、ALiBi、CABLE、RA-CABLE 四种方法。
- [x] 固定主干为 GPT-2 Small 级模型、训练长度 1024、每模型 1B token，并预注册 42–49 八个配对 seed；数据顺序固定使用 data seed 2025。
- [x] 完成理论设计、代码设计和实验设计文档。

# 【四种位置方法】
- [x] 实现 RoPE，并支持 KV cache 增量解码。
- [x] 实现 ALiBi 固定几何 head slope 和单调线性位置 bias。
- [x] 实现 CABLE6 的累计 ReLU 距离与 query-dependent scale。
- [x] 实现 RA-CABLE 的真实 QK relevance gate、CABLE bias 豁免和稀疏正则。
- [x] 保证四种方法共享相同 Transformer 主干初始化。
- [x] 保证 CABLE 与 RA-CABLE 的基础 CABLE 参数使用相同初始化。
- [x] 保持 RoPE 没有伪造的 additive position bias。

# 【模型与训练基础设施】
- [x] 实现统一入口 `python main.py run <stage|all>`。
- [x] 实现 FineWeb-Edu `sample-10BT` 数据准备、固定数据顺序和二进制 memmap token 数据集。
- [x] 实现 GPT-2 BPE tokenizer 固定版本与本地缓存。
- [x] 实现 AdamW、线性 warmup、cosine decay、梯度裁剪和梯度累积。
- [x] 实现自动检测合格 GPU 并为每张卡分配一个独立方法任务；当前不是 DDP。
- [x] 实现模型、optimizer、scheduler、RNG、数据 epoch 和 batch 位置的 checkpoint 保存与恢复。
- [x] 实现 checkpoint 配置指纹，防止配置不匹配时静默复用旧权重。
- [x] 实现以成功 checkpoint 为基准的有效训练 wall-clock/GPU-hours 时间账本；只累计取训练 batch、forward、backward 和 optimizer step，排除加载、日志、验证、评估与 checkpoint 写盘时间，失败且无法恢复的运行区间回滚。
- [x] 实现 `force=True` 时直接清除当前方法的旧训练状态；代码不保留历史格式或归档分支。
- [x] 单元测试通过：37 个测试和 4 个 subtest 全部通过；覆盖计时/恢复、双 checkpoint 评估、近重复 shingles、DAPE full/incremental、静态门控参数匹配和低 seed 禁止推断。
- [x] 默认启用自动 GPU 规划：自动选择 GPU 数量与并行任务数，并结合主机 RAM 限制并发。
- [x] 使用 RA-CABLE 完整 forward/backward/AdamW 试跑校准 micro-batch，在所有选中 GPU 上取满足 85% 显存安全预算的共同最大值。
- [x] 评估阶段固定 `DataLoader(num_workers=0)`，避免每 GPU 任务进程内再次创建短生命周期 worker 导致 `SIGABRT`；训练与适配仍使用自动规划的 workers。
- [x] 自动跳过空闲显存不足 4GB 或已有显存占用超过 10% 的 GPU，保存逐卡筛选原因与校准记录。

# 【检索适配与评估】
- [x] 实现训练长度 1024 内由程序生成的单 query key-value retrieval 适配；该数据只用于受控机制测试，不作为真实语料效果证据。
- [x] 实现由程序生成的 1024、2048、4096、8192 嵌套长度检索样本；该数据只用于测量距离变化，不能包装成真实长文本能力。
- [x] 实现 FineWeb-Edu held-out PPL 和 WikiText-103 PPL。
- [x] 实现 retrieval accuracy、near/far accuracy、distance-conditioned accuracy 和目标位置分桶。
- [x] 实现 RCUG：移除远端相关 value 后比较正确答案 NLL。
- [x] 实现 Length Degradation Rate。
- [x] 实现 multi-query associative recall，并报告逐 query accuracy 与整样本 exact match。
- [x] 实现多个共享前缀、仅成员 token 不同的相似 key 系统化干扰条件。
- [x] 让每个长度的目标位置循环覆盖开头、中间和结尾，同时保持跨长度目标关联身份不变。
- [x] 实现目标项与近端干扰项的位置交换实验，并报告交换前后 accuracy 差值。
- [x] 实现原生 RoPE 与线性 RoPE-PI 的正式双路评估；PI scale 为 `max(1, length / 1024)`。

# 【数据真实性与结果披露】
- [x] 正式语言模型预训练数据配置为 FineWeb-Edu `sample-10BT`，不是程序随机生成的语料。
- [x] 自然语言 PPL 评估配置包含 FineWeb-Edu held-out 和 WikiText-103。
- [x] 已明确识别：当前 retrieval 适配、retrieval 评估和 attention 审计中的 key-value 样本是程序生成的合成控制数据。
- [x] 所有结果表明确写出数据来源、数据版本、split、样本数和是否由程序生成；另存 `data_sources.json`。
- [x] 自然语料 PPL、真实长文档问答、合成控制、attention 审计和效率严格分表，禁止混合平均。
- [x] 合成数据指标统一放入 `synthetic_control.csv` 并标注 `program_generated=true`，报告禁止称为真实检索、真实问答或真实用户场景表现。
- [x] 增加固定 revision 的公开 QASPER validation 长文档问答，正式配置固定使用 64 个样本；与 QASPER/WikiText 精确或近重复的 FineWeb 文档在进入 split 前直接排除。
- [x] 增加 QASPER 连续真实论文窗口中的跨段依赖测试，报告 Answer NLL/PPL、Token F1、EM、证据移除利用增益和证据距离。
- [ ] 检查真实评估数据与 FineWeb-Edu 训练数据之间的文档泄漏或重复。
- [x] 保存 QASPER revision、paper/question ID、处理规则、document/evidence/sample SHA-256，使四种方法使用完全相同的样本。
- [ ] 最终论文关于“长程信息利用得到改善”的结论必须同时得到真实数据结果支持；只有合成任务变好时只能报告为机制线索。
- [ ] 若真实数据没有改善，必须如实报告负结果，不得用合成任务结果替代或掩盖。

# 【Attention 机制审计】
- [x] 分开保存和分析 content logit、additive position bias、最终 attention、CABLE context distance 与 RA-CABLE gate。
- [x] 实现 MAD、NAD、R90、GHF、RHDD 和 CAS。
- [x] 实现 BMVR、RAA、SESR、FER 和 attention sink ratio 的当前版本。
- [x] 审计默认覆盖第 0、5、11 层和 1024、2048、4096 长度。
- [x] 主统计只使用序列最后 25% 的 query。
- [x] 增加自然文本、同样本内容打乱文本和合成远端目标三类配对审计，并保存条件输入哈希。
- [x] 将 BMVR 主口径扩展为明确的远近 key pair 比较；相邻 key 违反率仅保留为诊断项。
- [x] 将 FER 扩展到生成器明确标出的全部远端无关 token mask。
- [x] 将 attention sink 扩展到 BOS/EOS、换行、分隔符、query 及合并特殊 token。
- [ ] 验证 RHDD/CAS 的变化是否与检索提升配对相关，而不只报告 attention 变远。

# 【效率与资源评估】
- [x] 实现参数量和位置模块新增参数统计。
- [x] 实现训练前向/反向、prefill 和 KV-cache decode 的时间测量。
- [x] 实现 median、P95、tokens/s、ms/token、峰值 VRAM 与主机 RAM 输出。
- [x] 实现训练与适配的 checkpoint-backed 纯 optimizer-step wall-clock 和 GPU-hours 汇总。
- [x] 正式 profiling 配置已调整为预热 20 次、测量 100 次、decode 256 token。
- [x] 训练 step profiling 覆盖完整 65,536-token effective batch、全部 gradient accumulation、梯度裁剪、正式 AdamW 参数组、optimizer step 和 scheduler step。
- [x] 使用阶段内 RSS 采样分别测量训练、prefill、decode 的主机 RAM 峰值，不再复用跨阶段 high-water mark。
- [ ] 在正式比较前固定实际硬件、软件版本、attention kernel、dtype 和 batch 条件。

# 【统计与报告】
- [x] 实现跨 seed 聚合、bootstrap 区间、配对 sign-flip 和 Holm 校正；单 seed 不伪造零宽区间，少于 8 个配对 seed 时 `raw_p/holm_p` 强制为空。
- [x] 实现 `summary.csv` 与 Markdown 摘要报告生成。
- [x] 正式配置为 seed 42–49；只有 8 个配对运行全部完成后才允许推断统计。
- [x] 增加 RA-CABLE 对 CABLE 的独立核心配对比较、Holm 校正及检索变化与 CAS/RHDD 等机制变化的配对记录。
- [ ] 在最终报告中补齐 PPL、Retrieval、LDR、MAD/NAD、GHF、RHDD、CAS、BMVR、RAA、SESR、FER 和完整效率指标。
- [ ] pilot 现象稳定后运行多个 seed；在此之前不得声称统计显著。

# 【当前正式实验进度】
- [ ] 使用当前最终代码从零完成 RoPE 的预训练、适配、评估、attention 审计和 profiling。
- [ ] 使用当前最终代码从零完成 ALiBi 的预训练、适配、评估、attention 审计和 profiling。
- [ ] 使用当前最终代码从零完成 CABLE 的预训练、适配、评估、attention 审计和 profiling。
- [ ] 使用当前最终代码从零完成 RA-CABLE 的预训练、适配、评估、attention 审计和 profiling。
- [ ] 四模型全部完成后重新运行 stats 和 report，现阶段不能比较最终优劣。
- [x] 删除旧 checkpoint 对应的本地历史输出，避免旧计时口径与当前结果混合。
- [ ] 从服务器同步由当前最终代码生成的 checkpoint、时间账本和完整输出。

# 【环境与文档一致性】
- [x] 当前服务器已识别到 4 张 NVIDIA L40S；默认资源配置改为自动模式，四张卡均空闲且 RAM 足够时并行运行四个独立方法任务。
- [x] 文档和 CFG 已更新为当前实际硬件“4 × NVIDIA L40S 48GB、最多同时运行四个独立单卡任务”。
- [ ] 确认所有正式结果来自同一硬件条件；不同 GPU 产生的效率结果不能直接混合比较。

# 【下一步执行顺序】
- [ ] 可选低成本检查：先执行 `python main.py run pilot`，仅运行 `run.seed` 对应的四方法完整流水线；该结果不得用于推断统计，正式 `run all` 会直接复用其 checkpoint。
- [ ] 第一优先级：同步最终代码并确认四种方法均显示 `state=fresh | checkpoint=none | step=0 | total=0s`。
- [ ] 第一优先级：执行 `python main.py run all`，从零完成四种方法的同口径实验。
- [ ] 第一优先级：确认 `training_time.json` 和 `adaptation_time.json` 只累计可由 checkpoint 恢复的 optimizer-step 时间，恢复时 JSONL 日志同步回滚。
- [ ] 第二优先级：完成四种方法的 1B token 训练及后续全部阶段。
- [ ] 第二优先级：同步四模型完整输出并生成第一版 pilot 汇总。
- [ ] 第二优先级：在汇总中把真实语料 PPL、合成检索和真实长文档任务严格分开标注。
- [x] 第三优先级：补齐 multi-query recall、自然/打乱文本审计、位置交换和严格 profiling 口径。
- [x] 第三优先级：补充固定版本 QASPER 真实长文档问答数据；最终结论仍必须等待四模型真实结果和完整泄漏检查。
- [ ] 第三优先级：重点比较 RA-CABLE 与 CABLE 的检索、RAA、SESR、FER、attention sink、短上下文 PPL 和资源开销。
- [ ] 第四优先级：只有当 pilot 同时满足性能、机制和成本要求时，才扩展到多个 seed。
- [ ] 最终交付：形成能够明确回答四项核心假设、包含限制说明和可复现实验命令的项目详情与研究报告。

# 【形成可信最终结论前的硬性缺口】
- [x] 配置 8 个配对 seed（42–49）并实现批量调度；三项主终点经 Holm 校正后理论上仍可达到 `p<0.05`。
- [ ] 在服务器实际完成全部 8 个配对 seed；代码配置完成不等于实验结果已经获得。
- [x] 预注册正式样本量：PPL 50 batch、QASPER 64、合成检索 512、attention 审计 16，并保证跨方法复用输入哈希。
- [x] 实现 FineWeb-Edu 对 QASPER/WikiText 的来源 ID、全文哈希和 token-shingle 近重复检查，命中文档在进入 split 前排除。
- [ ] 用最终 FineWeb 数据从零完成全量近重复扫描并核查 `leakage_check_complete=true`。
- [x] 将预训练与适配后 checkpoint 分开评估、保存和报告。
- [x] 正式 profiling 由调度器在同一物理 GPU 上逐个顺序运行；训练任务顺序按 seed 轮换方法—GPU 映射。
- [x] 实现逐方法/seed 的 `pip freeze --all` 锁文件及驱动/CUDA/cuDNN/PyTorch/Git 状态自动保存。
- [x] 每个方法/seed 自动保存 Git 状态、Python 包、PyTorch、CUDA、cuDNN、GPU 和平台环境清单，并设置 `CUBLAS_WORKSPACE_CONFIG=:4096:8`。
- [ ] 在正式服务器环境生成锁文件并使用干净 Git commit；`warn_only=True` 仍不保证 CUDA 逐位复现，必须依靠多 seed 稳健性。
- [x] 预注册 3 个确认性主终点和 6 个次要终点及方向；Holm 只用于三个主终点，次要字段作预注册描述性结果。
- [x] 实现 `ra_cable_static` 参数匹配静态门控消融，可通过 `python main.py run extensions` 对 8 个 seed 运行。
- [x] 接入 DAPE-v1 + Kerple 强数据自适应基线；核心公式、MLP 宽度和初始化来自作者 Apache-2.0 官方仓库提交 `bde344a844f2bd1f498b2bac70240dcda41c50c1`，并有 full/incremental 一致性测试。
- [ ] 在服务器实际完成 `ra_cable_static` 与 `dape_kerple` 的 8-seed 扩展实验。

# 【RA-CABLE 成功判定】
- [ ] 2048–8192 长度的远程检索和关联回忆优于 CABLE。
- [ ] 1024 长度 PPL 相对 CABLE 的恶化不超过 1%。
- [ ] RAA 和 SESR 提高。
- [ ] FER 和 attention sink ratio 没有明显恶化。
- [ ] RHDD/CAS 的改变与远程任务提升具有对应关系。
- [ ] 相对 CABLE 的训练时间、GPU-hours、prefill/decode 延迟、VRAM 和主机 RAM 增幅均已报告。
- [ ] 主要时间与显存开销增幅尽量控制在 10% 以内。
- [ ] 多 seed 复核后再形成统计性结论。
