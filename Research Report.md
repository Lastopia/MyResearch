# 面向大语言模型内生可解释性的结构干预研究

## 摘要

本文使用小型 GPT 式 Transformer 进行机制验证，研究是否能够通过设计 Transformer 的内部结构，诱导模型形成更容易被理解、分解和干预的表示，而不是完全依赖后验解释。本文追踪模型学习并形成语义特征表示的过程，分别对位置编码、Attention、FFN 稀疏性及显式概念接口进行结构修改与消融实验，试图找到能够可靠提升模型可解释性的方案。本文目前得到的结论及仍在检验的方向如下：

1. 解耦 _what–where_ 的位置编码并没有带来更好的 SAE 重构、特征稳定性或语言模型性能。一个可能的原因是，在位置编码中被区分的内容与位置信息会在后续计算中重新混合，因此仅修改位置编码难以直接改善模型内部表示的可解释性。

2. 在 Attention 和 FFN 中引入结构稀疏性，可以将 FFN 激活密度降低至约 9%，并稳定降低固定稀疏度 SAE 的重构误差。然而，更好的稀疏性与可压缩性并没有同步转化为更清晰的语义边界或更局部的因果干预。稀疏性因此只能作为可解释性的间接目标，不能直接等同于语义单一性。

3. ALiBi 和 CABLE 在位置编码实验中表现出较快的收敛速度和较好的语言模型性能，由此引出了 RA-CABLE：尝试根据 query–key 相关性，有条件地减弱远距离位置惩罚。目前该实验仍在进行，因此本文暂不对其有效性作出判断。

4. 从完整 FFN 中间激活中投影固定身份的概念槽，可以形成可读取、可消融和可交换的概念接口。但在参数量、投影结构和训练目标保持一致的条件下，额外加入跨 token 概念 Attention 的 Concept Bus V2 并未优于更简单的 Concept Projector，且在主要任务和概念指标上表现更差。这说明显式概念接口具有一定研究价值，但更复杂的通信结构不一定能够带来额外收益。

## 1. 引言

Transformer 已成为现代语言模型的基础结构 <sup>[1]</sup> (Vaswani et al., 2017)。在模型内部，语义信息需要经过 Attention 的上下文聚合，以及 FFN 的升维、非线性激活和降维写回。这些计算使语义特征不断发生混合与重组，也使其形成过程难以被直接追踪和解释。

现有研究可以大致分为两条路线。后验方法在模型训练完成后，使用探针、归因、回路分析或稀疏自编码器等方法解释已经形成的黑箱；内生方法则在模型结构或训练目标中预先加入概念、稀疏性、模块化或信息瓶颈等约束，使解释接口直接参与模型计算。

后验方法能够保留原模型的性能，但其解释通常是近似的，并可能受到特征分裂、特征吸收、跨随机种子不稳定以及忠实性不足等问题的影响 <sup>[2]</sup><sup>[3]</sup><sup>[4]</sup> (Huben et al., 2024; Chanin et al., 2025; Paulo et al., 2026)。内生方法则有机会使模型形成更容易理解、干预和审计的内部特征，但额外约束可能损害模型性能，人工设计的解释机制也可能被模型绕开。本文主要沿内生可解释性路线展开，并研究以下问题：

> 我们能否在基本保持语言模型性能和计算效率的前提下，通过结构干预诱导模型形成更容易理解、分解和干预的内部表示？

围绕这一核心问题，本文进一步研究四个子问题：

1. 改变位置编码中内容与位置的耦合方式，能否间接改善表示的 SAE 可分解性？
2. 在 Attention 与 FFN 中引入结构稀疏性，能否减少特征叠加并形成更容易解释的表示？
3. 根据内容相关性有条件地减弱位置惩罚，能否在保留局部先验的同时改善模型对远程信息的利用？
4. 从完整 FFN 中间激活中投影出的低维概念槽，如果在降维写回前进行受控的跨 token 通信，能否形成身份稳定、能够结合上下文且支持局部干预的概念接口？

## 2. 文献综述

### 2.1 机制可解释性与 SAE

机制可解释性的目标是把 Transformer 内部复杂的计算拆解为更容易分析的组成部分，例如特征、Attention 头、功能模块，以及多个部分共同完成任务时形成的计算回路 <sup>[5]</sup> (Elhage et al., 2021)。研究者可以使用线性探针判断模型表示中是否包含某种信息 <sup>[6]</sup> (Alain et al., 2016)，使用概念激活向量寻找概念在表示空间中的方向 <sup>[7]</sup> (Kim et al., 2018)，使用自动解释方法为神经元或特征生成自然语言描述 <sup>[8]</sup> (Bills et al., 2023)，并通过激活修补 <sup>[9]</sup> (Wang et al., 2023) 和因果追踪 <sup>[10]</sup> (Meng et al., 2022) 检验这些内部表示是否真正影响模型输出。然而，能够从表示中读出某个概念，并不意味着模型在推理时实际使用了该概念。不同特征方向经过 FFN 的非线性变换和后续计算后，其作用可能相互叠加、削弱或抵消。因此，解释结果还需要通过消融、替换和反事实干预检验其忠实性 <sup>[11]</sup> (Geiger et al., 2025)，并进一步判断其是否具有足够的覆盖率和可靠性，以支持对隐藏目标和风险行为的系统审计 <sup>[12]</sup> (Marks et al., 2025)。

SAE 是一种常用的后验可解释性方法。它把稠密激活编码为少量活跃的稀疏特征，并利用这些特征重构原始激活，以尝试分离表示空间中相互叠加的语义方向 <sup>[2]</sup> (Huben et al., 2024)。现有研究已经从大型语言模型中提取出与国家、城市和代码模式等抽象概念相关的特征 <sup>[13]</sup> (Templeton et al., 2024)。但稀疏性并不直接等同于语义单义性：一个概念可能被拆分到多个特征中，也可能被其他更具体的特征吸收 <sup>[3]</sup> (Chanin et al., 2025)；即使模型和训练数据相同，不同随机种子训练出的 SAE 也可能得到不同的特征集合 <sup>[4]</sup> (Paulo et al., 2026)。SAEBench 进一步表明，无监督重构代理指标的改善未必能够转化为特征解耦、模型遗忘等实际任务收益 <sup>[14]</sup> (Karvonen et al., 2025)。Transcoder 使用稀疏特征计算近似稠密 MLP，以支持更细粒度的回路分析 <sup>[15]</sup> (Dunefsky et al., 2024)；Crosscoder 学习跨层或跨模型共享的特征 <sup>[16]</sup> (Lindsey et al., 2024)；Circuit Tracing 则利用跨层 Transcoder 构建特定输入上的计算图 <sup>[17]</sup> (Ameisen et al., 2025)。然而，FFN 包含升维、非线性激活和降维写回，其中降维映射会把多个中间方向压缩到同一表示空间，因此输出通常不足以唯一恢复中间激活。即使后验方法能够准确近似模型计算并重构内部特征，也不能据此证明这些特征就是原模型实际使用的计算来源。

### 2.2 内生或设计型可解释性

2026 年的内生可解释性综述将相关结构归纳为功能透明、概念对齐、表示可分解、显式模块化和潜在稀疏诱导五类设计范式 <sup>[18]</sup> (Gao et al., 2026)。Concept Bottleneck Model 让模型先预测人类概念，再使用概念完成任务 <sup>[19]</sup> (Koh et al., 2020)；Codebook Features 使用稀疏离散代码形成更明确的内部地址 <sup>[20]</sup> (Tamkin et al., 2024)；可解释 MoE 尝试把不同功能分配给不同模块 <sup>[21]</sup> (Yang et al., 2025)。Shazeer 等提出的稀疏门控 MoE 主要用于扩展模型容量，并不是专门的可解释性方法，但为模块化路由提供了结构基础 <sup>[22]</sup> (Shazeer et al., 2017)。Lorsa 则以大量稀疏的一维注意力单元替换原多头注意力，尝试分解跨 token 的特征交互 <sup>[23]</sup> (He et al., 2026)。这些方法分别在概念预测、离散表示、模块路由和 Attention 交互层面引入显式结构，其干预位置和解释对象并不相同。这些内生性可解释性方法可能存在解释接口被模型绕开、损害模型性能，以及结构复杂度超过实际解释收益等风险。


### 2.3 Transformer 的结构切入点

位置编码与 Attention 共同影响 token 如何建立上下文关系。RoPE 将相对位置写入 query/key 的旋转相位 <sup>[24]</sup> (Su et al., 2024)；ALiBi 直接在注意力分数中加入距离偏置 <sup>[25]</sup> (Press et al., 2022)；PoPE 尝试区分内容幅值与位置相位 <sup>[26]</sup> (Gopalakrishnan et al., 2026)；CABLE 使用上下文相关的位置偏置 <sup>[27]</sup> (Veisi et al., 2025)；DIET 则把位置和分段信息从输入嵌入移入独立的位置注意力项 <sup>[28]</sup> (Chen et al., 2021)。相关分析表明，预训练位置嵌入能够编码多种位置特征 <sup>[29]</sup> (Wang et al., 2020)，部分 BERT 注意力头也会形成固定偏移、分隔符、句法和共指等模式 <sup>[30]</sup> (Clark et al., 2019)。然而，观察到这些模式并不意味着 Attention 权重能够直接构成忠实解释：大量注意力头可以在测试时删除而不明显损害模型性能 <sup>[31]</sup> (Michel et al., 2019)；当序列长度超过头维度时，相同输出还可能对应不同的注意力权重分布 <sup>[32]</sup> (Brunner et al., 2020)。Jain 和 Wallace 据此指出，差异显著的 Attention 分布可能产生近似相同的预测 <sup>[33]</sup> (Jain et al., 2019)；Wiegreffe 和 Pinter 则认为这一结论取决于解释定义和对照设计，并提出跨 seed、均匀权重和对抗训练等更严格的检验 <sup>[34]</sup> (Wiegreffe et al., 2019)。因此，本文将位置编码和 Attention 视为通过信息路由间接干预语义形成的机制。已有研究虽然分析并尝试解耦内容与位置，但这种结构解耦能否直接改善内部表示的 SAE 可分解性与可解释性，仍缺少直接的实验证据。

FFN 对每个已经上下文化的 token 进行非线性特征变换，并可被理解为一种 key-value memory <sup>[35]</sup> (Geva et al., 2021)。因此，FFN 中间层是稀疏化、概念分组和固定地址设计的重要位置。Sparsemax、Entmax、ProSparse 和 Spark Transformer 分别探索了稀疏概率、稀疏激活和稀疏路由 <sup>[36]</sup><sup>[37]</sup><sup>[38]</sup><sup>[39]</sup> (Martins et al., 2016; Peters et al., 2019; Song et al., 2025; You et al., 2025)。然而，这些研究大多以计算效率或模型性能为主要目标。结构稀疏性虽然可能减少同时参与计算的特征及其叠加，但并不必然形成语义单一、身份稳定或因果局部的表示。因此，稀疏性只能作为可解释性的间接指标，还需要结合 SAE 重构、语义边界、跨随机种子稳定性和因果干预结果进行验证。

如图 1 所示，本文主要关注语义表示形成过程中的六个结构位置：Token Embedding 提供 token 身份信息，Positional Encoding 提供位置关系，Attention 聚合跨 token 上下文，随后 FFN Up 将表示升维，FFN Hidden-Layer Activation 形成中间激活，FFN Down 再将其写回残差空间。四个实验分别针对其中的位置机制、Attention 和 FFN 中间激活进行结构干预。

![Transformer 中的主要信息流](figures/transformer-information-gradient-flow.svg)

*图 1　本文关注的 Transformer 主要信息流。Token Embedding 与 Positional Encoding 共同影响 Attention，随后上下文化表示依次经过 FFN Up、FFN Hidden-Layer Activation 和 FFN Down；模块内标记给出四个实验的主要结构切入点。*

### 2.4 研究缺口与本文目标定位

现有研究已经从位置编码、稀疏结构、概念瓶颈和后验特征分解等方向提高模型内部计算的可分析性，但这些方向与本文关注的结构可解释性之间仍存在以下缺口：

- 位置编码与表示可解释性：现有位置编码研究主要关注语言模型性能和长度外推。虽然已有工作分析或尝试解耦内容与位置，但这种解耦能否改善内部表示的 SAE 可分解性和特征身份稳定性，仍缺少直接证据。
- 结构稀疏与语义单一性：现有稀疏 Attention 和稀疏 FFN 研究多以计算效率或模型性能为主要目标。结构稀疏能否在改善 SAE 重构的同时形成更清晰的语义边界和更局部的因果作用，尚未得到充分验证。
- 内容相关的信息路由：CABLE 将 ALiBi 固定的线性距离偏置改为上下文相关的可学习偏置，但位置惩罚仍随距离单调增加，无法根据 query–key 相关性为远距离的重要信息提供豁免。这是否限制模型对远程信息的利用，仍需进一步检验。
- FFN 概念接口：Concept Bottleneck、Codebook 和后验读出方法能够提供不同形式的显式概念接口，但从 FFN 中间激活投影出的概念是否应在降维写回前进行跨 token 通信，以及这种额外通信是否优于简单概念投影，仍缺少参数匹配的受控比较。
- 统一评价不足：相关研究往往分别报告模型性能、稀疏度、可读出性或计算效率，较少在同一受控实验中联合检验表示保真度、语义质量、因果作用和工程成本。因此，单一代理指标的改善通常不足以证明总体可解释性得到提升。

本文围绕上述缺口设计四组受控实验，分别干预位置编码、Attention 和 FFN 中间激活。本文不预设任何结构或代理指标能够直接代表总体可解释性，而是通过简单基线、参数匹配、跨随机种子复验和因果干预，检验新增结构是否产生独立且可复核的收益。

## 3. 总体研究方法

### 3.1 研究策略

本文采用逐步收紧的可证伪研究策略。各实验首先提出结构假设，再在控制数据、模型规模、训练预算和 SAE 设置的条件下比较基线与变体。只有当新增结构相对于更简单的公平对照显示独立收益时，才具备进一步扩展的实验依据。

四个研究方向的配置并不完全相同，因此本文只在同一实验内部比较数值，不把不同实验的绝对指标直接拼接为统一排名。

**表 1　本文研究设计总览**

| 实验   | 结构切入点                | 核心问题                          | 主要评价维度                                        |
| ---- | -------------------- | ----------------------------- | --------------------------------------------- |
| 实验 1 | 位置编码与 Attention      | 内容—位置解耦能否改善表示的 SAE 可分解性？      | 语言模型性能、内容—位置相关性、SAE 重构、跨 seed 特征稳定性           |
| 实验 2 | Attention 与 FFN 中间激活 | 结构稀疏能否减少特征叠加并促进语义单一性？         | 语言模型性能、激活稀疏度、SAE 重构、语义边界、因果局部性、时间成本           |
| 实验 3 | 位置编码与 Attention      | 内容相关的位置惩罚豁免能否改善远程信息利用？        | 短程与长程语言性能、远程检索、语义豁免与错误豁免、Attention 模式、时间与显存成本 |
| 实验 4 | Attention 与 FFN 中间激活 | 降维写回前的概念槽通信能否形成身份稳定且可干预的概念接口？ | 任务性能、概念可读出性、置零与交换干预、因果局部性、参数量与时间成本            |

### 3.2 评价维度

本文不把可解释性视为能够由单项指标代表的目标，而将表示目标、因果证据和方法可靠性归纳为以下几个相互关联的维度：

- 可读出性与人类可理解性：概念不仅能够被探查出，还应具有稳定且可以被人理解的语义描述。
- 语义单一性与身份稳定性：单个特征应具有相对集中的语义边界，并在上下文、模型和随机种子变化下保持可辨认的概念身份。
- 层级性与可组合性：低层概念能够组合为高层概念，使概念关系和干预传播具有可追踪的结构。本文将其作为理想目标，但没有完成子概念向父概念的显式聚合实验。
- 忠实性：解释变量应真实参与原模型的计算，而不只是能够从表示中被后验读出。
- 因果可控性：对解释变量进行定向干预时，应产生预期效果，并尽量限制无关副作用。
- 可审计性：解释方法应具有足够的覆盖率、稳定性和可复核记录，以支持对未知行为的系统检查。
- 稀疏性：少量活跃特征有利于检索和局部干预，但稀疏本身不能保证清晰的语义边界，因此只作为间接目标。

这些目标与实验指标并非一一对应。可读出性、语义单一性和身份稳定性主要通过语义指标评价；忠实性与因果可控性需要置零、交换或证据移除等干预结果支持。实验 4 只探索了固定身份概念槽及其跨 token 组合，没有实现子概念向父概念的显式聚合，因此不能视为层级化验证。跨 seed 复验、统计区间和实验记录只能提高结果的可复核性，也不能单独证明方法已经具备完整的可审计性。

本文采用的证据分为以下六组。其中，语义质量和因果作用是可解释性的主要证据；任务性能、结构稀疏和工程成本用于检查方案是否有效且可实施，不被直接视为可解释性提升。表中列出各实验已经使用或预先规定的指标，并不表示每个实验都会使用同一行中的全部指标。

| 实验评价层面 | 作用 | 对应实验 | 已使用或预先规定的指标 |
|---|---|---|---|
| 任务与模型性能 | 检查结构修改是否损害模型能力 | 实验 1–4 | 验证交叉熵、困惑度、远程检索、Exact、COUNTRY 与 COLOR 准确率 |
| 结构效果与 SAE 重构 | 检查结构是否按预期生效，以及表示是否更容易重构；不直接代表可解释性 | 实验 1、2 | 内容—位置得分相关性、Attention 距离模式、FFN 激活密度、SAE 解释方差、归一化均方误差和损失恢复率 |
| 语义质量 | 检查特征身份、内部一致性和语义边界 | 实验 1、2、4 | 跨 SAE seed 特征稳定性、token 身份保留、上下文一致性、概念 AUPRC、困难负例分离度和概念 Macro-F1 |
| 因果作用 | 检查解释变量是否被模型使用，以及干预是否局部 | 实验 2–4 | 必要性与充分性隔离度、证据移除、概念槽置零、交换成功率和非目标副作用率 |
| 结果可复核性 | 检查结论是否依赖单次初始化或单一统计口径 | 实验 1–4 | 跨 seed 均值与离散度、置信区间、配对检验、多重校正，以及配置、代码和输出记录 |
| 工程可行性 | 检查新增结构的实际成本；不属于可解释性本身 | 实验 2–4 | 参数量、MAC、训练时间、吞吐和峰值显存 |

## 4. 实验 1：位置编码与表示可分解性

### 4.1 研究动机与假设

最初假设认为，RoPE 将内容与位置深度耦合，而 PoPE 将内容幅值与位置相位显式区分，因此 PoPE 可能产生更容易被 SAE 分解的残差表示。实验在统一的 GPT 式 Transformer 与训练协议下比较 RoPE、原始 PoPE、修订 PoPE、ALiBi 与 CABLE，并在多个残差层训练 Top-K SAE。

### 4.2 结构与核心公式

**RoPE。** 将 query/key 的每两个维度视为一个具有幅值 $\mu$ 和内容相位 $\phi$ 的二维向量。加入位置旋转后，其注意力分数可写为 <sup>[24]</sup> (Su et al., 2024)：

$$
a_{ts}^{RoPE}
=\sum_{c=1}^{d/2}
\mu_{q_{tc}}\mu_{k_{sc}}
\cos\left((s-t)\theta_c+\phi_{k_{sc}}-\phi_{q_{tc}}\right)
$$

其中 $(s-t)\theta_c$ 表示相对位置，$\phi_{k_{sc}}-\phi_{q_{tc}}$ 来自 query/key 的内容。内容相位会改变位置响应最大的方向，因此 RoPE 的“what”与“where”在同一个余弦项内发生耦合。

**PoPE。** 实验实现将每两个 query/key 维度组成一对，先计算该对的内容幅值：

$$
r_i(x)=\sqrt{x_{2i}^2+x_{2i+1}^2+10^{-9}}.
$$

原始 PoPE 对该幅值再使用 softplus，而修订 PoPE 保留原幅值：

$$
\mu_i^{origin}(x)=\operatorname{softplus}(r_i(x)),\qquad
\mu_i^{modify}(x)=r_i(x).
$$

query 和 key 的方向均被替换为位置相位加可学习的逐头偏置：

$$
\phi^Q_{t,h,i}=t\theta_i+b^Q_{h,i},\qquad
\phi^K_{s,h,i}=s\theta_i+b^K_{h,i},
$$

其中可学习偏置被限制在 $[-\pi,\pi]$。因此每对维度对注意力分数的贡献为：

$$
a_{ts,h}^{PoPE}=\sum_i\mu_i(q_{t,h})\mu_i(k_{s,h})
\cos\left((t-s)\theta_i+b^Q_{h,i}-b^K_{h,i}\right).
$$

与 RoPE 相比，PoPE 用内容幅值保留 query/key 信息，并以位置和可学习的注意力头偏置决定方向，删除了原始 query/key 的内容方向。实验分别实现原始 PoPE 与移除 softplus 幅值变换的修订 PoPE，后者用于检验该幅值变换是否构成性能差异的主要来源 <sup>[26]</sup> (Gopalakrishnan et al., 2026)。

**ALiBi。** ALiBi 不旋转 query/key，而是在第 $h$ 个注意力头的 logit 上加入固定线性距离惩罚 <sup>[25]</sup> (Press et al., 2022)：

$$
\ell_{ts,h}^{ALiBi}
=\frac{q_{t,h}^{\top}k_{s,h}}{\sqrt{d_h}}-m_h(t-s),\qquad s\le t
$$

各头斜率 $m_h$ 在训练前按固定规则设置。对于相同距离，不同头的位置偏置比例保持不变：

$$
\frac{|b_{ts,h}|}{|b_{ts,h'}|}=\frac{m_h}{m_{h'}}
$$

这并不意味着实际 Attention 权重完全固定，因为内容项仍会变化；固定的是各头的距离先验和相对尺度。本实验观察到 ALiBi 的 Attention 距离模式一致性达到 0.9900，说明固定斜率按预期形成了稳定的头部距离模板。该结果主要用于验证结构是否按设计工作，不作为独立的可解释性收益。

**CABLE。** CABLE 将 ALiBi 的固定斜率推广为由输入内容决定的 token 特定、注意力头特定偏置。实验中对每个注意力头先计算非负增量，并形成负的累计坐标 <sup>[27]</sup> (Veisi et al., 2025)：

$$
u_h(X_i)=\operatorname{ReLU}(X_iw_{c,h}+b_{c,h}),\qquad
S_{i,h}=-\sum_{r\le i}u_h(X_r).
$$

再学习当前位置对该偏置的缩放：

$$
g_h(X_i)=\operatorname{softplus}(X_iw_{s,h}+b_{s,h})
$$

$$
B_{ij,h}(X)=g_h(X_i)\left(S_{i,h}-S_{j,h}\right)
$$

在因果掩码区域 $i\ge j$ 时，$S_{i,h}-S_{j,h}$ 是从 $j$ 到 $i$ 的非正累计增量，因此 $B_{ij,h}(X)$ 构成内容相关的距离惩罚并直接加入注意力 logit。ALiBi 的惩罚只由固定斜率和距离决定，而 CABLE 的单位距离增量与当前位置缩放都依赖输入，因此不同注意力头和不同 token 可以形成不同的相对距离模式。

### 4.3 实验设计与指标

语言模型使用 OpenWebText 和 GPT-2 分词器，序列长度为 1024；训练集和验证集分别包含 20,000 与 2,000 个预分块序列。模型包含 12 层、12 个注意力头，$d_{model}=768$、$d_{ff}=3072$、dropout 为 0.1，词表大小为 50,257，输入与输出嵌入共享参数。RoPE 与 ALiBi 各含 123,653,376 个参数；两种 PoPE 分别增加 9,216 个参数（0.0075%），CABLE 增加 221,472 个参数（0.1791%），均低于预设的 1% 参数差异容限。

每种结构使用模型 seed 41、42 和 43 独立训练 30,000 步，批量大小为 8，每次训练处理 245.76M token。优化器为 AdamW，初始学习率 $6\times10^{-5}$，权重衰减为 0.1，前 500 步线性预热，随后采用余弦调度；训练使用 bf16，梯度范数裁剪为 1.0。主结果统一使用第 30,000 步的最终检查点，以匹配训练步数和 token 预算；验证损失匹配检查点只用于稳健性分析。实验运行于单张 Tesla V100-SXM2-32GB。

SAE 提取第 2、6、10 个 Transformer 模块的 `residual_post_block` 激活。主比较使用字典大小 4096、Top-K 64、SAE seed 42、43 和 44；每个 SAE 训练 2,000 步，最多使用 65,536 个训练激活 token 和 16,384 个验证激活 token。字典大小 2048/8192 与 Top-K 32/128 的单轴扫描用于检验结论对容量和稀疏度的敏感性。

比较内容包括语言建模性能、Attention 距离、SAE 解释方差、跨 SAE 随机种子稳定性、token 身份保留，以及内容—位置得分相关性。

**指标与计算口径。** 本实验采用以下计算方式。

验证时先对各批次的下一 token 交叉熵求均值，困惑度计算为：

$$
\operatorname{PPL}=\exp\left(\min(\mathcal{L}_{valid},20)\right)
$$

其中截断 20 只是数值保护，本实验的 loss 远低于该值。

验证损失曲线下面积对相邻验证点使用梯形积分，再除以首末验证 step 的间隔。各方法使用相同的验证步，因此该值越低，表示整个训练过程中验证损失总体越低。

对于注意力权重 $\alpha_{b,h,t,s}$，首先计算每个 query 的加权绝对距离，再对 batch 和 query 位置求平均：

$$
D_{l,h}=\frac{1}{BT}\sum_{b,t}\sum_s\alpha^{(l)}_{b,h,t,s}|t-s|.
$$

其中 $B$ 为批次数，$T$ 为 query 位置数，$\alpha^{(l)}_{b,h,t,s}$ 为第 $l$ 层、第 $h$ 个头从 query 位置 $t$ 指向 key 位置 $s$ 的归一化注意力权重。

结果表中的 Attention 平均距离继续对头、层和模型 seed 汇总。该指标只描述注意力偏局部还是偏长程，不评价关注内容的语义质量；类似用法可参考 Vig 和 Belinkov <sup>[40]</sup> (Vig et al., 2019)。

Attention 距离模式一致性先把第 $l$ 层所有头的距离组成向量 $\mathbf d_l=(D_{l,1},\ldots,D_{l,H})$，计算所有层对之间沿 head 维度的 Pearson 相关，再取平均：

$$
C_{dist}=\frac{2}{L(L-1)}\sum_{l<r}\operatorname{Corr}_{h}(\mathbf d_l,\mathbf d_r).
$$

其中 $L$ 和 $H$ 分别为模型层数与注意力头数，$\operatorname{Corr}_h$ 表示沿注意力头维度计算 Pearson 相关。

实验同时计算相邻层相关性，但表中使用的是全部层对相关性的均值，并进一步跨模型随机种子汇总。高值表示相同注意力头编号的相对距离模式在不同层重复；由于相同编号不保证语义对齐，该指标只能描述结构模式，不是“越高越好”的可解释性分数。

下图给出 seed 41 的逐头 Attention 平均距离。ALiBi 在 L1、L6 与 L10 中反复呈现近似相同的头部距离顺序，而 CABLE 未呈现同样的跨层编号对应关系；seed 42 和 43 的输出也具有相同的定性差异。ALiBi 的 Attention 距离模式一致性为 0.9900，而 CABLE 的全部层对平均相关为 0.0032。后者表示平均线性相关接近零，不代表每一层对均完全无关。各面板纵轴独立缩放，因此该图用于比较头部模式及其跨层稳定性，不用于直接比较不同面板的柱高。

| 层 | CABLE | ALiBi |
|---|---|---|
| L1 | ![CABLE seed 41 layer 1 attention distance](figures/experiment1-cable-seed41-layer1-attention-distance.png) | ![ALiBi seed 41 layer 1 attention distance](figures/experiment1-alibi-seed41-layer1-attention-distance.png) |
| L6 | ![CABLE seed 41 layer 6 attention distance](figures/experiment1-cable-seed41-layer6-attention-distance.png) | ![ALiBi seed 41 layer 6 attention distance](figures/experiment1-alibi-seed41-layer6-attention-distance.png) |
| L10 | ![CABLE seed 41 layer 10 attention distance](figures/experiment1-cable-seed41-layer10-attention-distance.png) | ![ALiBi seed 41 layer 10 attention distance](figures/experiment1-alibi-seed41-layer10-attention-distance.png) |

*图 3　CABLE 与 ALiBi 在模型 seed 41、不同 Transformer 层中的逐头 Attention 平均距离。横轴为注意力头编号，纵轴单位为 token 间隔；各面板纵轴独立缩放，不能直接根据不同面板的柱高比较绝对距离。模型使用第 30,000 步最终检查点。*

SAE 在标准化后的残差激活 $x$ 上计算解释方差，并对每一维分别求总体方差后求和：

$$
\operatorname{EV}=1-\frac{\sum_d\operatorname{Var}(x_d-\hat{x}_d)}{\sum_d\operatorname{Var}(x_d)}.
$$

其中 $x$ 与 $\hat{x}$ 分别为原始残差激活和 SAE 重构，$d$ 为残差维度。

高 EV 表示当前 SAE 重构保留了更多激活变化，但不能单独证明特征语义单一或人类可读 <sup>[2]</sup> (Huben et al., 2024)。

跨 SAE 随机种子稳定性使用字典大小 4096、Top-K 为 64 的 SAE。实验先将 decoder 列向量单位化并计算余弦相似度矩阵，只保留互为最近邻的特征对：

$$
S_{ij}=\cos(w_i^{A},w_j^{B}),\qquad
j=\arg\max_{j'}S_{ij'},\quad i=\arg\max_{i'}S_{i'j}.
$$

其中 $w_i^A$ 和 $w_j^B$ 分别为两次 SAE 训练的第 $i$、$j$ 个归一化解码器特征方向，$S_{ij}$ 为其余弦相似度。

随后让两套 SAE 编码同一批最多 4096 个验证残差 token，对每个匹配特征计算激活 Pearson 相关，表中报告这些相关性的均值，并再跨 SAE seed 对、层和模型 seed 汇总。它衡量当前匹配规则下的功能复现性；未被互最近邻匹配的特征不进入相关性均值，因此还需结合匹配率判断 <sup>[4]</sup> (Paulo et al., 2026)。

SAE token 身份保留裕量使用训练集中最高频的 32 个 token ID 加一个 `other` 类，共 33 类。SAE 激活按训练集均值和标准差标准化后，训练单层线性分类器；当前配置为 8192 个训练 token、4096 个验证 token、200 个 AdamW 更新步和批量大小 512。多数类基线由训练标签决定，并在验证集上计算：

$$
M_{token}=\operatorname{Accuracy}_{probe}-\operatorname{Accuracy}_{majority}.
$$

该指标只衡量 token 身份能否从 SAE 激活中线性读出，不证明单个 SAE 特征就是独立的 token 概念。线性探针的一般方法可参考 Alain 和 Bengio <sup>[6]</sup> (Alain et al., 2016)。

**内容—位置得分相关性。** 该指标不使用上述线性探针的输出，而由每个 SAE 特征的相关性和互信息直接构造。实验先将验证集中的每个特征激活按分位数离散为 10 个区间。位置分数取“激活与归一化绝对位置的绝对 Pearson 相关”和“特征区间与 16 类位置区间的归一化互信息”中的较大者：

$$
p_j=\max\left(\left|\operatorname{Corr}(z_j,pos)\right|,\frac{I(\operatorname{bin}_{10}(z_j);pos_{16})}{H(pos_{16})}\right).
$$

内容分数取 token 类别和 token 频率两种归一化互信息中的较大者：

$$
c_j=\max\left(\frac{I(\operatorname{bin}_{10}(z_j);category)}{H(category)},\frac{I(\operatorname{bin}_{10}(z_j);frequency)}{H(frequency)}\right).
$$

其中 token 类别分为字母、数字、标点、空白和其他五类，频率按训练集 token 频次的三分位数分为三类。最终指标是在同一 SAE 的全部字典特征上计算：

$$
\rho_{CP}=\operatorname{Corr}_{j}(c_j,p_j).
$$

90% 分位阈值只用于把非死亡特征标记为仅内容、仅位置、混合或低选择性，不参与 $\rho_{CP}$ 的计算。约 82% 的特征被归入低选择性，说明当前内容和位置标签只覆盖部分 SAE 语义。因此，本文只用 $\rho_{CP}$ 比较不同方法下内容与位置的解耦程度，不用它单独判断总体可解释性。

### 4.4 关键结果

表中数值为均值 ± 样本标准差，独立统计单位是三个语言模型训练 seed（41、42、43，$n=3$）。Attention 平均距离先在每个模型 seed 内跨层平均；Attention 距离模式一致性则先计算该 seed 的全部层对相关均值。SAE 指标先在每个模型 seed 内跨第 2、6、10 层及 SAE seed 42、43、44 平均，再跨模型 seed 汇总；跨 SAE seed 稳定性指标还先对每层的三个 SAE seed 配对取平均。该分层汇总避免把同一语言模型产生的不同层、SAE 重复或 SAE 配对误当成独立样本。由于 $n=3$ 较小，标准差只描述跨训练 seed 波动，不用于宣称统计显著性。

![实验一的验证损失轨迹](figures/experiment1-validation-loss-trajectories.png)

*图 4　五种位置编码在模型 seed 41、42、43 下的验证损失轨迹。各方法使用相同的验证 step，CABLE 的三条轨迹整体最低，ALiBi 次之。*

| 指标 | RoPE | 原始 PoPE | 修订 PoPE | ALiBi | CABLE |
|---|---:|---:|---:|---:|---:|
| 验证集困惑度（低好） | 66.20 ± 0.45 | 75.90 ± 0.43 | 76.33 ± 0.58 | 64.01 ± 0.16 | **61.86 ± 0.08** |
| 验证损失曲线下面积（低好） | 4.3783 ± 0.0040 | 4.4903 ± 0.0044 | 4.5060 ± 0.0054 | 4.3257 ± 0.0030 | **4.2921 ± 0.0017** |
| Attention 平均距离（描述性） | 120.65 ± 0.88 | 53.97 ± 0.50 | 82.44 ± 0.37 | 36.37 ± 0.53 | 49.81 ± 1.61 |
| Attention 距离模式一致性（结构验证） | 0.0058 ± 0.0238 | 0.0281 ± 0.0485 | −0.0053 ± 0.0337 | 0.9900 ± 0.0040 | 0.0032 ± 0.0140 |
| SAE 解释方差（高好） | **0.7409 ± 0.0055** | 0.7086 ± 0.0016 | 0.7197 ± 0.0009 | 0.7225 ± 0.0027 | 0.7225 ± 0.0080 |
| SAE 跨 seed 互近邻匹配率（高好） | 0.4979 ± 0.0043 | 0.4956 ± 0.0009 | **0.5010 ± 0.0035** | 0.4754 ± 0.0021 | 0.4699 ± 0.0016 |
| SAE 跨 seed 激活相关性（匹配后均值，高好） | **0.4593 ± 0.0019** | 0.4175 ± 0.0008 | 0.4356 ± 0.0018 | 0.4326 ± 0.0010 | 0.4325 ± 0.0015 |
| SAE token 身份保留裕量（高好） | **0.2547 ± 0.0060** | 0.2134 ± 0.0063 | 0.2244 ± 0.0052 | 0.2358 ± 0.0055 | 0.2352 ± 0.0049 |
| SAE 内容/位置得分相关性（低表示解耦更明显） | 0.1035 ± 0.0098 | **0.0773 ± 0.0066** | 0.0819 ± 0.0040 | 0.1065 ± 0.0135 | 0.1115 ± 0.0130 |

内容—位置得分相关性衡量同一个 SAE 特征的内容得分与位置得分是否共同升高。较低数值表示二者在当前评分规则下较少共同出现，即内容与位置的解耦更明显，但不代表总体性能或可解释性更好。原始 PoPE 在该指标上取得最低值，修订 PoPE 次之；然而，两者的 SAE 解释方差、稳定性、token 身份保留和语言建模性能均未显示相应收益。修订 PoPE 的困惑度略高于原始 PoPE，说明移除 softplus 幅值变换没有修复其语言建模差距。图 4 和表中的曲线下面积显示，CABLE 在当前训练预算下的验证损失轨迹与最终困惑度最低，ALiBi 次之；这一结果描述的是当前实验中的语言模型性能差异，不能证明残差表示更适合 SAE 分解。

### 4.5 结论与研究启示

RoPE—PoPE 的核心假设未得到支持：PoPE 在当前特征评分下呈现较低的内容—位置相关性，但没有获得更好的 SAE 解释方差、跨 seed 激活稳定性或 token 身份保留，同时困惑度更高。该结果与“过早限制内容—位置自由耦合可能损害表达能力”的理论风险一致。然而，本实验没有直接追踪位置与内容在后续层中的动态混合过程，因而尚不能确认具体作用机制。在本实验配置下，位置编码对 SAE 可解释性的影响较为间接，不构成后续结构干预的优先切入点。

除预设假设外，ALiBi 与 CABLE 在固定训练预算下呈现更低的验证损失轨迹与最终困惑度。ALiBi 的固定斜率为各头提供稳定的距离先验，使较远位置受到更强惩罚，但也可能限制模型在复杂或更长语境中重新分配各头的距离偏好。ALiBi 接近 1 的 Attention 距离模式一致性主要验证了固定斜率按预期形成稳定模板，不是可解释性收益。CABLE 通过可学习、上下文相关的位置增量与权重解除这一固定比例；其 Attention 距离模式一致性为 0.0032，表明跨层头部距离向量的平均相关接近零。该结果尚不能证明 CABLE 具有更高可解释性，但为继续研究兼具高效距离先验与上下文自适应头部尺度的注意力机制提供了实验依据。

## 5. 实验 2：结构稀疏能否转化为可解释性

### 5.1 假设与方法

**研究动机。** 实验一表明，位置编码对 SAE 可解释性的影响较为间接。因此，本实验直接修改负责 token 间信息路由的 Attention 以及负责 token 内特征变换的 FFN，以检验结构稀疏能否在表征形成阶段减少特征叠加。候选结构包括 Top-K Attention、Grouped FFN、Sparse FFN 和 L1 激活损失；最终受控实验比较标准 Transformer、受 Spark Transformer 启发的 SP Attention + SP FFN，以及面向实际计算效率设计的 fast-SP <sup>[38]</sup><sup>[39]</sup><sup>[41]</sup> (Song et al., 2025; You et al., 2025; Madhyastha et al., 2026)。

**假设。** 结构稀疏性可能促进语义单一性。如果每个 token 只从少量上下文位置接收信息，并只激活少量 FFN 单元，不同语义在同一表示中的叠加可能减少，从而形成边界更清楚、身份更稳定的特征。这样的表示也应更容易由固定稀疏度的 SAE 重构，并可能支持更局部的因果干预。然而，SAE 重构改善仅构成中间证据；稀疏表示的语义单一性仍需通过上下文一致性、困难负例边界和因果指标分别验证。

![实验二的 Attention 与 FFN 稀疏化流程](figures/experiment2-sparsity-flow.svg)

*图 5　实验二同时修改跨 token 的 Attention 路由和 token 内的 FFN 激活。评价将结构稀疏、SAE 可压缩性、语义边界、因果局部性与实际计算成本分开，避免把较低激活密度直接等同于可解释性或加速。*

**SP Attention。** 每个注意力头的 query 和 key 被拆分为 predictor 与 value 两个子空间：

$$
Q=[Q_p,Q_v],\qquad K=[K_p,K_v]
$$

predictor 子空间决定保留哪些历史位置，value 子空间决定被保留位置的附加门值：

$$
S_p=\frac{Q_pK_p^\top}{\sqrt r}+B_{pos},\qquad
S_v=\frac{Q_vK_v^\top}{\sqrt{d_h-r}}
$$

为避免逐行精确排序，实验使用正态近似阈值选择候选位置。对某个 causal row 中的有效 predictor scores，阈值为：

$$
\theta=\mu+\sigma\Phi^{-1}\left(1-\frac{k}{n}\right)
$$

其中 $n$ 是该行可见位置数，$k=256$。保留满足 $S_p\ge\theta$ 的位置后，Attention 权重计算为：

$$
A=\operatorname{softmax}(S_p\mid S_p\ge\theta)\odot\operatorname{softplus}(S_v),\qquad O=AV
$$

该实现不对相乘后的 $A$ 再次归一化。CABLE 条件下，内容相关位置偏置 $B_{pos}$ 只加入 predictor score。最终实验的上下文长度为 1024，因此 $k=256$ 确实限制了候选范围；但阈值只是近似目标数量，实际保留数不保证严格等于 256。

**SP FFN。** 输入维度同样被拆分为 predictor 与 value 两部分。predictor 先估计哪些 FFN 单元应被激活：

$$
p=W_px_p,\qquad
\theta_p=\mu(p)+\sigma(p)\Phi^{-1}\left(1-\frac{k_f}{d_{ff}}\right)
$$

随后以软阈值形成门控，并与 value 分支逐元素相乘：

$$
g=\operatorname{GELU}\big(\operatorname{ReLU}(p-\theta_p)\big),\qquad
h=g\odot W_vx_v,\qquad y=W_oh
$$

实验设置 $k_f/d_{ff}=0.08$。由于统计阈值并非精确 Top-K，实际 FFN L0 约为 140，而不是严格的 $0.08\times1536$。

**fast-SP。** 原始 SP 虽产生大量零激活，仍计算完整的 predictor、value 和 down projection。fast-SP 将 Attention 改为长度 256 的规则 causal window，并使用能够跳过空块的稀疏计算；FFN 则直接把物理宽度缩减为 128：

$$
h=\operatorname{GELU}(W_px)\odot W_vx,\qquad y=W_oh,\qquad h\in\mathbb{R}^{128}
$$

因此 fast-SP 的密度固定为 $128/1536=8.33\%$。它减少了实际计算，但同时牺牲了原始 SP 的完整参数容量和内容相关远距离路由，属于效率近似而非等价实现。

**实验对照。** 最终实验采用 $3\times2$ 设计：Base、SP 和 fast-SP 分别与 RoPE、CABLE 组合。模型包含 6 层、6 个注意力头，$d_{model}=384$、$d_{ff}=1536$、上下文长度为 1024；使用 OpenWebText 训练 60000 steps，并对模型随机种子 41、42、43 重复实验。SAE 在第 6 层 residual stream 上训练，扩展倍数为 8，Top-K 网格为 $8,16,32,64,128$。最终共完成 18 次模型训练和 90 次 SAE 训练；主结果在 SAE $k=64$ 下对三个模型随机种子取平均。

### 5.2 指标与计算口径

FFN 激活密度直接按非零隐藏单元比例计算：

$$
D_{FFN}=\frac{1}{NTd_{ff}}\sum_{n,t,j}\mathbf{1}[h_{ntj}\ne0]
$$

其中 $N$ 为评估样本数，$T$ 为每个样本的 token 位置数，$d_{ff}$ 为 FFN 中间维度，$h_{ntj}$ 为相应中间单元的激活值。

SAE normalized MSE 将重构误差除以 residual activation 的中心化能量：

$$
\operatorname{NMSE}=\frac{\mathbb{E}\lVert x-\hat{x}\rVert_2^2}{\mathbb{E}\lVert x-\mathbb{E}[x]\rVert_2^2}
$$

为判断几何重构能否保留模型功能，实验还将原 residual 替换为 SAE 重构值，并以全零 residual 为下界，计算语言模型损失恢复率：

$$
R_{loss}=\frac{L_{zero}-L_{recon}}{L_{zero}-L_{base}}
$$

其中 $L_{base}$、$L_{recon}$ 和 $L_{zero}$ 分别表示使用原始残差表示、SAE 重构表示和全零残差表示时的语言模型损失；越接近 1 表示重构保留的模型功能越多。

上下文一致性（Context coherence）对每个 SAE 特征选取最高激活样本，将其残差表示单位化，并计算它们与归一化质心的平均余弦相似度。该指标越高，表示同一特征的高激活上下文在残差空间中越集中。

概念 AUPRC 使用预设的低层 token 属性作为标签，包括数字、标点、首字母和代词等。对每个属性，先在训练集上选择区分正负样本最强的 SAE 特征，再使用前五个特征训练逻辑回归探针，并在独立测试集上计算 AUPRC，最后对符合样本量要求的属性取宏平均。该指标衡量少量 SAE 特征对固定概念词表的稀疏可读性；它不覆盖开放词汇语义，也不能单独代表整体可解释性。

困难负例取 SAE 编码器预激活大于零、但在 SAE Top-K 竞争后被抑制为零的样本。若正例一致性与被抑制候选的一致性之差小于 0.05，则记为一次边界分离失败：

$$
F_{HN}=\mathbf{1}[C_{positive}-C_{suppressed}<0.05]
$$

因果必要性通过移除概念相关 SAE 特征检验，充分性通过向负例注入正例的平均特征贡献检验；两者均减去激活频率匹配的随机特征对照。表中的因果隔离度为控制校正后的概念 log-odds 效应与全词表 KL 变化之比：

$$
I=\frac{\operatorname{mean}(\Delta\operatorname{logit}_{concept}-\Delta\operatorname{logit}_{random})}{\sqrt{\max(10^{-4},\operatorname{mean}(D_{KL}))}}
$$

该指标衡量目标效应相对于整体输出分布变化的大小；其中 KL 仍包含目标概念 token，因此不能解释为严格排除目标 token 后的纯副作用。

### 5.3 关键结果

下表汇总模型随机种子 41、42、43 在最终检查点上的评估结果。FFN 激活密度由模型激活直接统计；SAE 指标固定为第 6 层、8 倍扩展、$k=64$ 和 SAE seed 42。所有数值均为三个模型 seed 的均值 ± 样本标准差；其中 SAE 指标的波动只反映模型训练随机性，不包含 SAE seed 的变化。

| 模型 | 验证交叉熵 ↓ | FFN 激活密度 ↓ | SAE NMSE ↓ | 损失恢复率 ↑ | 训练时间（s）↓ |
|---|---:|---:|---:|---:|---:|
| Base-RoPE | 4.0046 ± 0.0112 | 1.0000 ± 0.0000 | 0.1485 ± 0.0009 | 0.9659 ± 0.0006 | 5771 ± 57 |
| SP-RoPE | 4.0570 ± 0.0085 | 0.0917 ± 0.0009 | 0.1220 ± 0.0009 | 0.9628 ± 0.0010 | 19161 ± 883 |
| fast-SP-RoPE | 4.1752 ± 0.0124 | 0.0833 ± 0.0000 | 0.1224 ± 0.0071 | 0.9723 ± 0.0014 | 5833 ± 49 |
| Base-CABLE | 3.9815 ± 0.0037 | 1.0000 ± 0.0000 | 0.1492 ± 0.0039 | 0.9635 ± 0.0011 | 11360 ± 156 |
| SP-CABLE | 3.9512 ± 0.0057 | 0.0910 ± 0.0006 | 0.1284 ± 0.0052 | 0.9651 ± 0.0015 | 21223 ± 3726 |
| fast-SP-CABLE | 4.1257 ± 0.0058 | 0.0833 ± 0.0000 | 0.0863 ± 0.0105 | 0.9678 ± 0.0013 | 8432 ± 65 |

Base-CABLE 与 Base-RoPE 的比较用于单独观察位置编码差异。Base-CABLE 的验证交叉熵低 0.0231（约 0.58%），但训练时间增加 96.8%，SAE NMSE 高 0.47%，损失恢复率低 0.24%。在语义和因果指标上，Base-CABLE 的点估计也没有显示优势：上下文一致性低 1.38%，困难负例分离失败率高 45.92%，概念 AUPRC 低 2.06%，必要性与充分性隔离度分别低 5.80% 和 2.66%。这组比较中，CABLE 的语言建模损失点估计更低，但 SAE、语义和因果指标没有同步提高。由于预先确定的检验针对 SP 与相同位置编码 Base 的配对差异，这里的 Base-CABLE—Base-RoPE 结果只作描述性比较。

![不同结构和 SAE 稀疏度下的 normalized MSE](figures/experiment2-sae-nmse-by-k.png)

*图 6　模型 seed 41–43、SAE seed 42、第 6 层和 8 倍扩展条件下，六种结构随 SAE Top-K 变化的归一化均方误差；横轴为实际 SAE $k$（L0），纵轴为 NMSE。*

该图显示各结构的重构误差均随 SAE $k$ 增大而下降，且 SP 与 fast-SP 的相对趋势同时出现在多个 $k$ 与模型 seed 上。正文表格预先固定使用 $k=64$，以避免依据结果曲线事后选择评价点。

原始 SP 在 RoPE 下的 SAE NMSE 相对 Base-RoPE 降低 17.89%，在 CABLE 下相对 Base-CABLE 降低 13.97%；这一重构优势在三个模型 seed 与五个 SAE $k$ 的配对中均保持同一方向。按相同位置编码比较，SP-CABLE 的验证交叉熵和 SAE NMSE 均低于 Base-CABLE，概念 AUPRC 与因果必要性隔离度的点估计也更高；但后两项的精确检验均为 $p=0.25$、FDR 校正后 $q=0.75$，不能据此宣称存在显著改善。SP-RoPE 没有呈现同样的任务性能与概念 AUPRC 方向。因此，Base-CABLE 与 Base-RoPE 用于比较位置编码，SP-CABLE 与 Base-CABLE 则用于估计 SP 在 CABLE 条件下的净变化，两类比较不能混用。

统计检验以模型 seed 为独立单位，并在结果汇总前固定以下口径。每个 SP 或 fast-SP 模型都与相同位置编码、相同模型 seed 的基线配对；主评价点固定为第 6 层、8 倍 SAE 扩展，并选择达到 95% 损失恢复率的最小 SAE $k$。最终主评价点包含模型 seed 41、42、43，但每个模型 seed 只有 SAE seed 42，因此检验不能估计 SAE 训练随机性。对每个指标的三个配对差值执行双侧精确符号翻转检验，并对全部 12 个可计算检验的原始 $p$ 值统一使用 Benjamini–Hochberg 方法控制 FDR，$\alpha=0.05$。表中区间为以模型 seed 为重采样单位的 95% 自助法区间；由于每个模型 seed 只有一个 SAE seed，该区间只反映模型训练随机性，不能反映 SAE seed 波动。

| 配对比较 | 预先确定的指标 | 平均配对差值（95% 自助法区间） | 原始 $p$ | BH-FDR $q$ | 拒绝零假设 |
|---|---|---:|---:|---:|---:|
| SP-RoPE − Base-RoPE | 达到 95% 损失恢复率的最小 SAE $k$ | 0.000 [0.000, 0.000] | 1.00 | 1.00 | 否 |
| SP-RoPE − Base-RoPE | Concept Macro-AUPRC | −0.0010 [−0.0115, 0.0075] | 1.00 | 1.00 | 否 |
| SP-RoPE − Base-RoPE | 因果必要性隔离度 | 0.1044 [−0.1478, 0.4911] | 1.00 | 1.00 | 否 |
| SP-CABLE − Base-CABLE | 达到 95% 损失恢复率的最小 SAE $k$ | 0.000 [0.000, 0.000] | 1.00 | 1.00 | 否 |
| SP-CABLE − Base-CABLE | Concept Macro-AUPRC | 0.0091 [0.0032, 0.0139] | 0.25 | 0.75 | 否 |
| SP-CABLE − Base-CABLE | 因果必要性隔离度 | 0.2545 [0.1762, 0.3262] | 0.25 | 0.75 | 否 |
| fast-SP-RoPE − Base-RoPE | 达到 95% 损失恢复率的最小 SAE $k$ | −10.667 [−32.000, 0.000] | 1.00 | 1.00 | 否 |
| fast-SP-RoPE − Base-RoPE | Concept Macro-AUPRC | −0.0038 [−0.0085, 0.0022] | 0.50 | 1.00 | 否 |
| fast-SP-RoPE − Base-RoPE | 因果必要性隔离度 | −0.2262 [−0.3696, −0.0797] | 0.25 | 0.75 | 否 |
| fast-SP-CABLE − Base-CABLE | 达到 95% 损失恢复率的最小 SAE $k$ | 0.000 [0.000, 0.000] | 1.00 | 1.00 | 否 |
| fast-SP-CABLE − Base-CABLE | Concept Macro-AUPRC | −0.0040 [−0.0116, 0.0036] | 0.50 | 1.00 | 否 |
| fast-SP-CABLE − Base-CABLE | 因果必要性隔离度 | −0.5745 [−0.7912, −0.3964] | 0.25 | 0.75 | 否 |

第四项预先确定的指标稳定性 PW-MCC 因缺少可配对的重复 SAE 训练而无法计算，未进入 FDR 校正。三个配对 seed 的双侧符号翻转检验只有 8 种符号组合，最小可达到的非零 $p$ 值为 0.25；因此，即使三个差值方向完全一致，也不能在 0.05 水平拒绝零假设。未拒绝零假设只表示当前独立重复不足以提供统计判别力，不能据此确认结构收益不存在。部分自助法区间没有跨越零而精确检验仍不显著，正是小样本重采样区间与离散随机化检验口径不同所致，本文据此只报告效应方向和不确定性，不作显著性结论。

fast-SP-CABLE 的 NMSE 最低，相对匹配的 Base-CABLE 降低 42.17%，但达到 95% 损失恢复率所需的最低 SAE $k$ 仍为 64。这说明更低的几何重构误差没有转化为同比例的 SAE 稀疏效率或语言模型忠实性改善。

解释性指标呈现明显分化。以下百分比均相对相同位置编码的 Base 模型计算，正值表示该指标点估计提高，负值表示降低：

| 模型 | 匹配基线 | 上下文一致性 ↑ | 困难负例分离 ↑ | 必要性隔离度 ↑ | 充分性隔离度 ↑ |
|---|---|---:|---:|---:|---:|
| SP-RoPE | Base-RoPE | +7.23% | −54.68% | +6.75% | −26.39% |
| SP-CABLE | Base-CABLE | +4.68% | +22.86% | +17.49% | −15.81% |
| fast-SP-RoPE | Base-RoPE | +6.39% | +10.87% | −16.68% | −17.58% |
| fast-SP-CABLE | Base-CABLE | +7.24% | +41.21% | −39.47% | −38.45% |

原始 SP 在两种位置编码下都提高了上下文一致性和必要性隔离度的点估计，但 RoPE 条件下的困难负例分离明显下降，而 CABLE 条件下则提高；两组的充分性隔离度均下降。fast-SP 在两种位置编码下提高了困难负例分离的点估计，却使必要性和充分性隔离度同时下降。由此可见，样本内部一致性、特征间边界和因果局部性是三个不同性质，不能由单一稀疏指标替代。这些百分比是三个 seed 的描述性汇总，不替代上文的精确检验结果。

### 5.4 时间成本与实现边界

按相同位置编码比较，SP-RoPE 的平均训练时间相对 Base-RoPE 增加 232.0%，SP-CABLE 相对 Base-CABLE 增加 86.8%。虽然统计阈值避免了精确 Top-K 的逐行排序，但原始 SP 仍计算完整的 Attention predictor/value scores，以及完整的 FFN value/down 矩阵乘法；产生零值并不会使稠密矩阵乘法自动跳过这些位置。因此，它实现的是结构与行为稀疏，而不是实际加速。

fast-SP 将 SP-RoPE 的平均训练时间从 19161 s 降至 5833 s，已接近 Base-RoPE 的 5771 s；fast-SP-CABLE 的 8432 s 则比 Base-CABLE 的 11360 s 低 25.8%。不过，fast-SP 同时缩小 FFN 物理宽度并将 Attention 改为固定窗口，因此这些时间差不能说明原始 SP 只靠工程优化即可达到同样速度。独立的 Transformer 模块前向/反向基准还显示，fast-SP-RoPE 在长度 1024 和 2048 下分别比 Base-RoPE 慢 30.59% 和 86.98%。RoPE 基线能够使用高度优化的融合注意力算子，使稀疏结构只有在计算足够规则且上下文足够长时才可能抵消调度开销。

固定阈值难以跨层、输入和训练阶段维持稳定激活率；统计阈值解决了阈值尺度问题，却仍需要计算全量均值、方差和稠密投影。后续更可行的方向是可学习阈值、低成本 block predictor，以及在选择之后真正跳过未激活单元的硬件友好稀疏内核 <sup>[39]</sup><sup>[41]</sup> (You et al., 2025; Madhyastha et al., 2026)。

### 5.5 结论与研究启示

在本实验的三个模型 seed 与固定 SAE 设置下，SP 一致降低了固定 L0 下的 SAE 重构误差，但其他指标没有同步显示整体可解释性改善。相对 Base-CABLE，SP-CABLE 的任务、SAE 重构、上下文一致性、困难负例分离和必要性隔离度均呈现更好的点估计，但充分性隔离度下降，且训练时间增加 86.8%；概念 AUPRC 与必要性隔离度的精确检验也未达到显著水平。fast-SP 大幅降低了原始 SP 的计算成本，但其可压缩性改善同时伴随语言模型性能与因果局部性损失。

因此，FFN 激活密度、SAE 重构忠实性、语义单一性和因果局部性需要分别评价。更低的 NMSE 可能仅反映表征能量集中、有效维度下降或模型容量受限；只有在语言模型性能、语义边界和干预副作用同时受控时，方可将其解释为可解释性的实质提升。

## 6. 实验 3：面向远程相关性的 RA-CABLE

### 6.1 研究动机与假设

**研究动机。** 实验一显示，ALiBi 与 CABLE 在固定训练预算下具有更低的验证损失轨迹和最终困惑度，但二者均保留随距离单调增加的惩罚。ALiBi 为不同注意力头预先指定固定斜率，使各头承担近似固定的局部—全局距离尺度。CABLE 将固定斜率改为由上下文决定的累计距离，但仍要求路径增量非负。因此，远端且与当前 query 高度相关的 key 仍需克服较大的位置惩罚。

需要强调的是，单调的是位置偏置，而不是最终 Attention。内容分数仍可能使远端 token 获得较高权重；“固定跨头比例”和“单调偏置限制远程利用”都只是待检验假设，不能由公式直接判定。

**假设。** 本实验提出四项假设：

1. ALiBi 的 head attention distance share 在不同长度和内容下比 RoPE、CABLE 与 RA-CABLE 更稳定，并更接近固定几何分布。
2. 当远端相关 token 与近端干扰 token 同时存在时，严格单调的距离偏置可能降低远端相关 token 的注意力优势和检索准确率。
3. 受约束的相关性豁免可以使 RA-CABLE 改善远程信息利用，同时保持短上下文语言模型质量，并控制错误豁免和注意力汇聚点现象。
4. 非单调性本身不是成功标准；只有语义相关性触发的豁免同时带来任务收益，才能支持该机制。

![实验三的 RA-CABLE 相关性豁免流程](figures/experiment3-ra-cable-flow.svg)

*图 7　实验三针对 Attention 内部的位置偏置。RA-CABLE 使用 query-key 内容相关性生成豁免门，在不提供正位置奖励的前提下减弱 CABLE 距离惩罚，并通过远端检索、语义豁免、错误豁免、长上下文性能和成本共同判断其有效性。*

### 6.2 方法与核心公式

ALiBi 对第 $h$ 个注意力头使用预先确定的斜率 $m_h$：

$$
b_{ij}^{h,\mathrm{ALiBi}}=-m_h(i-j),\qquad j\le i
$$

其中 $i$ 和 $j$ 分别为 query 与 key 的位置，$b_{ij}^h$ 为加入内容分数的逐头位置偏置。

CABLE 首先从每个 token 的 hidden state 学习非负路径增量，并形成累计坐标：

$$
c_t^h=\operatorname{ReLU}(w_c^{h\top}x_t),\qquad
d_{ij}^h=\sum_{t=j+1}^{i}c_t^h
$$

再由 query 位置生成正尺度，将累计距离变为位置偏置：

$$
g_i^h=\operatorname{Softplus}(w_g^{h\top}x_i),\qquad
b_{ij}^{h,\mathrm{CABLE}}=-g_i^hd_{ij}^h
$$

其中 $x_t$ 为第 $t$ 个 token 的隐藏表示，$c_t^h$ 为非负路径增量，$d_{ij}^h$ 为累计路径距离，$g_i^h$ 为由 query 表示决定的正尺度。

由于 $c_t^h\ge0$ 且 $g_i^h>0$，对同一个 query 而言，更远 key 的 CABLE 基础偏置不会优于更近 key。RA-CABLE 在这一稳定的局部先验上加入由实际 query-key 内容分数控制的豁免门。首先计算标准内容 logit：

$$
\ell_{ij}^h=\frac{q_i^h{k_j^h}^{\top}}{\sqrt{d_h}}
$$

然后为每个注意力头学习门控尺度与偏置：

$$
r_{ij}^h=\sigma\left(\operatorname{Softplus}(s_h)\ell_{ij}^h+a_h\right)
$$

最终位置偏置为：

$$
b_{ij}^{h,\mathrm{RA}}=b_{ij}^{h,\mathrm{CABLE}}(1-r_{ij}^h)
$$

其中 $q_i^h$、$k_j^h$ 和 $d_h$ 分别为第 $h$ 个注意力头的 query、key 与头维度，$r_{ij}^h\in(0,1)$ 为相关性豁免门。

当 $r_{ij}^h$ 接近 0 时，模型保留原有 CABLE 惩罚；当其接近 1 时，该 query-key 对的位置惩罚趋近于 0，但不会由位置模块直接获得正奖励。实验将 gate bias 初始化为 $-3$，使模型从接近 CABLE 的状态开始，并加入平均豁免率正则：

$$
L=L_{LM}+\lambda\,\mathbb{E}_{i,j,h}[r_{ij}^h],\qquad \lambda=10^{-3}
$$

这一正则用于抑制“所有位置均被豁免”的平凡解。RA-CABLE 的主要风险是放大原始 $QK^\top$ 偏差、错误豁免高频 token，以及增加完整 Attention 矩阵上的显存和逐元素计算开销。

### 6.3 实验设计

主实验比较 RoPE、ALiBi、CABLE 和 RA-CABLE。四种方法共享相同的 token 序列、GPT-2 tokenizer、模型主体初始化、训练 token 数和优化器；CABLE 与 RA-CABLE 的基础投影也采用配对初始化，避免把随机初值差异误认为门控收益。RoPE 额外报告原生外推与 Position Interpolation 推理结果 <sup>[42]</sup> (Chen et al., 2023)。

模型采用 GPT-2 Small 级别结构：12 层、12 个注意力头、$d_{model}=768$、$d_{ff}=3072$。训练语料为 FineWeb-Edu，训练长度为 1024，每个模型预算为 10 亿 token；实验设计规定使用随机种子 42–49，共形成 $4\times8=32$ 个独立训练任务。预训练后再进行 500 steps 的合成长距离检索适配，以区分原生长度外推能力和经过检索任务适配后的能力。

评估长度为 1024、2048、4096 和 8192。自然语言质量使用 held-out FineWeb-Edu 与 WikiText-103；真实长文档问答使用 QASPER validation <sup>[43]</sup> (Dasigi et al., 2021)；合成控制任务同时放置远端目标、近端相似干扰项和多查询条件，以直接检验模型是否能够利用远端相关内容。Attention 审计覆盖自然文本、打乱文本和合成远端目标三种条件，并固定检查第 0、5、11 层。

### 6.4 指标与成功标准

对注意力权重 $A_{bhts}$，平均注意力距离只在序列末尾 25% 的 query 上计算，其中 $b,h,t,s$ 分别表示样本、注意力头、query 位置和 key 位置：

$$
\operatorname{MAD}_h=\mathbb{E}_{b,t}\left[\sum_s A_{bhts}(t-s)\right]
$$

为检验不同注意力头的距离比例是否随上下文长度改变，先把每个长度下的 MAD 归一化为 head share：

$$
p_h^{(L)}=\frac{\operatorname{MAD}_h^{(L)}}{\sum_{h'}\operatorname{MAD}_{h'}^{(L)}}
$$

相对头距离漂移（Relative Head Distance Drift, RHDD）取各头距离占比跨长度变异系数的平均：

$$
\operatorname{RHDD}=\frac{1}{H}\sum_h\frac{\operatorname{Std}_L(p_h^{(L)})}{\operatorname{Mean}_L(p_h^{(L)})}
$$

值越高表示头间距离比例越会随长度调整。12 头 ALiBi 的标准斜率并不按原始 head 编号构成一条连续等比序列，因此几何头拟合度（Geometric Head Fit, GHF）不再直接拟合 head 编号，而是在每个 head 对应的 ALiBi 参考斜率 $m_h$ 与平均注意力距离之间拟合：

$$
\log(\operatorname{MAD}_h+\epsilon)=a+b\log(m_h),
$$

并报告 $R^2$。四种方法均使用同一组 12 头 ALiBi 斜率作为参考。高值只表示各头距离更接近 ALiBi 的固定尺度分工，不表示模型质量或可解释性更高。

上下文自适应分数（Context Adaptivity Score, CAS）将每个样本的注意力头距离占比与其平均分布比较，并取 Jensen–Shannon 散度的均值。高 CAS 表示跨头距离分工更依赖输入内容。

非单调率使用加到 Attention logit 上的位置偏置 $B_{bhts}$ 计算。对序列末尾 25% 的 query 和全部相邻 causal key，若更远的 key 获得比相邻近端 key 更高的位置偏置，则记为一次违反：

$$
V_{adj}=\mathbb{E}_{b,h,t,s}\mathbf{1}\left[B_{b,h,t,s-1}>B_{b,h,t,s}+10^{-7}\right].
$$

其中 $s-1$ 比 $s$ 离 query 更远。合成远端任务还报告配对远近违反率，只比较同一最终 query 下预先标注的远端 key 与近端 key，即 $\Pr(B_{far}>B_{near}+10^{-7})$。前者描述整体偏置是否非单调，后者检查非单调性是否出现在实验关心的远近位置对上；二者都不是越高越好。

在合成远端目标条件下，相关注意力优势（Relevant Attention Advantage, RAA）直接比较最终 query 对远端相关 key 和近端干扰 key 的注意力：

$$
\operatorname{RAA}=\mathbb{E}[A_{relevant}-A_{distractor}]
$$

语义豁免成功率（Semantic Exemption Success Rate, SESR）统计远端相关 key 的位置偏置是否高于配对近端干扰 key：

$$
\operatorname{SESR}=\Pr(b_{relevant}>b_{distractor})
$$

错误豁免率（False Exemption Rate, FER）则统计无关位置的 RA-CABLE 门值超过 0.5 的比例。SESR 高而 FER 同时可控，才能说明门控学习到语义豁免，而不是普遍削弱距离惩罚。

注意力汇聚点比例检查模型是否把大部分注意力集中到少数特殊 token。设 $G$ 为预先指定的特殊 token 集合，$Q_{last}$ 为序列末尾 25% 的 query，则：

$$
R_{sink}(G)=\frac{1}{BH|Q_{last}|}\sum_{b,h,t\in Q_{last}}\mathbf{1}\left[\sum_{s\in G}A_{bhts}>0.5\right].
$$

自然文本分别统计 EOS、换行及其合集，合成任务分别统计 BOS、分隔符、query token 及其合集。该比例升高表示更多 query–head 将超过一半的注意力分配给这些特殊 token；它需要与检索和语言模型结果共同解释。

任务指标包括各长度的困惑度、合成检索准确率、按目标距离分组的准确率、长度退化率，以及 QASPER token F1、精确匹配率和证据利用增益。证据利用增益通过移除标注证据前后的答案负对数似然差计算：

$$
G_{evidence}=L_{removed}-L_{full}
$$

其中 $L_{removed}$ 和 $L_{full}$ 分别为移除标注证据与保留完整证据时的答案负对数似然。正值越大，说明模型越依赖文档中的真实证据。效率指标包括单步训练时间、总 GPU 小时、预填充/解码延迟、吞吐、峰值显存、峰值主机内存和新增参数量。

RA-CABLE 只有同时满足以下条件，才能支持核心假设：远程检索优于 CABLE；长度 1024 的困惑度相对 CABLE 的恶化不超过 1%；RAA 与 SESR 提高而 FER 和注意力汇聚点比例不明显恶化；距离模式的变化与任务收益相关；时间和显存开销保持在可接受范围。只有更高的非单调率、更大的平均注意力距离或更低的单一长度困惑度，均不能单独视为成功。

### 6.5 结果与结论

截至本文撰写时，本实验的全部训练、长上下文评测、Attention 审计和跨随机种子统计尚未完成，因此本节不报告方法优劣。后续结果将依据上述预先确定的指标与成功标准进行汇总。

## 7. 实验 4：双轴概念 Attention 与显式概念接口

### 7.1 研究动机与假设

本实验研究能否在模型内部形成具有固定身份、可读取且可干预的双轴概念接口，重点考察上下文聚合所处的表示空间对概念身份与因果局部性的影响。

在传统 Transformer 中，token Attention 首先在 $d_{model}$ 维 residual stream 上聚合其他 token 的信息，FFN 随后才把已经上下文化的表示升维。该 residual stream 持续累积多层 Attention 与 FFN 的输出，因此不同特征共享同一低维通信空间。FFN 的升维与非线性变换在 $d_{ff}$ 空间产生更丰富的特征基，为概念特征的分离和选择提供候选表示空间。然而，标准结构要求这些中间特征先经降维投影写回 residual stream，随后才能在下一层参与跨 token 通信；这一瓶颈可能削弱中间特征的身份稳定性。

据此，本文考察从完整 FFN 中间激活投影出的两个低维概念槽，在降维写回前参与跨 token 通信时，能否直接完成上下文组合。高层属性通常由上下文关系确定，而非 token 的固定标签。例如，COUNTRY 轴上的 France 属性取决于实体与地点、关系和修饰语之间的组合；COLOR 轴上的 Red 属性则取决于颜色词与具体实体的上下文绑定。因此，Concept Bus V2 同时使用 FFN 中间激活提供特征内容，并沿 token 轴更新概念槽；Concept Projector 则保留相同投影与读出，只移除跨 token 通信。

在完整 $d_{ff}$ 空间执行第二次 Attention 会引入较高的序列二次计算与显存成本，并可能重新形成密集特征混合。另一种设计是将中间维度划分为内容保留、向量内组合和向量外通信等功能分支；但固定坐标划分本身无法约束模型按照预设角色使用各分支，并可能造成语义在多个分支间再次分散。基于这一设计约束，实验采用低维概念槽作为一种低成本的受控概念接口，用于检验降维写回前的跨 token 通信能否带来额外收益。

具体而言，模型从完整 FFN 激活中投影出 COUNTRY 与 COLOR 两个低维固定身份槽；普通 FFN 更新写入 residual 的私有子空间，概念概率写入独占的概念子空间；Concept Bus V2 进一步允许两个槽分别执行跨 token causal Attention。该结构将概念投影与跨 token 通信分解为两个可独立比较的因素，从而估计额外通信相对于简单概念投影的净贡献。

实验四检验三个递进假设。首先，从完整 FFN 中间激活投影出的固定槽能够形成可读取、可置零和可交换的 COUNTRY/COLOR 概念接口。其次，在参数量与投影深度匹配的条件下，跨 token 概念 Attention 应优于仅处理当前 token 的 Concept Projector，从而支持概念槽受益于降维写回前上下文通信的判断。最后，结构收益应同时体现于任务性能、概念质量与因果局部性，而非仅体现于探针、置零幅度或单项交换响应。

![实验四的 FFN 概念接口与双轴通信流程](figures/experiment4-concept-flow.svg)

*图 8　实验四针对 FFN 升维激活与降维写回之间的概念通路。完整 FFN 激活被投影为固定身份的 COUNTRY/COLOR 槽；Projector 仅进行 self-only 处理，V2 增加跨 token causal Attention，其余参数和训练目标保持匹配。*

### 7.2 实现结构与公平对照

本节仅报告最终受控复验结果。Standard、Concept Projector 与 Concept Bus V2 在 seed 11、22、33 下共完成 9 次运行，并采用一致的模型实现与实验配置；此前的探索性运行不进入定量汇总或结论。

在最后一个 Transformer block 中，标准 token Attention 先形成上下文化表示，随后计算完整 FFN 中间激活：

$$
H^{ctx}=H+\operatorname{CausalAttn}_{token}(\operatorname{RMSNorm}(H))
$$

$$
U=\operatorname{GELU}\left(W_{up}\operatorname{RMSNorm}(H^{ctx})\right),\qquad
S=\operatorname{reshape}(W_PU)\in\mathbb{R}^{T\times 2\times d_s}
$$

其中两个 $d_s=64$ 的槽分别固定为 COUNTRY 和 COLOR。Concept Bus V2 对每个槽独立执行严格 causal Attention：

$$
A^{V2}_{t,k}=\operatorname{Attn}(S_{t,k},S_{1:t,k},S_{1:t,k})
$$

Concept Projector 是最重要的公平对照。它保留与 V2 完全相同的 FFN 投影、槽位、QKV/O、概念读出和写回参数，只把跨 token 通信改为 self-only：

$$
A^{Proj}_{t,k}=\operatorname{Attn}(S_{t,k},S_{t,k},S_{t,k})
$$

因此，$M(V2)-M(Projector)$ 才能表示额外跨 token 概念通信的净贡献，而不是参数量或投影深度的差异，后续分析也以这一组比较为主。实现中 self-only Attention 仍注册完整 QKV/O 参数，但前向只计算数学上有效的 V/O 路径。

Standard 只作为常规 Transformer 的描述性基线。它与 Projector/V2 不仅在概念投影和通信结构上不同，后两者还加入了辅助概念监督、反事实损失，以及私有子空间与概念子空间的分离写回。因此，涉及 Standard 的比较只反映完整方案的总体差异，不能单独归因于概念投影或第二套 Attention。

两个槽分别输出六个 sigmoid 概念节点：COUNTRY 存在节点及其 France、United Kingdom、China、Japan、Unknown 类别节点；COLOR 存在节点及其 Red、Blue、Yellow、Green、Unknown 类别节点。存在节点与类别节点使用两级监督标签，但均由对应槽并行读出，当前结构不包含从子类别显式聚合至父概念的运算。因此，本实验检验的是具有两级标签监督的固定身份接口，而不是低层概念自发组合形成高层概念。概念概率通过可学习编码写入残差流的最后 32 维，普通 FFN 只写入前 480 维：

$$
p=\sigma(W_CA),\qquad
\Delta H=\operatorname{concat}\left(W_{private}U,\ pC\right)
$$

其中 $A$ 为对应槽的通信输出，$p$ 为 12 个概念节点的预测概率，$C$ 为可学习概念编码，$W_{private}U$ 为写入非概念子空间的 FFN 更新。

这一独占写回用于减少普通 FFN 对概念通道的直接旁路。训练目标包含 COUNTRY/COLOR 分类损失、12 个概念节点的二元交叉熵、COUNTRY/COLOR 交换后的反事实损失，以及两个投影基底的正交约束。前三项目标先按各自指数移动平均尺度归一化，再用平滑最大值聚合；检查点只依据未经辅助目标混合的验证任务损失选择。

### 7.3 实验设计与指标口径

受控复验只比较 Standard、Concept Projector 与 Concept Bus V2。三种方法使用同一份确定性 DualTag 数据与相同计算预算，并分别以模型 seed 11、22、33 训练。数据生成 seed 固定为 20260806，包含 200,000 个训练样本、20,000 个验证样本和 20,000 个测试样本，序列最长 256；20% 属性来自 Unknown 类，验证与测试模板和 Unknown 实体均与训练集分离。模型为 12 层、$d_{model}=512$、$d_{ff}=2048$、8 个主注意力头，训练 6,000 步，微批量为 64、梯度累积为 2、有效批量大小为 128；每 500 步在完整验证集上选择最佳检查点。每次运行的最终因果审计使用 2,000 个测试样本。

主任务要求同时预测 COUNTRY 与 COLOR。联合准确率（Exact Accuracy）只有两个标签都正确时才记为成功：

$$
\operatorname{Exact}=\frac{1}{N}\sum_{i=1}^{N}
\mathbf{1}[\hat y_i^{country}=y_i^{country}\land \hat y_i^{color}=y_i^{color}]
$$

其中 $N$ 为评估样本数，$y_i$ 和 $\hat y_i$ 分别为真实标签与预测标签。

概念质量使用 0.5 阈值将 12 个 sigmoid 节点二值化，分别计算每个节点的 F1 后取宏平均：

$$
F1_{macro}=\frac{1}{12}\sum_{j=1}^{12}
\frac{2TP_j}{2TP_j+FP_j+FN_j}
$$

其中 $TP_j$、$FP_j$ 和 $FN_j$ 分别为第 $j$ 个概念节点的真正例、假正例和假负例数。

概念通道必要性通过置零前后的性能差计算。例如总线必要性为：

$$
\Delta_{zero}=\operatorname{Exact}_{base}-\operatorname{Exact}_{zero\text{-}bus}
$$

其中 $\operatorname{Exact}_{base}$ 与 $\operatorname{Exact}_{zero\text{-}bus}$ 分别为正常推理和概念总线置零时的联合准确率；差值越大表示该通道对任务越必要。

定向干预在严格配对子集中交换 France/United Kingdom 或 Red/Blue 两个概念概率。交换成功率衡量目标轴是否变为配对类别；副作用率则统计非目标轴的预测是否发生变化。前者高、后者低，才表示干预既有效又局部。槽位余弦只用于检查 COUNTRY 与 COLOR 表示是否塌缩到相同方向，不能单独代表语义正确性。工程指标直接记录参数量、估算 MAC、训练吞吐、总训练时间和峰值 CUDA 显存。

统计汇总以独立训练的模型 seed 为单位（$n=3$），表中报告均值与跨 seed 样本标准差。V2 与 Projector 的差异在相同 seed 内配对，使用双侧精确符号翻转随机化检验，并对本节报告的全部成对检验作 Holm 校正；差值的 95% 区间使用 $t$ 区间。由于三个 seed 时双侧精确检验可达到的最小 $p$ 值为 0.25，统计结果主要用于量化方向一致性与不确定性，而不能提供高功效的显著性判断。

### 7.4 关键结果

![Projector 与 Concept Bus V2 的受控比较](figures/experiment4-comparison.svg)

*图 9　参数匹配的 Concept Projector 与 Concept Bus V2 在各自最佳验证检查点上的测试结果；柱高为 seed 11、22、33 的均值，误差条为跨 seed 样本标准差，每个 seed 使用 20,000 个测试样本。*

| 方法 | Exact ↑ | COUNTRY ↑ | COLOR ↑ | 概念 Macro-F1 ↑ | 最佳检查点（seed 11/22/33） |
|---|---:|---:|---:|---:|---:|
| Standard | 68.525% ± 2.083% | 80.188% ± 2.587% | 86.173% ± 4.245% | — | 500/500/500 |
| Concept Projector | **79.720% ± 4.652%** | **90.378% ± 5.016%** | 87.793% ± 0.657% | **83.916% ± 4.648%** | 2,000/3,500/4,500 |
| Concept Bus V2 | 71.623% ± 8.536% | 81.505% ± 8.811% | **88.132% ± 0.412%** | 77.905% ± 3.143% | 2,000/1,500/6,000 |

V2 相对参数与训练目标匹配的 Projector，在三个 seed 上均降低 Exact、COUNTRY 与 Concept Macro-F1；平均差分别为 −8.097 pp、−8.873 pp 和 −6.011 pp。COLOR 平均提高 0.338 pp，但幅度小于两种方法的跨 seed 标准差。作为完整方案的描述性比较，Projector 和 V2 相对 Standard 的 Exact 平均分别提高 11.195 pp 和 3.098 pp；由于 Standard 同时缺少辅助概念监督、反事实损失和分离写回，不能将这些差值归因于概念投影或跨 token 概念通信。

| V2 − Projector | 平均差 | 差值 95% CI | 原始 $p$ | Holm $p$ |
|---|---:|---:|---:|---:|
| Exact | −8.097 pp | [−17.755, 1.562] pp | 0.25 | 1.00 |
| COUNTRY | −8.873 pp | [−18.305, 0.559] pp | 0.25 | 1.00 |
| COLOR | +0.338 pp | [−0.647, 1.324] pp | 0.50 | 1.00 |
| 概念 Macro-F1 | −6.011 pp | [−10.586, −1.435] pp | 0.25 | 1.00 |

四项差异经 Holm 校正后均未达到显著水平。Concept Macro-F1 的 $t$ 区间未跨越 0，但精确随机化检验仍为 $p=0.25$；在仅三个配对 seed 的条件下，不据此作显著性结论。

| 因果审计指标 | Projector（均值 ± SD） | V2（均值 ± SD） | V2 − Projector |
|---|---:|---:|---:|
| 总线置零 Exact 下降 | 12.900 ± 6.396 pp | 9.317 ± 5.490 pp | −3.583 pp |
| COUNTRY 置零后 COUNTRY 下降 | 8.417 ± 3.893 pp | 5.000 ± 2.737 pp | −3.417 pp |
| COLOR 置零后 COLOR 下降 | 8.083 ± 2.026 pp | 6.567 ± 5.325 pp | −1.517 pp |
| COUNTRY 交换成功率 | 98.721% ± 2.216% | 87.340% ± 14.771% | −11.380 pp |
| COLOR 交换成功率 | 100.000% ± 0.000% | 100.000% ± 0.000% | 0 pp |
| COUNTRY 交换对 COLOR 的副作用 | 0.333% ± 0.189% | 0.617% ± 0.562% | +0.283 pp |
| COLOR 交换对 COUNTRY 的副作用 | 0.000% ± 0.000% | 0.367% ± 0.333% | +0.367 pp |
| COUNTRY/COLOR 槽位余弦 | 0.0338 ± 0.0623 | 0.0167 ± 0.0503 | −0.0171 |

| COUNTRY 交换成功率 | COUNTRY 交换对 COLOR 的副作用 |
|---|---|
| ![COUNTRY counterfactual success](figures/experiment4-country-counterfactual-success.svg) | ![COUNTRY counterfactual color side effect](figures/experiment4-country-counterfactual-side-effect.svg) |

*图 10　seed 11、22、33 的最佳验证检查点在每个 seed 的 2,000 个因果审计样本上的 COUNTRY 定向交换成功率与非目标 COLOR 副作用；柱为均值，误差条为跨 seed 样本标准差，点为逐 seed 原始结果。交换成功率越高、非目标副作用越低越好。*

V2 的槽位余弦更低，但其总线及两个目标轴的平均置零效应均弱于 Projector，COUNTRY 交换成功率更低，两类交换的非目标副作用也更高。除 COLOR 交换成功率均为 100% 外，因果指标的跨 seed 方差较大；相关配对检验经 Holm 校正后均不显著。因此，这些结果只表明当前三个 seed 的总体方向不利于 V2，不能把单项代理指标解释为整体可解释性收益。

### 7.5 时间与资源成本

| 方法 | 参数量 ↓ | MAC/样本 ↓ | 训练吞吐（样本/s） ↑ | 训练时间 ↓ | 峰值 CUDA 显存 ↓ |
|---|---:|---:|---:|---:|---:|
| Standard | 37,813,760 | 10.469B | **3,999.08 ± 53.39** | **192.07 ± 2.57 s** | **1.461 ± 0.003 GB** |
| Concept Projector | 38,028,044 | 10.524B | 3,354.54 ± 100.61 | 229.08 ± 6.97 s | 1.478 ± 0.004 GB |
| Concept Bus V2 | 38,028,044 | 10.545B | 3,324.56 ± 71.57 | 231.08 ± 5.00 s | 1.478 ± 0.001 GB |

Projector 与 V2 参数量完全相同；V2 仅因跨 token Attention 多出 0.199% MAC。三 seed 平均下，V2 相对 Projector 吞吐降低 29.98 样本/s、训练时间增加 2.00 秒；两项配对检验的原始 $p$ 值均为 1.00，未显示稳定的计时差异。作为完整方案的成本参考，V2 相对 Standard 平均吞吐下降 16.87%，训练时间增加 20.31%，峰值显存增加约 1.19%；该差异包含概念监督、反事实目标和分离写回等全部新增设计的成本。

### 7.6 结论与研究启示

三个配对 seed 的点估计均不支持“第二套跨 token concept Attention 能改善显式概念接口”的假设。Concept Projector 已能形成可读取、可置零和可交换的概念通道；在参数、投影和训练目标相同的条件下，额外通信在三个 seed 上均降低任务 Exact、COUNTRY 准确率和概念 Macro-F1。该受控比较将退化方向定位到当前配置中的跨 token concept Attention，但不构成对 FFN 概念投影方法的普遍否定。

可读性、必要性、可控性、准确率和成本应分别检验；较低槽位余弦不能抵消任务与副作用指标的整体退化。相较于单一 seed 的初步结果，多 seed 复验能够更可靠地描述差异方向，但 $n=3$ 的检验功效仍然有限，Holm 校正后没有差异达到显著水平。因此，较稳妥的结论是：V2 在当前配置与三个初始化下没有显示相对 Projector 的优势；该结论仍不能直接外推至其他数据、模型规模或概念结构。后续研究应以 Projector 为强基线，优先检验更简洁的 FFN 固定概念接口。

## 8. 综合讨论

### 8.1 三个已完成实验的共同结论

三个已完成实验从不同方向重复得到同一结论：可解释性的代理指标不能互相替代。

| 研究问题 | 最关键证据 | 当前结论 | 证据边界 |
|---|---|---|---|
| 内容与位置的解耦能否改善 SAE？ | PoPE 的内容—位置得分相关性为 0.0773，低于 RoPE 的 0.1035；但 PPL 为 75.90，SAE EV 为 0.7086，均劣于 RoPE | 更明显的内容与位置解耦没有转化为更好的可分解性 | 未直接追踪后续层中的重新交融机制 |
| 结构稀疏能否促进语义单一？ | SP-RoPE 的 SAE NMSE 降低 17.89%，训练时间增加 232.0%，困难负例与充分性指标恶化 | 稀疏改善可压缩性，但不保证语义边界和因果局部性 | 三个模型 seed；尚无专用稀疏计算核 |
| 远程相关性豁免能否改善 CABLE？ | 训练与长上下文评估尚未全部完成 | 暂不形成判断 | 评价指标与成功标准已经预先确定 |
| 第二套概念 Attention 是否优于简单投影？ | V2 相对 Projector：Exact −8.097 pp、Concept Macro-F1 −6.011 pp、MAC +0.199%；三 seed 同方向 | 当前配置下没有观察到跨 token 概念通信的净收益 | 三个模型 seed；精确检验功效有限，结论不外推到其他规模 |

实验二进一步显示，概念可读性与因果作用的点估计并不同步。相对 Base-RoPE，SP-RoPE 的 Concept Macro-AUPRC 平均差为 −0.0010，而因果必要性隔离度为 +0.1044；相对 Base-CABLE，SP-CABLE 的 AUPRC 和必要性隔离度分别提高 0.0091 和 0.2545，但充分性隔离度下降 15.81%。针对 AUPRC 与必要性隔离度的预先确定检验均未达到显著水平，充分性差异只是三个 seed 的描述性汇总，因此不能据此宣称结构收益；这些点估计只说明概念可读性、必要性和充分性回答的是不同问题，评价时不能互相替代。

跨实验来看，内容与位置的解耦、结构稀疏、SAE 重构、概念读出、因果干预和运行成本都只能反映可解释性的一个侧面。单项指标改善后，仍需检查任务性能、语义边界、干预副作用和实际计算成本是否同步变化。

### 8.2 对研究目标的修正

初始研究目标侧重于设计内生可解释结构。现有实验将该目标进一步限定为：识别能够同时满足任务性能、语义身份、因果局部性、解释忠实性和工程成本要求的最小接口。候选结构只有在简单投影、参数匹配和计算匹配对照下显示独立收益，才具备扩大实验规模的依据。

### 8.3 负结果的研究价值

现有实验尚未得到优于全部对照的最终结构，但排除了几种过于直接的判断方式：更明显的内容与位置解耦、更低的 SAE 重构误差、更高的概念读出分数或更大的干预响应，都不足以单独证明整体可解释性得到改善。受控负结果的价值在于缩小后续设计范围：新增结构应先与参数和训练目标匹配的简单基线比较，并分别验证任务、语义、因果和成本收益，再决定是否扩大实验规模。

## 9. 局限性和未来计划

### 9.1 当前局限性

本文尚未得到能够同时改善任务性能、语义质量、因果局部性和工程成本的最终结构。现有贡献主要是受控实验结果、方法适用边界及由此形成的设计经验，而不是已经可以推广使用的新模型。除此之外，研究还存在以下限制：

1. 多数实验使用小型模型，不能直接外推到大规模预训练模型。
2. 各实验目前最多包含三个模型 seed；虽然已报告跨 seed 方差，但统计功效仍然有限。
3. SAE 和概念解释主要依赖自动 proxy，尚缺少完整开放词汇解释与盲化人工评估。
4. DualTag 等合成任务有利于精确因果审计，但可能高估人工概念在真实文本中的清晰程度。
5. 稀疏实验没有实现真正的稀疏 Attention 和硬件内核，因此不能评价实际加速上限。
6. 不同实验批次使用的 GPU 型号并不完全一致，且实验 2 未保留可核验的具体 GPU 型号。因此，训练时间、吞吐和显存等效率指标只在同一实验波次及相同运行协议下比较，不用于跨实验评价结构优劣；不同硬件、计算核和非确定性操作也可能引入轻微数值波动。
7. 截至本文撰写时，实验 3（RA-CABLE）尚未完成全部评估；相关章节仅报告研究设计，不据此形成方法有效性的结论。

### 9.2 未来计划

基于现有证据，后续工作按以下顺序推进：

- 完成实验 3 的全部训练、长上下文评测、Attention 审计和跨 seed 统计，并同步更新摘要、综合讨论与结论；
- 在真实文本任务和更大模型上复验 Standard、Concept Projector 与 Concept Bus V2，并增加模型 seed 数量；
- 研究 Projector 的概念泄漏、身份稳定性和因果边界，并将新增总线复杂度作为独立变量进行验证；
- 在结构稀疏中加入特征去重、边界对比和因果局部性目标，并分别验证其收益；
- 研究低成本激活预测与可学习阈值，并结合能够真正跳过未激活单元的硬件友好稀疏内核，检验实际时间收益；
- 探索 ALiBi/CABLE 式高效位置偏置与上下文自适应注意力模式的结合。

## 10. 结论

本文考察了位置编码、结构稀疏和显式概念接口三条内生可解释性路线。现有结果共同表明，结构干预能够改变内容—位置解耦、激活稀疏度、SAE 重构或概念接口，但这些局部变化没有稳定转化为任务、语义、因果和成本的同步改善。特别是在参数与训练目标匹配的比较中，第二套跨 token 概念 Attention 没有显示出相对简单 Projector 的额外收益。

综合上述实验，本文采用并整理了一套同时覆盖任务、语义、因果、忠实性与工程成本的多维评价方法。结论适用于本文的小型模型、训练预算、数据与指标口径；三个 seed 的复验仍不足以支持对其他数据、初始化分布或模型规模的普遍外推。实验三完成前，本文不对 RA-CABLE 的有效性作出判断。后续研究应以最简单且有竞争力的基线为起点，通过受控比较逐项验证新增结构的独立价值。

## 参考文献

[1] A. Vaswani, N. Shazeer, N. Parmar, et al., “Attention Is All You Need,” in *Advances in Neural Information Processing Systems*, vol. 30, 2017. [Online]. Available: https://arxiv.org/abs/1706.03762

[2] R. Huben, H. Cunningham, L. Smith, A. Ewart, and L. Sharkey, “Sparse Autoencoders Find Highly Interpretable Features in Language Models,” in *International Conference on Learning Representations*, 2024. [Online]. Available: https://proceedings.iclr.cc/paper_files/paper/2024/hash/1fa1ab11f4bd5f94b2ec20e794dbfa3b-Abstract-Conference.html

[3] D. Chanin, J. Wilken-Smith, T. Dulka, et al., “A Is for Absorption: Studying Feature Splitting and Absorption in Sparse Autoencoders,” in *Advances in Neural Information Processing Systems*, vol. 38, 2025. [Online]. Available: https://proceedings.neurips.cc/paper_files/paper/2025/hash/764ff7477b8e24dbe01531f6791e8bdf-Abstract-Conference.html

[4] G. Paulo and N. Belrose, “Sparse Autoencoders Trained on the Same Data Learn Different Features,” in *International Conference on Learning Representations*, 2026. [Online]. Available: https://iclr.cc/virtual/2026/poster/10010650

[5] N. Elhage, N. Nanda, C. Olsson, et al., “A Mathematical Framework for Transformer Circuits,” *Transformer Circuits Thread*, 2021. [Online]. Available: https://transformer-circuits.pub/2021/framework/index.html

[6] G. Alain and Y. Bengio, “Understanding Intermediate Layers Using Linear Classifier Probes,” *arXiv preprint arXiv:1610.01644*, 2016. [Online]. Available: https://arxiv.org/abs/1610.01644

[7] B. Kim, M. Wattenberg, J. Gilmer, et al., “Interpretability Beyond Feature Attribution: Quantitative Testing with Concept Activation Vectors (TCAV),” in *Proceedings of the 35th International Conference on Machine Learning*, 2018, pp. 2668–2677. [Online]. Available: https://proceedings.mlr.press/v80/kim18d.html

[8] S. Bills, N. Cammarata, D. Mossing, et al., “Language Models Can Explain Neurons in Language Models,” OpenAI, 2023. [Online]. Available: https://openaipublic.blob.core.windows.net/neuron-explainer/paper/index.html

[9] K. R. Wang, A. Variengien, A. Conmy, B. Shlegeris, and J. Steinhardt, “Interpretability in the Wild: A Circuit for Indirect Object Identification in GPT-2 Small,” in *International Conference on Learning Representations*, 2023. [Online]. Available: https://arxiv.org/abs/2211.00593

[10] K. Meng, D. Bau, A. Andonian, and Y. Belinkov, “Locating and Editing Factual Associations in GPT,” in *Advances in Neural Information Processing Systems*, vol. 35, 2022. [Online]. Available: https://proceedings.neurips.cc/paper_files/paper/2022/hash/6f1d43d5a82a37e89b0665b33bf3a182-Abstract-Conference.html

[11] A. Geiger, D. Ibeling, A. Zur, et al., “Causal Abstraction: A Theoretical Foundation for Mechanistic Interpretability,” *Journal of Machine Learning Research*, vol. 26, no. 83, pp. 1–64, 2025. [Online]. Available: https://jmlr.org/papers/v26/23-0058.html

[12] S. Marks, J. Treutlein, T. Bricken, et al., “Auditing Language Models for Hidden Objectives,” *arXiv preprint arXiv:2503.10965*, 2025. [Online]. Available: https://arxiv.org/abs/2503.10965

[13] A. Templeton, T. Conerly, J. Marcus, et al., “Scaling Monosemanticity: Extracting Interpretable Features from Claude 3 Sonnet,” *Transformer Circuits Thread*, 2024. [Online]. Available: https://transformer-circuits.pub/2024/scaling-monosemanticity/

[14] A. Karvonen, C. Rager, J. Lin, et al., “SAEBench: A Comprehensive Benchmark for Sparse Autoencoders in Language Model Interpretability,” in *Proceedings of the 42nd International Conference on Machine Learning*, 2025, pp. 29223–29264. [Online]. Available: https://proceedings.mlr.press/v267/karvonen25a.html

[15] J. Dunefsky, P. Chlenski, and N. Nanda, “Transcoders Find Interpretable LLM Feature Circuits,” in *Advances in Neural Information Processing Systems*, vol. 37, 2024. [Online]. Available: https://proceedings.neurips.cc/paper_files/paper/2024/hash/2b8f4db0464cc5b6e9d5e6bea4b9f308-Abstract-Conference.html

[16] J. Lindsey, A. Templeton, J. Marcus, et al., “Sparse Crosscoders for Cross-Layer Features and Model Diffing,” *Transformer Circuits Thread*, preliminary research note, 2024. [Online]. Available: https://transformer-circuits.pub/2024/crosscoders/index.html

[17] E. Ameisen, J. Lindsey, A. Pearce, et al., “Circuit Tracing: Revealing Computational Graphs in Language Models,” *Transformer Circuits Thread*, 2025. [Online]. Available: https://transformer-circuits.pub/2025/attribution-graphs/methods.html

[18] Y. Gao, Q. Meng, Y. Zhou, and L. Pan, “Towards Intrinsic Interpretability of Large Language Models: A Survey of Design Principles and Architectures,” in *Proceedings of the 64th Annual Meeting of the Association for Computational Linguistics*, 2026, pp. 34741–34754. [Online]. Available: https://aclanthology.org/2026.acl-long.1605/

[19] P. W. Koh, T. Nguyen, Y. S. Tang, et al., “Concept Bottleneck Models,” in *Proceedings of the 37th International Conference on Machine Learning*, 2020, pp. 5338–5348. [Online]. Available: https://proceedings.mlr.press/v119/koh20a.html

[20] A. Tamkin, M. Taufeeque, and N. D. Goodman, “Codebook Features: Sparse and Discrete Interpretability for Neural Networks,” in *Proceedings of the 41st International Conference on Machine Learning*, 2024, pp. 47535–47563. [Online]. Available: https://proceedings.mlr.press/v235/tamkin24a.html

[21] X. Yang, C. Venhoff, A. Khakzar, et al., “Mixture of Experts Made Intrinsically Interpretable,” in *Proceedings of the 42nd International Conference on Machine Learning*, 2025, pp. 71231–71248. [Online]. Available: https://proceedings.mlr.press/v267/yang25ag.html

[22] N. Shazeer, A. Mirhoseini, K. Maziarz, et al., “Outrageously Large Neural Networks: The Sparsely-Gated Mixture-of-Experts Layer,” in *International Conference on Learning Representations*, 2017. [Online]. Available: https://openreview.net/forum?id=B1ckMDqlg

[23] Z. He, J. Wang, R. Lin, et al., “Towards Understanding the Nature of Attention with Low-Rank Sparse Decomposition,” in *International Conference on Learning Representations*, 2026. [Online]. Available: https://openreview.net/forum?id=9A2etpDFIB

[24] J. Su, M. Ahmed, Y. Lu, S. Pan, W. Bo, and Y. Liu, “RoFormer: Enhanced Transformer with Rotary Position Embedding,” *Neurocomputing*, vol. 568, Art. no. 127063, 2024. [Online]. Available: https://doi.org/10.1016/j.neucom.2023.127063

[25] O. Press, N. A. Smith, and M. Lewis, “Train Short, Test Long: Attention with Linear Biases Enables Input Length Extrapolation,” in *International Conference on Learning Representations*, 2022. [Online]. Available: https://openreview.net/forum?id=R8sQPpGCv0

[26] A. Gopalakrishnan, R. Csordás, J. Schmidhuber, and M. C. Mozer, “Decoupling the ‘What’ and ‘Where’ with Polar Coordinate Positional Embeddings,” in *Proceedings of the 43rd International Conference on Machine Learning*, 2026. [Online]. Available: https://arxiv.org/abs/2509.10534

[27] A. Veisi, H. Amirzadeh, and A. M. Mansourian, “Context-Aware Biases for Length Extrapolation,” in *Proceedings of the 2025 Conference on Empirical Methods in Natural Language Processing*, 2025, pp. 30374–30395. [Online]. Available: https://aclanthology.org/2025.emnlp-main.1545/

[28] P.-C. Chen, H. Tsai, S. Bhojanapalli, H. W. Chung, Y.-W. Chang, and C.-S. Ferng, “A Simple and Effective Positional Encoding for Transformers,” in *Proceedings of the 2021 Conference on Empirical Methods in Natural Language Processing*, 2021, pp. 2974–2988. [Online]. Available: https://aclanthology.org/2021.emnlp-main.236/

[29] Y.-A. Wang and Y.-N. Chen, “What Do Position Embeddings Learn? An Empirical Study of Pre-Trained Language Model Positional Encoding,” in *Proceedings of the 2020 Conference on Empirical Methods in Natural Language Processing*, 2020, pp. 6840–6849. [Online]. Available: https://aclanthology.org/2020.emnlp-main.555/

[30] K. Clark, U. Khandelwal, O. Levy, and C. D. Manning, “What Does BERT Look at? An Analysis of BERT’s Attention,” in *Proceedings of the 2019 ACL Workshop BlackboxNLP*, 2019, pp. 276–286. [Online]. Available: https://aclanthology.org/W19-4828/

[31] P. Michel, O. Levy, and G. Neubig, “Are Sixteen Heads Really Better than One?,” in *Advances in Neural Information Processing Systems*, vol. 32, 2019. [Online]. Available: https://proceedings.neurips.cc/paper/2019/hash/2c601ad9d2ff9bc8b282670cdd54f69f-Abstract.html

[32] G. Brunner, Y. Liu, D. Pascual, O. Richter, M. Ciaramita, and R. Wattenhofer, “On Identifiability in Transformers,” in *International Conference on Learning Representations*, 2020. [Online]. Available: https://openreview.net/forum?id=BJg1f6EFDB

[33] S. Jain and B. C. Wallace, “Attention Is Not Explanation,” in *Proceedings of the 2019 Conference of the North American Chapter of the Association for Computational Linguistics: Human Language Technologies*, 2019, pp. 3543–3556. [Online]. Available: https://aclanthology.org/N19-1357/

[34] S. Wiegreffe and Y. Pinter, “Attention Is Not Not Explanation,” in *Proceedings of the 2019 Conference on Empirical Methods in Natural Language Processing and the 9th International Joint Conference on Natural Language Processing*, 2019, pp. 11–20. [Online]. Available: https://aclanthology.org/D19-1002/

[35] M. Geva, R. Schuster, J. Berant, and O. Levy, “Transformer Feed-Forward Layers Are Key-Value Memories,” in *Proceedings of the 2021 Conference on Empirical Methods in Natural Language Processing*, 2021, pp. 5484–5495. [Online]. Available: https://arxiv.org/abs/2012.14913

[36] A. F. T. Martins and R. F. Astudillo, “From Softmax to Sparsemax: A Sparse Model of Attention and Multi-Label Classification,” in *Proceedings of the 33rd International Conference on Machine Learning*, 2016, pp. 1614–1623. [Online]. Available: https://proceedings.mlr.press/v48/martins16.html

[37] B. Peters, V. Niculae, and A. F. T. Martins, “Sparse Sequence-to-Sequence Models,” in *Proceedings of the 57th Annual Meeting of the Association for Computational Linguistics*, 2019, pp. 1504–1519. [Online]. Available: https://aclanthology.org/P19-1146/

[38] C. Song, X. Han, Z. Zhang, et al., “ProSparse: Introducing and Enhancing Intrinsic Activation Sparsity within Large Language Models,” in *Proceedings of the 31st International Conference on Computational Linguistics*, 2025, pp. 2626–2644. [Online]. Available: https://aclanthology.org/2025.coling-main.180/

[39] C. You, K. Wu, Z. Jia, et al., “Spark Transformer: Reactivating Sparsity in Transformer FFN and Attention,” in *Advances in Neural Information Processing Systems*, vol. 38, 2025. [Online]. Available: https://proceedings.neurips.cc/paper_files/paper/2025/hash/220d26c7fb478c2a397bd3fef0fe1d6f-Abstract-Conference.html

[40] J. Vig and Y. Belinkov, “Analyzing the Structure of Attention in a Transformer Language Model,” in *Proceedings of the 2019 ACL Workshop BlackboxNLP: Analyzing and Interpreting Neural Networks for NLP*, 2019, pp. 63–76. [Online]. Available: https://aclanthology.org/W19-4808/

[41] M. Madhyastha, D. Haziza, J. Cai, et al., “To 2:4 Sparsity and Beyond: Neuron-level Activation Function to Accelerate LLM Pre-Training,” *arXiv preprint arXiv:2602.06183*, 2026. [Online]. Available: https://arxiv.org/abs/2602.06183

[42] S. Chen, S. Wong, L. Chen, and Y. Tian, “Extending Context Window of Large Language Models via Positional Interpolation,” *arXiv preprint arXiv:2306.15595*, 2023. [Online]. Available: https://arxiv.org/abs/2306.15595

[43] P. Dasigi, K. Lo, I. Beltagy, A. Cohan, N. A. Smith, and M. Gardner, “A Dataset of Information-Seeking Questions and Answers Anchored in Research Papers,” in *Proceedings of the 2021 Conference of the North American Chapter of the Association for Computational Linguistics: Human Language Technologies*, 2021, pp. 4599–4610. [Online]. Available: https://aclanthology.org/2021.naacl-main.365/
